# v0.9.30 Billing Capability Evidence Reconciliation / 组织计费能力证据对账

- Date / 日期: 2026-09-24
- Status / 状态: Production capability confirmed; phase 8 closeout pending / 生产能力已确认；阶段 8 收口待完成
- Current delivery branch / 当前交付分支: `codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
- Current branch commit / 当前分支提交: `6911aaf`
- Billing implementation commit / 账单实现提交: `59c79e0`

## What is present / 已具备能力

The current production delivery branch includes schema v31, append-only organization billing policies, immutable monthly statements, BigInt-safe fee calculation, own-organization and platform-admin reads, optimistic policy updates, bounded audit events, compact organization-directory controls, and a root-only acceptance verifier.

当前生产交付分支包含 schema v31、只追加组织计费策略、不可变月度账单、BigInt 安全费用计算、本组织与平台管理员读取、乐观版本更新、有界审计事件、组织目录紧凑控制和 root-only 验收脚本。

## Evidence / 证据

- Focused billing suite: `27/27` passed, covering migration, immutability, month resolution, integer rounding, tenant authorization, exact request bodies, UI contract, acceptance verification, and release inventory.
- Syntax checks passed for `platform/app.js`, `platform/server/server.js`, `platform/server/services/organization_billing_service.js`, and `platform/server/routes_organization_billing.js`.
- Public read-only checks on 2026-09-24: `/api/health` returned `200`; unauthenticated `/api/organization-billing` returned `401 No token provided`; public `app.js` contained the billing policy route marker.
- Existing release evidence in `CHANGELOG.md` references the billing acceptance digest `0c9a338005419f244ffcf7cc949320b2c1a23bb2af933dee1c403f1458591307`; this reconciliation record does not regenerate or overwrite that protected evidence.

## Safety boundary / 安全边界

- No production write, credential rotation, password-hash mutation, or `derrick` smoke test was performed during this reconciliation.
- The release-smoke identity remains the only permitted acceptance identity for future billing release work; login failure must fail closed.
- This record does not claim a new deployment run. The remaining work is Phase 8 closeout: independent final review, complete release evidence reconciliation, external archive synchronization, and only then any separately authorized guarded release.

## Explicit exclusions / 明确不包含

No payment collection, invoice generation, tax/VAT, refunds, credits, manual adjustments, provider-cost pass-through, dynamic plan pricing, or payment gateway integration is included.
