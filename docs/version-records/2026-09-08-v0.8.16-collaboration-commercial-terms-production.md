# v0.8.16 Collaboration Commercial Terms Production Record / 合作商业条款生产记录

## Release / 发布

- Date / 日期: `2026-09-08`
- Scope / 范围: Phase 7 M4 collaboration commercial terms contract and currency-safe summaries / 阶段 7 M4 合作商业条款合同与安全多币种汇总
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Plan / 计划: `b8b024d`
- Implementation and fixes / 实现与修正: `98df478`, `d742137`, `4a5f6f7`, `96cebc1`, `06b5c99`, `da75940`
- Database / 数据库: schema remains `v15`; no migration / schema 保持 `v15`，无迁移
- Production deployment run / 生产发布运行: `a5fbbcc4f3d04d4982285d20987299b5`

## Delivered / 已交付

- Collaboration orders now carry an immutable `collaboration.resource.v2` commercial contract with creator cost, client quote, normalized currency, server-derived margin, and payment terms. / 合作订单新增不可变 `collaboration.resource.v2` 商业合同，分别保存网红成本、客户报价、规范化币种、服务端计算毛利和付款条件。
- The compatibility projection continues to write creator cost to the historical `cost_quoted` field, so legacy rows and consumers remain readable. Both campaign-linked and standalone creation reject reserved v2 payloads before writes or resource reservation. / 兼容投影继续把网红成本写入历史 `cost_quoted` 字段，因此旧记录和旧调用方仍可读取；关联活动和独立下单均在写入或资源预占前拒绝非法保留 v2 数据。
- M4 exposes the five commercial fields without replacing the approved interface. Currency input is normalized to uppercase, and valid zero cost, quote, margin, and settlement values remain visible. / M4 在既有批准界面内展示五项商业字段；币种统一为大写，合法的零成本、零报价、零毛利和零结算值均不会被误判为空。
- Collaboration statistics group costs by currency. One-currency results keep a total and currency; mixed-currency results return `totalCost: null`, `totalCostCurrency: null`, and a complete `costByCurrency` breakdown. / 合作统计按币种分组；单币种继续返回总额和币种，多币种返回空总额、空总额币种以及完整 `costByCurrency` 明细。
- The latest M4 shell, upload/search/export/Feishu controls, AI and knowledge integration, and frozen proposal PPT remain unchanged. / 最新 M4 壳层、上传/搜索/导出/飞书控制、AI 与知识关联及冻结方案 PPT 均保持不变。

## Focused Verification And Independent Review / 聚焦验证与独立审查

- The affected controller, service, security, workflow, and client contract matrix passed `101/101`; the final zero-value display regression matrix passed `12/12`. / 受影响控制器、服务、安全、工作流及前端合同矩阵 `101/101` 通过；最终零值展示回归矩阵 `12/12` 通过。
- JavaScript syntax, `git diff --check`, focused secret scanning, frozen PPT hash, and guarded deployment preflight passed. / JavaScript 语法、`git diff --check`、定向密钥扫描、冻结 PPT 哈希和受保护部署预检均通过。
- Independent review checked legacy validation precedence, immutable terms, zero preservation, currency normalization, mixed-currency summaries, and pre-write rejection. The final verdict was `APPROVE`, with no open high-risk finding. / 独立审查覆盖历史校验优先级、不可变条款、零值保留、币种规范化、多币种汇总和写入前拒绝；最终结论为 `APPROVE`，无未关闭高风险问题。
- The production candidate passed real Express replay `8/8`, release gate `21/21`, and built-in browser smoke `2/2`. / 生产候选通过真实 Express 回放 `8/8`、发布守卫 `21/21` 和内置浏览器冒烟 `2/2`。

## Production Evidence / 生产证据

- Recoverable backup / 可恢复备份: `/root/turingmarket/backups/v060-crm-sales-workspace-20260908-142825`
- Backup checksum manifest / 备份校验清单: `296` entries; SHA-256 `238b185436218b6727e0635d2abc7c6d6f520b95d86c5a8b0f2814cde0295b10` / 共 `296` 项
- Candidate tree SHA-256 / 候选树摘要: `5460c48354e0f245ae4edbfcefbfb10e63a70bf140760323f176a9bdbcdc262f`
- Accepted parser runtime SHA-256 / 已接受解析器运行时摘要: `04630b2ee928ba8031a4ea5ce6435519161c19e05884813e5ec623d12adb38ce`
- Public `/api/health` and `/m4` returned `200`; PM2 `turingmarket` is `online`; Nginx points to `/etc/nginx/sites-available/turingmarket`. / 公网 `/api/health` 与 `/m4` 返回 `200`；PM2 `turingmarket` 为 `online`；Nginx 指向 `/etc/nginx/sites-available/turingmarket`。
- Authenticated production checks confirmed the administrator can log in, the collaboration list returns the existing three records, and statistics expose `costByCurrency` plus `totalCostCurrency`. / 登录态生产检查确认管理员可登录，合作列表可返回既有三条记录，统计接口包含 `costByCurrency` 和 `totalCostCurrency`。
- SQLite schema is `v15`, `quick_check=ok`, foreign-key violations are `0`, and the knowledge FTS projection is `914/914`. / SQLite schema 为 `v15`，`quick_check=ok`、外键异常 `0`，知识 FTS 投影为 `914/914`。
- The requested administrator credential was restored through a focused private backup and an audited recovery action; the password is not copied into release artifacts. / 用户要求的管理员凭据通过定向私有备份和带审计恢复操作完成，版本产物不复制密码。
- Frozen proposal `ppt.js` SHA-256 remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 冻结方案 `ppt.js` 摘要保持不变。

## Controlled Recovery / 受控恢复

- The first candidate preflight detected a stale knowledge FTS projection (`914` chunks versus `915` indexed rows) and then the parser disk-safety floor blocked restart. That candidate was not accepted as the new production version. / 首个候选预检发现知识 FTS 投影不一致（`914` 个切片、`915` 条索引），随后解析器磁盘安全下限阻止重启；该候选未被接受为新生产版本。
- The FTS index was rebuilt to `914/914`, SQLite integrity and foreign keys were rechecked, and the prior accepted version was restored. Fourteen obsolete, unreferenced parser experiment copies were then removed, releasing `3,681,824,768` bytes before a fresh formal deployment. / FTS 索引重建为 `914/914`，重新检查 SQLite 完整性和外键并恢复上一已接受版本；随后删除 `14` 份未被引用的历史解析器实验副本，释放 `3,681,824,768` 字节，再执行全新正式发布。
- Recovery evidence is retained under `/root/turingmarket/deployment-evidence/`; no business data was lost. / 恢复证据保存在 `/root/turingmarket/deployment-evidence/`，没有业务数据丢失。

## Delivery Cadence And Next / 发布节奏与下一步

- Normal features now use the approved small-batch cadence: exact affected tests, syntax/diff/secret checks, one independent review, recoverable backup, immediate production deployment, and online core-path smoke. Full suites are reserved for schema, authentication, security, shared runtime, external writes, broad cross-module changes, scheduled windows, and phase closeout. / 普通功能改用已批准的小步节奏：执行精确受影响测试、语法/差异/密钥检查、一次独立审查、可恢复备份、立即生产部署和线上核心路径冒烟；完整套件仅用于 schema、鉴权、安全、共享运行时、外部写入、大范围跨模块改动、计划窗口及阶段收口。
- This slice completes commercial terms only. Negotiation, contract, content review, publish, payment, settlement, and review remain separate Phase 7 feature releases. / 本切片只完成商业条款；谈判、签约、内容审核、发布、付款、结算和复盘继续作为阶段 7 的独立功能发布。
- Real configured Feishu delivery remains separately gated by approved production configuration and a test campaign. / 真实配置飞书投递仍须具备获批生产配置和测试活动后独立验收。
