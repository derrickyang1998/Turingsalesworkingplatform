# TuringMarket v0.8.7 Production Deployment Record

## Release

- Release: `v0.8.7-performance-ai-review-approval`
- Phase: `7B.3c`
- Commit: `733b900a4e12536fd4999eae0b251fd96d5737cd`
- Date: `2026-09-04`
- Status: production deployment completed; administrator access acceptance pending a compliant recovery credential.

## Delivered

- Added the protected campaign route `POST /api/campaigns/:id/performance/ai-review-draft/approve`.
- Campaign owners and organization administrators can human-edit a validated AI performance-review draft, choose private or team visibility, and confirm one immutable campaign-local knowledge artifact.
- Confirmation rechecks AI conversation/message lineage, campaign custody, current evidence snapshot, all required `[PERF-n]` citations, safe metadata-only language and idempotency/replay rules.
- The original generated draft remains immutable. The archived knowledge carries original/final content hashes, evidence snapshot hash and source identifiers without promoting the result across campaigns.

## Explicit Non-Goals

- No schema migration, report/PPT generation, video or media collection, hook/style/cause analysis, web search, external provider call, Feishu read/write, scheduled synchronization, or production business-data mutation for release verification.

## Focused Verification And Review

- Feature AI/RAG, performance route and frontend-contract matrix: `53/53` passed.
- Trusted production-source matrix: `32/32` passed. Deployment source-contract matrix: `57/57` passed.
- JavaScript syntax checks, `git diff --check`, targeted new-secret scan and `deploy_v8.ps1 -ValidateLocalOnly`: passed.
- Independent code review: `APPROVE` after all identified safety, replay and busy-state issues were fixed and regression-covered.

## Production Evidence

- Backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260904-160643`
- Backup manifest SHA-256: `683917375944e6db1ee40e91a2f37078a4a78e099f066832f7fd441ebae2a7e5`
- Public `/api/health`: `200`, `ok`, parser ready. Public `/performance-dashboard`: `200`.
- Nginx active; PM2 `turingmarket` online with zero restarts after cutover.
- The ten reviewed runtime/source-manifest files match the deployed SHA-256 values.
- SQLite readonly evidence: schema migration `v12`, `quick_check=ok`, foreign-key violations `0`, `performance_ai_review_audits` present.
- Anonymous approval-route smoke: `401`; no real campaign knowledge mutation was made during production verification.

## Remaining Acceptance Item

The deployment itself is complete. The administrator login, identity and logout smoke remains pending because the previously requested recovery password violates the current production password policy. The standard reset service rejected it, and the security policy was not bypassed. On receipt of a user-selected compliant password, this record will receive an acceptance addendum after login, identity, protected-route and logout verification.
