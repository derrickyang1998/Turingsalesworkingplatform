# v0.8.25 Performance Freshness Update Queue Production Record / 效果新鲜度更新队列生产记录

## Release / 发布

- Date / 日期: `2026-09-10`
- Scope / 范围: provider-independent freshness status and manual update queue inside the existing Content Monitor / 既有内容监控内的服务商无关新鲜度状态与人工更新队列
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Implementation / 实现: `a7cbeba`
- Production run / 生产运行: `86b2f1b93e5341f49022c57e66d2bc54`
- Candidate / 候选: `v060-crm-sales-workspace-20260910-205839`
- Database / 数据库: migrations `17/17`, schema `v17` unchanged / 保持不变

## Delivered / 已交付

- Added a compact freshness summary and update queue to the existing monitored-content surface. It reuses the current manual-input interaction and does not add a route, replace the approved shell, or alter proposal/PPT behavior. / 在既有已监控内容界面增加紧凑的新鲜度概览和更新队列，复用当前人工补录交互，不新增页面、不替换已批准壳层，也不改变方案/PPT。
- The backend evaluates the campaign's complete authorized performance projection into `current`, `due`, `stale`, `unobserved`, `date_required`, `data_issue`, `not_started`, and `monitoring_complete`. Cadence is deterministic: 6 hours through 72 hours, 12 hours through day 14, daily through day 45, weekly through day 180, then complete. / 后端基于活动完整授权投影计算八种状态；节奏依次为 72 小时内每 6 小时、14 天内每 12 小时、45 天内每日、180 天内每周，随后结束。
- Queue entries inherit existing Campaign authorization and redaction. The client sequence-guards campaign changes and stale responses, safely renders only HTTP(S) links, and hydrates the authoritative content snapshot before opening manual input even when the item is outside the current filtered page. / 队列继承既有 Campaign 权限与脱敏；客户端隔离活动切换和陈旧响应，只允许 HTTP(S) 外链，并在目标不在当前筛选页时先取得权威内容快照再补录。
- Provider state remains truthfully `not_configured`, with no fabricated provider success or next-run timestamp. This release performs no automatic collection, scheduler dispatch, live Feishu write, or schema migration. / Provider 状态真实保持未配置，不伪造成功或下次运行时间；本版本不执行自动采集、调度、真实飞书写入或迁移。

## Focused Verification And Review / 聚焦验证与审查

- The affected freshness service, route, existing performance contract, frontend behavior, and request-pipeline matrix passed `59/59`. JavaScript syntax, diff formatting, focused credential scan, frozen-PPT hash, and local deployment preflight passed. / 新鲜度服务、路由、既有效果合同、前端行为和请求流水线受影响矩阵 `59/59` 通过；语法、差异、定向凭据、冻结 PPT 和本地发布预检通过。
- Two independent reviews returned `PASS`. The first review's URL-scheme and provider-timestamp findings were fixed and re-reviewed; no P0-P2 remains. / 两次独立审查均为 `PASS`；首轮外链协议与 provider 时间来源问题已修复并复审，无剩余 P0-P2。
- The production candidate passed real Express replay `8/8`, release guard `21/21`, built-in browser smoke `2/2`, migration rehearsal, parser cache reuse, Nginx checks, and final acceptance. / 生产候选通过真实 Express 回放、发布守卫、内置浏览器冒烟、迁移演练、解析器缓存复用、Nginx 和最终验收。

## Production Evidence / 生产证据

- Guarded candidate SHA-256: `c9de61c1057b4121120f8206d131501eebeeb2789cca2a4629dd1e49e815584c`. Recoverable backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260910-205839`. / 受保护候选与可恢复备份均已保留。
- Initial and cutover manifests contain 304 and 35 entries; SHA-256 values are `83c022bd5248140f3d5240c1173a0f4c68b101fe7b20e5e3503ad212dd728c97` and `96585b9c0a6da39b914096e491f4c9a42f74bc9787ba1ac3b86d5cb750f9bbd0`. / 初始与切换清单均已复核。
- Public root and health returned `200`; the new route returned `401` without authentication. Production administrator login and authenticated campaign `1` queue retrieval returned `200`, with `performance-freshness-queue-v1`, one actionable item, and provider dispatch disabled. / 公网、未登录拒绝、管理员登录及真实活动队列合同均通过。
- PM2 is online with restart count `0`; Nginx is active. SQLite reports migrations `17/17`, `integrity_check=ok`, and zero foreign-key violations. Deployed `app.js`, `server.js`, route, and freshness service hashes match reviewed local files. / PM2、Nginx、数据库及线上文件哈希均通过。
- Frozen `ppt.js` remains SHA-256 `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 冻结方案 PPT 保持不变。

## Release Incident And Cadence / 发布事件与节奏

- The first candidate stopped before production mutation because available disk space was about 3 MB below the parser safety threshold. Removing the failed pre-cutover backup and vacuuming system journals/package cache reclaimed `668,880,896` bytes; the second candidate passed without relaxing any gate. / 首次候选因可用空间比解析器安全阈值少约 3 MB 而在切换前停止；清理失败候选备份及系统日志/包缓存后回收 `668,880,896` 字节，第二候选在未放宽门禁的前提下通过。
- Next slice: one explicitly approved provider plus scheduler/run history; live Feishu idempotent writes remain separately gated by production configuration. Normal slices continue focused checks, one independent review, verified backup, immediate deployment, and online feature smoke. / 下一切片为一个明确批准的 provider 与调度/运行历史；真实飞书写入继续等待独立门禁。普通切片保持定向检查、一次独立审查、可验证备份、立即上线和线上功能冒烟。
