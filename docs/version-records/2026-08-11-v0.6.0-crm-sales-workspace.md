# v0.6.0 CRM Sales Workspace / CRM 销售工作台

Date / 日期: 2026-08-11
Candidate update / 候选更新: 2026-08-24
Status / 状态: Release candidate, not production / 发布候选，尚未生产切换
Branch / 分支: `codex/v0.6.0-crm-sales-workspace`

## Delivery / 交付

- Customer dashboard and customer detail remain separate routes and screens. / 客户看板与客户明细继续保持独立路由和界面。
- Organization-safe customer/opportunity reads, team and personal scopes, governed lifecycle transitions, contacts, tasks, evidence, claim/release, public-pool handling, and audit events are integrated. / 客户与商机读取、团队/个人作用域、受治理生命周期、联系人、任务、证据、领取/释放、公海与审计已统一接入组织权限。
- Follow-up notes append to the knowledge base; delegated teammates cannot replace owner strategy or proposal artifacts. / 跟进备注以追加方式进入知识库；团队协作者不能替换负责人的策略或方案正式成果。
- The latest product shell, influencer workflow, AI/knowledge paths, proposal/export behavior, Feishu integration points, and PPT bridge remain intact. / 最新产品壳层、网红执行、AI/知识库、方案/导出、飞书接入点与 PPT 桥保持不回退。
- Customer updates accept an optional exact `expected_notes` precondition. Stale writes fail as bounded `CRM_CUSTOMER_CONFLICT` 409 responses, while release smoke writes and restores use the same atomic SQLite compare-and-set contract. / 客户更新支持精确的可选 `expected_notes` 前置条件；陈旧写入以受限 409 冲突失败，发布冒烟写入与恢复共用同一 SQLite 原子比较更新合同。

## Data And Release Contract / 数据与发布合同

- Migration: `006_crm_sales_workspace`.
- Trusted migration: exact managed v1 source to schema 6, two restored runs, legacy preservation required.
- Trusted source: 49 SHA-256-pinned files, including the one-time legacy v0->v1 adoption, migration 006, CRM services, the current trusted runtime, the parser CA and `sitecustomize.py`, the independent public release guard, and the migration cleanup helper/unit.
- App build/query: `20260811-v060-crm-sales-workspace` / `20260811v060crmsalesworkspace`.
- Frozen PPT: build `20260702-v916-kb-bridge-client-cn`, query `20260702v916kbbridge`, SHA-256 `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`.

## Parser Runtime Appliance / 解析器运行时设备

The current candidate adds an independently built and sealed upload parser for knowledge, influencer, and demand-file routes. This is an implemented release control, not evidence of production installation. / 当前候选为知识库、网红和需求文件路由增加独立构建、密封的上传解析器；这是已实现的发布控制，不代表已在生产安装。

- Build platform / 构建平台: Linux `x86_64`, Node.js `v20.20.2`, Python `3.14.4`.
- Runtime tree / 运行时树: 3,476 files, 435 directories, 640,592,018 bytes; SHA-256 `20b5f5186ec26b726d07659566071c0ce6b367138497f772d57830734f5d4418`; Python bytecode caches are excluded / 不包含 Python 字节码缓存.
- Manifest / 清单: `turingmarket-parser.manifest.json` SHA-256 `44db310046efe65bd68c110313b4887995c73e276e7d58f65fe037c09a973c5b`.
- Parser staging / 解析暂存: the worker writes the already integrity-verified input buffer exclusively to `/scratch`; this avoids the denied `copy_file_range` fast path without widening the systemd sandbox / worker 将已完成完整性校验的输入字节独占写入 `/scratch`，不放宽 systemd 隔离策略。
- OCR acceptance / OCR 验收: the BMP fixture zero-initializes its header and fills only the raster white, while marker comparison collapses equivalent whitespace. The production-matched RapidOCR runtime recognizes the fixture as `OCR\n123`, which now correctly proves the `OCR 123` marker without changing the parser runtime or systemd policy. / BMP 夹具只填白像素区并保持文件头合法，标记比较折叠等价空白；生产同构 RapidOCR 返回 `OCR\n123` 时可正确证明 `OCR 123`，不修改解析器运行时或 systemd 策略。
- Build/install authority / 构建安装权限: the release workflow builds, snapshots, installs, and rolls back only from root-owned lifecycle paths; provisioning rejects non-Linux or non-root execution. / 发布流程只允许在 root 所有的生命周期路径内构建、快照、安装和回滚；非 Linux 或非 root 安装会被拒绝。

### Exact Node closure / 精确 Node 闭包

`package-lock.json` SHA-256: `e1d6e5ababef1fb6f0aa69363be328e4da42283c96e268378c2f83076722fc46`.

| Package / 包 | Exact version / 精确版本 |
|---|---:|
| `read-excel-file` | `9.2.0` |
| `@xmldom/xmldom` | `0.9.11` |
| `fflate` | `0.8.3` |
| `unzipper-esm` | `0.13.3` |
| `graceful-fs` | `4.2.11` |
| `node-int64` | `0.4.0` |

The builder runs `npm ci --omit=dev --ignore-scripts`; no additional npm package belongs in the appliance. / 构建器执行 `npm ci --omit=dev --ignore-scripts`，设备中不允许出现额外 npm 包。

### Exact Python closure / 精确 Python 闭包

`requirements.lock` SHA-256: `22f72070ee7c62b428261b64435fbb0b24d87f0aaa7a4d0ac0d8e7a45bfb44df`. Every entry is exact and hash-pinned, and installation requires binary wheels with `--require-hashes --only-binary=:all: --no-deps`. / 每个条目均锁定精确版本与 SHA-256；安装只接受二进制 wheel，并启用 `--require-hashes --only-binary=:all: --no-deps`。

| Package / 包 | Exact version / 精确版本 | Package / 包 | Exact version / 精确版本 |
|---|---:|---|---:|
| `certifi` | `2026.7.22` | `charset-normalizer` | `3.5.0` |
| `colorlog` | `6.12.0` | `flatbuffers` | `25.12.19` |
| `idna` | `3.18` | `numpy` | `2.5.2` |
| `omegaconf` | `2.4.0.dev13` | `onnxruntime` | `1.28.0` |
| `opencv-python` | `5.0.0.93` | `packaging` | `26.3` |
| `pillow` | `12.3.0` | `protobuf` | `7.35.1` |
| `pyclipper` | `1.4.0` | `PyMuPDF` | `1.27.2.3` |
| `pypdf` | `6.14.2` | `PyYAML` | `6.0.3` |
| `rapidocr` | `3.9.2` | `requests` | `2.34.2` |
| `shapely` | `2.1.2` | `six` | `1.17.0` |
| `tqdm` | `4.70.0` | `typing_extensions` | `4.16.0` |
| `urllib3` | `2.7.0` |  |  |

### Isolation and self-tests / 隔离与自检

- Each parse runs as locked, no-login `turingmarket-parser` in a systemd 257+ oneshot unit with `RootDirectory=/var/lib/turingmarket-parser/runtime-root`. Private network, PID, IPC, user, and mount namespaces; an empty capability set; socket and syscall denials; a read-only root; and CPU, memory, task, file, and scratch limits constrain the job. / 每个解析任务以锁定且禁止登录的 `turingmarket-parser` 身份运行在 systemd 257+ oneshot unit 中，并使用上述 chroot、私有命名空间、空 capability、socket/系统调用拒绝、只读根目录及资源上限。
- An unprivileged build unit initializes the real RapidOCR engine against the staged dependency tree before sealing and hashing. Release tests exercise `--self-test-image` with a raster image for RapidOCR 3 object output and the legacy tuple path. / 非特权构建单元在密封并哈希前针对暂存依赖树初始化真实 RapidOCR；发布测试使用栅格图像覆盖 RapidOCR 3 对象输出及旧 tuple 路径。
- Provisioning and acceptance require all 21 self-tests, including XLSX, PPTX, OCR inference, identity, mount, syscall, network, socket, PID, write-boundary, and resource-pressure checks. Any missing or false result fails closed. / 安装与验收要求包含 XLSX、PPTX、OCR 推理及隔离边界在内的 21 项自检全部为真；缺项或失败均关闭发布。

## Cutover Contract / 切换合同

- Cleanup-control replay accepts only the canonical absent/enabled topologies, idempotently resolves the one exact `.restore.next` residue, validates all control-path parents before mutation, and binds effective PID 1 fragment/Names/DropInPaths across every systemd search root. Public watchdog deadlines are operation-specific: 120 seconds for initial cutover and 7,200 seconds for accepted finalization plus rollback/resume; identity is revalidated before each public link swap and disarm. / 清理控制回放仅接受两种 canonical topology，并在首次变更前验证父链及 PID 1 实际配置；公网 watchdog 的操作专属窗口为初始切换 120 秒、接受态 finalization 与回滚/恢复 7,200 秒，公网切换及解除前均复核 active identity。
- External state now has five targets: four exact root-owned links for environment, database, uploads, and temporary files, plus the direct root-only `PPT_CACHE_DIR=/var/lib/turingmarket/ppt-cache` directory. The PPT cache is not a release-tree symlink. / 外置状态现有五个目标：环境、数据库、上传和临时文件使用四个精确 root 所有软链接，PPT 缓存则直接使用 root-only 目录；PPT 缓存不是发布树软链接。
- A first-install backup records `ppt-cache.absent`. Only after PM2 writers stop does cutover create an empty `root:root 0700` cache. Pre-mutation recovery removes it only while empty; rollback preserves the recorded absent origin only when the restored cache is also empty. / 首次安装备份记录 `ppt-cache.absent`；只有 PM2 停写后才创建空的 `root:root 0700` 缓存。预变更恢复仅在其仍为空时删除，回滚也只在恢复缓存为空时恢复 absent 起源。
- The capacity planner groups paths by filesystem device and includes the database, PPT cache, existing and staged parser runtime, rollback staging, and `node_modules` growth. Each device must meet required bytes plus `max(10%, 512 MiB)` and emit `CUTOVER_CAPACITY_OK` before maintenance or mutation. / 容量规划器按文件系统设备汇总数据库、PPT 缓存、现有与待装解析器、回滚 staging 和 `node_modules` 增量；每个设备都必须满足所需字节加 `max(10%, 512 MiB)` 并在维护或变更前输出 `CUTOVER_CAPACITY_OK`。
- Ordering is fixed: enter all-traffic maintenance, drain parser admissions/spool/units/cgroup, stop and quiesce PM2 plus SQLite, drain the parser again, prepare the PPT cache, snapshot the database/cache/parser as one verified unit, exchange code, install the parser, start the candidate, and run acceptance. / 顺序固定为全流量维护、解析器 admission/spool/unit/cgroup 排空、PM2 与 SQLite 停写静默、再次排空解析器、准备 PPT 缓存、数据库/缓存/解析器同单元快照、交换代码、安装解析器、启动候选并验收。
- `/api/health` exposes `parser.ready` and `parser.manifest_sha256`. Candidate health requires `status=ok`, `parser.ready=true`, and the release manifest SHA-256. Installed self-test evidence, acceptance facts, and runtime-tree SHA-256 are bound into schema 3 accepted markers and rechecked before public traffic is restored. / `/api/health` 暴露 `parser.ready` 与 `parser.manifest_sha256`；候选健康要求 `status=ok`、解析器 ready 及精确发布清单哈希。安装自检、验收事实和运行时树哈希写入 schema 3 验收标记，并在恢复公开流量前重新核验。

## S6 Accepted Checkpoint Evidence / S6 已验收检查点证据

- S6 commit: `90713b23f417602045d144ca24e80555ff1580b2`.
- Independent Code Review: `APPROVE`.
- Independent QA: `GO`.
- Final review matrix: 342 passed, 0 failed.
- Fresh pre-commit matrix: 373 passed, 0 failed.
- Recovery bundle: `phase5-v060-90713b23f417-full-source.bundle`.
- Bundle SHA-256: `fd4b925d82056d1eb53a5c65cc67461bc3eadd9ba88f47020ed37343d5dae887`.

These verdicts apply to the accepted S6 checkpoint. The current release candidate includes later release-gate fixes for cross-platform trusted hashes, populated schema 6 CRM sanitization, probability-domain preservation, deploy inventory, and the parser/cutover controls above, so the latest feature slice requires its own focused review before production. Full review remains a phase-closeout or broad-risk gate. / 上述结论仅适用于已验收的 S6 检查点；当前发布候选此后又加入跨平台可信哈希、已填充 schema 6 CRM 脱敏、概率域保持、部署清单修复和上述解析器/切换控制，因此最新功能切片上线前必须完成自己的定向复审；全量复审保留为阶段收口或高风险跨模块门禁。

## Current Candidate Evidence / 当前候选证据

- Parser staging remediation / 解析暂存修复: the denied `copy_file_range` path was reproduced before the change and passes after the worker reuses the integrity-verified buffer. Focused CSV/XLSX/PPTX, zip-bomb, and trusted-artifact tests are `5/5`; Phase 4 integration is `32/32`; local deploy preflight, hash closure, diff, and focused secret scan pass. Independent code review: `APPROVE`, with no findings. Five broad legacy contract failures reproduce unchanged on baseline `e4ca1ce`. / 已完成 RED/GREEN、定向与集成验证，独立审查无发现；五项宽范围旧合同失败可在未修改基线复现。
- OCR acceptance remediation / OCR 验收修复: the first guarded cutover after parser staging remediation passed isolated XLSX and PPTX jobs, then failed closed on the BMP OCR fixture. Automatic rollback restored verified v0.4 and both public and loopback health returned `ok`. RED/GREEN header and whitespace regressions are `26/26`, and the same Linux RapidOCR runtime recognizes `OCR\n123` with no warning. / 首次受控切换通过 XLSX/PPTX 后在 BMP OCR 夹具处关闭发布，自动回滚恢复 v0.4；头部与空白匹配回归 26/26，生产同构 RapidOCR 实测无警告识别 `OCR\n123`。
- Runtime evidence diagnostics / 运行时证据诊断: the next guarded cutover failed closed before parser installation because runtime evidence no longer matched its pinned projection, then automatically restored healthy v0.4. Explicit diagnostic mode now emits only canonical expected/observed/provided runtime identity facts; normal mode remains bounded and the comparison is unchanged. / 下一次受控切换在解析器安装前因运行时证据不一致而关闭发布，并自动恢复健康 v0.4；显式诊断模式现只输出 canonical 身份事实，普通模式和判定逻辑保持不变。
- PID isolation diagnostics / PID 隔离诊断: the following attempt passed runtime identity plus XLSX/PPTX/OCR acceptance, then failed closed at the first sibling-PID proof and restored healthy v0.4. Explicit diagnostic mode now emits only fixed namespace PID and host cgroup projections; normal CLI output, validation, and systemd isolation are unchanged. / 后续尝试通过运行时身份及 XLSX/PPTX/OCR 验收后，在首次同级 PID 证明处关闭并恢复健康 v0.4；仅显式诊断模式输出固定 PID/cgroup 投影，普通输出、判定与隔离策略不变。
- Source/snapshot digest separation / 原库与快照摘要分离: the next guarded attempt passed migration rehearsal, real Express replay `8/8`, release guard `21/21`, browser smoke `2/2`, and candidate construction, then failed closed because live adoption incorrectly required SQLite's online-backup bytes to equal the stopped source file. Rollback restored healthy v0.4. Cutover now binds and verifies the stopped live-source SHA-256 independently from the recovery-snapshot SHA-256. / 下一次受控尝试通过前述候选门禁后，因错误要求 SQLite 在线备份与停写原库字节哈希相同而关闭并恢复健康 v0.4；现分别绑定并验证原库与恢复快照 SHA-256。
- Post-upload amount repair / 上传后金额修复: the following attempt failed closed before production mutation because a 13:05 legacy-template upload added three negative amount rows beyond the original frozen repair sample. Production v0.4 remained healthy. Adoption now admits only the verified original one-row or exact current four-row profiles and converts their magnitudes to nonnegative values in the migration copy; future influencer imports normalize cost, quoted price, CPM, and CPV to nonnegative magnitudes. Focused RED/GREEN behavior tests, trusted-source `29/29`, and local deploy preflight pass. / 后续尝试因旧模板上传新增三条负数金额记录而在生产变更前关闭；接管仅接受已核验的原 1 条或当前精确 4 条形态，并在迁移副本中转为非负幅度，新版导入同时阻止再次写入负数金额。
- Concatenated quote repair / 拼接报价修复: the subsequent rehearsal admitted the four-row profile, then failed closed on the same upload's `id=2740` quote stored as an oversized REAL by the legacy parser. The exact row alone is now restored to first-tier quote `8000`, with CPM/CPV recomputed from 49,000 average views; every other REAL quote shape remains rejected and the source database stays immutable. / 后续演练已通过四行白名单，随后识别出同批 `id=2740` 的旧解析器拼接报价为超大 REAL；仅该精确记录恢复为首档 8000 并按 49000 均播重算指标，其他 REAL 形态继续失败关闭，源库保持不变。
- Adoption report contract / 接管报告契约: the guarded retry passed candidate migration, replay guards, and the minimal browser smoke before entering atomic cutover. The trusted adopter correctly reported five repaired influencer rows for the current frozen profile, while the release wrapper still accepted only the historical one-row report; the release failed closed and automatic rollback restored healthy v0.4. The wrapper now accepts only the two exact trusted reports (one or five influencer rows), with all other counts rejected. Focused deployment-source tests are `30/30`; PowerShell AST and local deploy preflight pass. / 重发已通过候选门禁并进入原子切换；接管器正确报告当前冻结画像修复 5 条网红记录，但包装器仍只接受历史 1 条报告，因此失败关闭并自动恢复健康 v0.4。现仅接受 1 条或 5 条两份精确受信任报告，其他数量继续拒绝；发布源定向测试 `30/30`、PowerShell AST 与本地预检通过。
- Parser mountpoint sealing / 解析器挂载点密封: the next guarded retry passed live adoption, then failed closed because first service startup added four mount directories and two zero-byte bind targets beneath the sealed parser root; rollback restored healthy v0.4. An isolated remote probe identified exact `0755` directories `/input`, `/output`, `/runtime`, `/scratch` and `0644` files `/input/input.bin`, `/runtime/request.json`. The builder now creates these systemd-required targets before sealing, fixing the tree at 3,476 files, 435 directories, 640,592,018 bytes, SHA-256 `20b5f5186ec26b726d07659566071c0ce6b367138497f772d57830734f5d4418`. The first rebuild retry failed before production mutation because those staging directories had already been set to `0555`; the unprivileged build now keeps them `0755` until placeholder creation, while the final sealed modes remain exactly as probed. / 下一次重发通过旧库接管后，因 systemd 首次启动在密封根中补建 4 个目录与 2 个零字节目标而关闭并回滚健康 v0.4；远端隔离探针确认精确路径和权限。首次重建又在生产变更前证明暂存目录过早设为 `0555` 会阻止非特权 worker 创建占位文件；现仅在构建期保持 `0755`，最终密封权限不变。
- Asymmetric PID acceptance / 非对称 PID 验收: the subsequent guarded retry passed all candidate gates, parser candidate construction, cutover capacity, and live adoption. It then failed closed when only the second worker's systemd `MainPID` changed; automatic rollback restored healthy public/loopback v0.4 and online PM2. Independent security review blocked an index-only fix because `host_pid_changed` cannot authenticate an arbitrary or exited target. The controller now waits for stable repeated cgroup/MainPID observations, starts the second worker against the verified first peer, and releases the waiting first worker against the verified second peer. Validation requires exact current cross-peer PIDs; mirrored RED/GREEN cases reject stale, unrelated, and borrowed-change evidence. Independent re-review after the HIGH fix: `APPROVE`. / 后续重发因单侧 `MainPID` 变化触发错误验收并自动回滚健康；独立安全复审阻断仅交换索引。控制器现使用连续稳定的 cgroup/MainPID 实时对端并要求双向精确匹配，旧 PID、无关 PID与借用变化证据均拒绝；HIGH 修复后独立复审为 `APPROVE`。
- Local post-remediation gates / 本地修复后门禁: Task 1 `182 total / 163 pass / 0 fail / 19 Linux skips`; Task 2 `135 / 119 / 0 / 16`; Task 3 `81 / 79 / 0 / 2`.
- AppSec remediation / AppSec 整改: historical focused evidence reported public guard `9/9`, deployment source contract `40/40`, lifecycle takeover `22/22`, and trusted-source plus v0.6 contracts `47/47`; that historical result does not replace the retained current run. / 历史 AppSec 聚焦结果保留，但不替代当前证据。
- Historical phase-closeout evidence / 历史阶段收口证据: the 71-file Windows non-browser aggregate remains `1,875 / 1,784 / 0 / 91`; earlier public-guard attempts keep their original classification and do not represent the latest feature-slice bytes. / 71 文件 Windows 非浏览器汇总与早期公网守护尝试保持原分类，只作为历史阶段收口证据，不代表最新功能切片字节。
- Incremental cadence / 增量节奏: exact affected tests, one independent review, verified backup, immediate guarded deployment, and production health/login/core-path smoke for every completed feature. Full regression, Playwright, and the complete reviewer matrix run at phase closeout, scheduled windows, or broad-risk changes. HIGH/CRITICAL findings, migration safety, backup/rollback readiness, and production smoke remain hard blockers. / 每个完成功能执行精确定向测试、一次独立审查、可校验备份、立即受控上线和生产健康/登录/核心路径冒烟；完整回归、Playwright 与完整审查矩阵改到阶段收口、计划窗口或高风险跨模块改动，关键风险仍是硬阻断项。
- DevOps rounds 5-10 / DevOps 第 5-10 轮: exact no-follow transaction-lock inventory, trusted transaction-locked reads, phase/RunId-bound four-family unit drain, explicit lock-only `absent` recovery, post-watchdog disarm recheck, durable parent fsync, immutable root/www-data GIDs, guard-before-PM2 accepted finalization, and a dedicated bounded 7,200-second accepted-finalization deadline close the reviewed takeover, recovery, and slow-start timeout races. / 上述机制封闭了已审查的接管、扫描顺序、解除守卫、接受态恢复及合法慢启动超时竞态。
- Candidate readiness and offline native build / 候选就绪与离线原生编译: the controller now requires the durable lifecycle phase to be exactly `candidate-ready` before parser preparation or cutover. Both trusted-runtime and candidate `better-sqlite3` rebuilds remain network-isolated and use the installed Node 20 headers through `npm_config_nodedir=/usr`. / 控制器在解析器准备或切换前要求生命周期阶段精确持久化为 `candidate-ready`；两处 `better-sqlite3` 重建继续断网，并通过 `npm_config_nodedir=/usr` 使用服务器配套 Node 20 头文件。
- Focused deployment evidence / 聚焦发布证据: deployment state-machine, trusted-source, and isolated-runtime suites report `119 total / 116 pass / 0 fail / 3 platform skips`; the independent re-review has no remaining finding. / 发布状态机、可信源码与隔离运行时套件为 `119 / 116 / 0 / 3`，独立复审无剩余发现。
- Better-sqlite hardlink convergence / Better-sqlite 硬链接收敛: node-gyp's three exact output hardlink pairs are admitted only after inode, link-count, path, and file-type verification. The trusted runtime and candidate gates remove only `obj.target`, `sqlite3.a`, and the test extension, retain `better_sqlite3.node` as a single-link runtime binary, and keep the final whole-tree hardlink rejection. Candidate pruning runs only after the build cgroup and gate processes are drained. / node-gyp 的 3 组精确输出硬链接只有在 inode、链接数、路径和文件类型全部验证后才允许收敛；两处门禁只删除上述编译中间件，保留单链接运行二进制和最终全树硬链接拒绝，候选侧必须先排空构建 cgroup 与门禁进程。
- Post-hardlink focused evidence / 硬链接修复后证据: `121 total / 118 pass / 0 fail / 3 platform skips`; PowerShell/JavaScript parsing, diff checks, and a real extracted candidate hardlink fixture pass. Independent review first blocked the pre-drain prune race; the RED/GREEN ordering fix was re-reviewed with no remaining findings and `APPROVE`. / 聚焦门禁、语法、差异和真实候选硬链接夹具通过；独立审查先阻断清理顺序竞态，修复后复审无剩余发现并 `APPROVE`。
- Cleanup-control access-time recovery / 清理控制面访问时间恢复: a subsequent pre-cutover attempt detected the prior replay journal at `topology-restored` and rejected before creating a new backup. Systemd and cleanup execution legitimately advance `atime`; replay still restores the captured timestamp initially, while final convergence remains bound to SHA-256, size, uid/gid, mode, `mtime`, links, effective fragment/drop-ins, and topology. The focused deployment-source contract is `48/48`, with independent `APPROVE`. / 后续尝试在新备份前识别出上一轮控制面恢复日志；修复仅把可变访问时间移出最终一致性条件，其余文件与 systemd 完整性条件全部保留，聚焦合同 48/48、独立复审通过。
- GNU df inode portability / GNU df inode 参数兼容: candidate tmpfs and destination inode capacity now use `df --output=itotal` and `df --output=iavail`; GNU coreutils rejects the prior redundant `-i` plus `--output` combination. Numeric validation and byte/inode bounds remain unchanged; focused regression `1/1` and independent review `APPROVE`. / 候选 tmpfs 与目标 inode 容量读取移除和 `--output` 冲突的冗余 `-i`，数值校验与容量边界不变，定向回归和独立复审通过。
- Candidate release traversal / 候选发布目录穿越: the unprivileged dependency unit now receives traverse-only `0711` on the root-owned release root and its dedicated `tmp` only while the candidate gate is active; the candidate source directory remains `0700` and unreadable. Cleanup is armed before the first permission change, independently restores verified parent/child directories to `0700`, refuses child operations beneath an invalid parent, and removes `tmp` only after exact root-owned non-symlink mode checks. RED/GREEN focused source contract plus Git Bash syntax is `2/2`; PowerShell parsing and diff checks pass. Independent review blocked three partial-cleanup paths and one invalid-parent path before final `APPROVE` with no remaining finding. / 非特权依赖 unit 只在候选门禁期间获得发布根和专用 `tmp` 的 `0711` 穿越权限，候选源码目录继续保持不可读的 `0700`；恢复在首次 chmod 前设防，父子路径分别回锁，父级异常时不操作子路径，仅在精确身份检查后删除空 `tmp`。定向 RED/GREEN、Bash 语法、PowerShell 解析和 diff 检查通过，独立复审最终批准。
- Current parent-focused evidence / 当前父级定向证据: lifecycle takeover `31/31`, deployment source contract `44/44`, selected public-guard concurrency/read/timeout tests `4/4`; PowerShell AST, Bash syntax, trusted hashes, diff check, and focused secret scan pass. Independent final review is recorded before deployment. / 生命周期接管 31/31、发布源合同 44/44、公网守护关键用例 4/4，语法、哈希、diff 与聚焦敏感信息检查通过；独立终审在部署前记录。
- Incremental release-wrapper evidence / 增量发布包装器证据: seven dynamic success/failure scenarios cover malformed responses, concurrent notes changes, applied-write timeouts, mandatory logout, rollback-health cleanup, and automatic rollback orchestration. Focused customer service/HTTP tests are `3/3`; the strict child-environment, remote Bash, local deploy validation, backup-before-mutation, GitHub dry-run, authenticated preflight, and focused secret checks pass. Independent verdict: `APPROVE`. / 7 条动态场景覆盖畸形响应、并发备注、已写入后超时、强制注销、回滚后会话清理及自动回滚编排；客户服务/HTTP 定向测试 3/3，严格环境、远端脚本、部署前备份顺序、GitHub 预推送、认证预检与敏感信息检查均通过，独立结论为 `APPROVE`。
- Current-candidate Playwright / 当前候选 Playwright: the local all-files attempt could not start browser-only tests because the required Playwright Chromium executable is absent; no browser was installed implicitly. This gate remains pending and is not recorded as passed. / 本机缺少所需 Playwright Chromium，可视化用例未启动；未擅自安装浏览器，本门禁仍不记为通过。
- Legacy adoption wiring / 旧库接管接线: the sixth guarded attempt reached migration rehearsal and proved that production is an exact legacy v0 database without `schema_migrations`. The v0.6 bundle pinned the adopter but did not invoke it, so the attempt stopped before production mutation, backup, or public cutover. RED/GREEN evidence moved the trusted adoption contract from `1/6` to `6/6`; atomic no-clobber publication and exact-output cleanup are `6/6`. / 第六次受控尝试在迁移演练中确认生产为无 `schema_migrations` 的精确 legacy v0；v0.6 包固定了接管器但未调用，因此在生产写入、备份和公网切换前停止。受信任接管合同由 RED 的 `1/6` 修复为 GREEN 的 `6/6`，原子 no-clobber 发布与精确失败清理为 `6/6`。
- Final adoption review / 接管终审: review first blocked untracked interrupted stages and a managed-v6 path that did not call the migration entrypoint. Production adoption now uses two deterministic, identity-checked root-only stages; cutover, retry, and rollback validate the complete artifact/sidecar/hardlink set before any unlink. Managed v6 runs the pinned migration entrypoint twice on a private copy and compares topology/logical/FTS digests. A pinned startup-mutation regression is rejected. Affected full files are `32/32`, the latest deployment-source suite is `25/25`, local deployment preflight and static/hash/secret checks pass, and independent final review is `APPROVE` with no remaining Critical/Important. / 终审先阻断异常中断残留与 v6 未真实执行迁移两项问题；修复后固定临时路径在完整身份验证后统一清理，v6 在私有副本执行两轮迁移并比较摘要。受影响完整测试、发布源复验、预检及静态门禁通过，最终独立复审批准。
- Offline candidate netlink compatibility / 离线候选 netlink 兼容: the seventh guarded attempt completed trusted legacy adoption and migration rehearsal on the real production snapshot, then stopped before cutover because `ip route` and `ip link` require `AF_NETLINK`, which the offline unit intentionally denies. The check now reads `/sys/class/net` and `/proc/net/route`, requires exactly `lo` and no IPv4 default route, and does not broaden the systemd sandbox or misclassify the isolated kernel IPv6 reject route. Focused RED/GREEN, the affected test file (`51 passed / 0 failed / 3 platform skips`), an equivalent remote transient-unit probe, local deployment preflight, and diff checks pass; independent review is `APPROVE` with no remaining finding, and redeployment remains pending. / 第七次受控尝试完成真实生产快照的旧库接管与迁移演练，随后因离线单元禁止 `AF_NETLINK` 而使 `ip route`/`ip link` 退出，未进入切换。新检查读取内核只读状态，要求仅有 `lo` 且无 IPv4 默认路由，不放宽沙箱，也不误判 IPv6 拒绝路由；定向测试、远端同等沙箱探针和本地预检通过，独立复审批准且无剩余发现，仍待重新部署。
- Sealed sanitized DB compatibility / 封存脱敏库兼容: the eighth guarded attempt passed adoption, migration rehearsal, and the offline-network assertion, then stopped before cutover because the compatibility probe loaded `./db` against the root-owned `0444` sanitized database and entered the write-capable migration/FTS startup path. An interim direct-readonly probe passed focused review but the ninth guarded attempt proved the sealed trusted artifact is intentionally managed v1, so expecting v6 without exercising startup migration also stopped before cutover. The final path keeps the sealed source immutable, creates a gate-owned `0700` directory and `0600` writable copy inside the isolated tmpfs, runs the real candidate `./db` v1->v6 startup migration, verifies integrity, foreign keys, and exact v6, and removes the copy on `EXIT` and success. Focused RED/GREEN, the affected test file (`51 passed / 0 failed / 3 platform skips`), local deployment preflight, and diff checks pass; independent review is `APPROVE` with no remaining blocker. / 第八次尝试因 `0444` 脱敏库误入可写启动路径停止；第九次又证明封存源按合同为 managed v1，不能直接只读断言 v6。最终实现保持封存源不变，在隔离 tmpfs 中复制可写副本并运行真实候选 v1->v6 启动迁移，校验后由成功路径和 `EXIT` trap 清理；定向验证与独立复审通过。
- Offline heredoc environment boundary / 离线 heredoc 环境边界: the tenth guarded attempt passed trusted adoption, migration rehearsal, the offline-network assertion, and the real candidate v1->v6 startup migration, then stopped before cutover because two later commands referenced parent-shell `CandidateDir` instead of the exported `CANDIDATE_DIR`. Both references now use the explicit environment variable. The contract test extracts every shell variable reference from the complete unprivileged heredoc and accepts only the exact passed or locally declared set, preventing another masked `env -i` mismatch. Focused RED/GREEN, the affected test file (`51 passed / 0 failed / 3 platform skips`), local deployment preflight, and independent review all pass; verdict `APPROVE`. / 第十次尝试已通过前述迁移与离线检查，随后因两处 `CandidateDir` 未传入而在切换前停止。现统一使用 `CANDIDATE_DIR`，并由完整变量集合回归锁定非特权 heredoc 的环境边界；定向验证和独立复审通过。
- Isolated proof PPT cache / 隔离验证 PPT 缓存: the eleventh guarded attempt passed trusted adoption, both v1->v6 migration paths, and the offline environment boundary, then stopped before cutover while the real Express one-request replay proof initialized artifact storage. The proof intentionally rebuilds a minimal child environment but omitted `PPT_CACHE_DIR`, so `server.js` fell back to the read-only candidate tree. The proof now binds PPT artifacts to its own temporary root beside uploads, runtime temp, and parser spool, then removes the complete root only after the server stops. RED/GREEN contract and real Express positive/negative replay tests are `6/6`; the candidate-inventory check, local deployment preflight, diff check, and independent review pass with verdict `APPROVE`. / 第十一次尝试在真实 Express 重放验证启动时因遗漏 `PPT_CACHE_DIR` 而尝试写入只读候选树，仍于切换前停止。修复把 PPT 缓存纳入 proof 独立临时目录并在服务停止后统一清理；正反向重放、候选清单、本地预检、差异检查与独立复审均通过。
- Replay-only parser readiness / 仅重放验证的解析器 readiness: the twelfth guarded attempt passed all earlier migration, offline-environment, and PPT-cache gates, then stopped before cutover because Linux production parser readiness requires a `root:root 0700` spool while the candidate proof intentionally runs as the unprivileged gate user. The sealed upload sandbox service and production readiness remain byte-for-byte unchanged. Only the exact one-request proof, requiring strict local-worker constraints, a dedicated marker, user ID `1`, and Node IPC, uses an isolated readiness snapshot; ordinary test adapters and production still execute the original readiness gate. Positive/negative proof tests are `7/7`; production readiness, legacy self-test, and readiness-failure regressions are `3/3`; no-IPC rejection, local deployment preflight, and the pinned service hash pass. Independent review verdict: `APPROVE`. / 第十二次尝试因无特权 proof 无法满足生产 spool 所有权合同而在切换前停止。解析器密封文件和生产 readiness 完全不变；只有同时满足严格 local-worker、专用 marker、用户 ID 与 IPC 的一次请求 proof 使用隔离快照，普通测试和生产继续执行原门禁。定向验证与独立复审通过。
- Unprivileged test boundary / 非特权测试边界: the thirteenth guarded attempt passed production-snapshot sanitization and migration, v1->v6 compatibility, the real one-request replay, and public replay guards, then stopped before cutover because the Linux root control-plane cases in `sanitized_migration_gate.test.js` were executed inside the unprivileged offline candidate unit. The candidate was removed and the control plane restored. The deployment now excludes only that root-oriented suite from the unprivileged wildcard run while retaining the preceding trusted `sanitize-and-verify` rehearsal against the real production backup. The suite itself and production migration logic are unchanged. RED/GREEN deployment contract, local preflight, and diff validation pass; independent review verdict is `APPROVE` with no blockers. The guarded retry remains pending. / 第十三次尝试因 root 控制面测试误入非特权离线单元而在切换前停止。修复只调整远端测试分层，保留真实生产备份的受信任清洗迁移演练，不修改测试套件和生产迁移逻辑；RED/GREEN 合同、本地预检和 diff 校验通过，独立复审为 `APPROVE`、无阻断项，等待重发。
- Bounded release proofs / 有界发布证明: the fourteenth guarded attempt passed the real production-backup sanitization and migration, schema v6 compatibility, one-request replay `8/8`, and public replay guards `21/21`, then showed that the remaining `tests/*.test.js` wildcard still mixed Linux-root fault injection, Windows historical-baseline tests, development-directory fixtures, and server-start tests that require their own PPT cache. The doomed candidate was deliberately stopped before cutover; cleanup and control-plane replay completed. Per the approved lightweight cadence, every production candidate now runs only source/build identity checks, the trusted real migration, schema compatibility, one-request proof, replay guards, two browser core-route smokes, and Nginx validation. Full developer regression remains a phase-closeout or risk-window activity. RED/GREEN contracts, the affected suite (`52 passed / 0 failed / 3 platform skips`), local preflight, and diff validation pass; independent review verdict is `APPROVE` with no blockers. The guarded retry remains pending. / 第十四次尝试证明全量通配测试仍混入 root、Windows 基线和开发机专用测试，并在切换前主动终止。生产候选现固定为真实迁移、schema、一次请求、重放守护、浏览器核心路由和 Nginx 等有界发布证明；全量开发回归留到阶段收口或风险窗口。定向验证与独立复审通过，等待重发。
- Deployment-smoke writable boundary / 部署冒烟可写边界: the fifteenth guarded attempt passed trusted production-backup migration, schema v6 compatibility, one-request replay `8/8`, and public replay guards `21/21`, then stopped before cutover because the pinned browser smoke still wrote its fixture database, spool, logs, and reporter state beneath the read-only candidate release root `.superpowers`. Cleanup and control-plane replay completed without production mutation. `TM_DEPLOYMENT_SMOKE_ROOT` now confines Playwright output plus the child fixture database, spool, PPT cache, temporary files, and logs to `$TEST_ROOT/browser-smoke`, while the candidate source remains read-only. The regression failed before the fix and now passes; the affected suite is `53 passed / 0 failed / 3 platform skips`, and a real spawned fixture health probe, path projection, local deployment preflight, and diff checks pass. Independent review first blocked missing child `TMP_DIR`/`PPT_CACHE_DIR` bindings; after correction the re-review verdict is `APPROVE` with no remaining finding. / 第十五次尝试在浏览器冒烟阶段发现夹具与报告仍写入只读候选发布根，因此在切换前停止；清理和控制面恢复完成，生产未变更。现将 Playwright 输出和子进程的夹具数据库、spool、PPT 缓存、临时文件及日志全部约束到 `$TEST_ROOT/browser-smoke`，候选源码继续只读；RED/GREEN、受影响测试、真实夹具健康探针、路径投影、本地预检和 diff 检查通过。独立审查先阻断子进程路径遗漏，整改后复审为 `APPROVE`、无剩余发现。
- Read-only deployment browser server / 只读部署浏览器服务: the sixteenth guarded attempt passed the same trusted migration, schema, one-request `8/8`, and replay-guard `21/21` proofs, then showed that the general browser fixture starts the complete backend and therefore reaches the Linux production parser spool ownership contract. A non-root candidate must not satisfy that `root:root` readiness contract. The attempt stopped before cutover and cleanup converged. Deployment browser smoke now uses a dedicated read-only static server that reuses the production `public_assets_service` allowlist and exposes only health, SPA shell, and static-boundary behavior; it starts no database, PPT store, upload parser, or business write path, and production parser readiness remains unchanged. RED/GREEN contract, a real HTTP shell/asset/private-path probe, the affected suite (`53 passed / 0 failed / 3 platform skips`), focused deployment-source checks `2/2`, local preflight, and diff checks pass. Independent review verdict is `APPROVE` with no remaining finding. / 第十六次尝试证明通用浏览器夹具会启动完整后端并触及 Linux 生产解析器 spool 所有权合同；非 root 候选不应满足该条件，因此仍在切换前停止并完成清理。部署冒烟现使用复用生产公开资产白名单的专用只读静态服务，只验证健康、SPA 壳层和静态边界，不启动任何业务写路径，也不修改生产解析器 readiness；RED/GREEN、真实 HTTP 探针、受影响测试、部署源定向检查、本地预检与 diff 校验通过，独立复审为 `APPROVE`、无剩余发现。
- Production / 生产: verified v0.4 remains untouched. Sixteen v0.6 attempts were rejected before cutover; the latest was the general browser fixture crossing into the production parser readiness contract inside the restricted candidate. Every failed candidate was cleaned and the latest control-plane replay converged without production mutation. / 已验证的 v0.4 保持原样；十六次尝试均在切换前拒绝，最近一次为通用浏览器夹具在受限候选中进入生产解析器 readiness 合同。失败候选和控制面均已收敛，生产未变更。

## Production Gate / 生产门禁

Production remains on verified v0.4. The current v0.6 feature slice remains inside the release gate until its exact focused tests and independent review pass, the authoritative checkout and GitHub commit are synchronized, a verified backup and capacity record exist, guarded deployment succeeds, and remote parser/runtime/API/access/rollback plus health/login/core-path smoke evidence is bound to the release record. Full regression, Playwright, and the complete reviewer matrix are additionally required at phase closeout or when the risk trigger applies. / 生产仍运行已验证的 v0.4；当前 v0.6 功能切片须完成精确定向测试与独立审查、权威工作区和 GitHub 提交同步、可校验备份与容量记录、受控部署，以及远端解析器/运行时/API/权限/回滚和健康/登录/核心路径冒烟证据后才能上线。完整回归、Playwright 与完整审查矩阵在阶段收口或风险触发时追加执行。
