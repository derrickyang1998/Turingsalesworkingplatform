# v0.8.28 Publication Lifecycle Custody Production Record / 发布生命周期保管生产记录

## Release / 发布

- Date / 日期: `2026-09-11`
- Scope / 范围: audited publication correction, tracking pause/resume, and immutable history inside the existing M4 collaboration workspace / 既有 M4 合作工作区内带审计的发布纠错、追踪暂停/恢复和不可变历史
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Implementation / 实现: `536dbf7`
- Release-gate correction / 发布门禁修正: `db59c69`
- Independent review / 独立审查: `APPROVE`
- Production run / 生产运行: `1b1779afbe07473c99052ef6ad41b6e0`
- Candidate / 候选: `v060-crm-sales-workspace-20260911-030235`
- Database / 数据库: migrations `1–18`, schema `v18`

## Delivered / 已交付

- Added correction, pause, resume, and lazily paginated history controls to the existing M4 collaboration table without adding a page or replacing the accepted product shell. / 在既有 M4 合作表内增加纠错、暂停、恢复和按需分页历史控件；不新增页面，也不替换已验收产品壳层。
- Added the append-only `collaboration_publication_lifecycle_versions` ledger. Every event is strictly chained to its prior version, immutable after insert, bound to organization/campaign/collaboration/custody, and accompanied by retrieval-ineligible knowledge evidence. / 新增只追加生命周期账本；每个事件与前一版本严格成链、写入后不可修改，并绑定组织、活动、合作和保管记录，同时生成不可检索知识证据。
- Same-content URL alias corrections retain the same publication and metrics while exposing the effective URL to performance search, export, and Feishu projections. Genuine content replacements bind or create a different `campaign_publications` row, and A→B→A retains a complete ordered record without cross-custody reuse. / 同内容链接别名纠错沿用发布记录与指标，并在效果搜索、导出和飞书投影中返回生效链接；真实内容替换绑定或创建另一活动发布记录，A→B→A 保留完整顺序且禁止跨保管复用。
- Paused content remains visible but leaves freshness/action queues and cannot receive new metric observations at either service or database level. Resume restores collection eligibility. / 暂停内容保持可见，但退出新鲜度与行动队列，服务层及数据库层均阻止新增指标；恢复后重新取得采集资格。
- Collaboration list projections remain compact and bounded. Full history is fetched only from the permission-checked paginated endpoint. / 合作列表投影保持紧凑有界，完整历史仅通过权限校验后的分页接口按需读取。

## Focused Verification And Review / 聚焦验证与审查

- Collaboration and performance service tests passed `46/46`; M4, freshness, request-policy, and migration tests passed `54/54`. Trusted-source corrections passed `4/4`, migration exactness `15/15`, release contracts `38/38`, sanitizer v18 profile `1/1`, and full v1→v18 migration rehearsal `1/1`. JavaScript syntax and diff checks passed. / 合作与效果服务 `46/46`、M4/新鲜度/请求策略/迁移 `54/54`、可信来源修正 `4/4`、迁移精确性 `15/15`、发布合同 `38/38`、sanitizer v18 `1/1`、完整 v1→v18 演练 `1/1` 均通过；语法与差异检查通过。
- Independent review found three release blockers: effective alias URLs were absent from downstream performance projections, four deployment-source tests still expected v17, and five migration-exactness tests still expected v16. All were fixed and the final verdict was `APPROVE`. / 独立审查发现三项上线阻断：链接别名未进入下游效果投影、4 项发布来源测试仍预期 v17、5 项迁移精确性测试仍预期 v16；全部修复后最终结论为 `APPROVE`。
- The first candidate safely stopped before production mutation because one real-request replay assertion still expected migrations 1–17. The assertion was corrected, its focused local test passed `8/8`, and the accepted candidate passed real replay `8/8`, release guard `21/21`, built-in browser smoke `2/2`, migration rehearsal, parser runtime reuse, Nginx checks, and final acceptance. / 首个候选因真实请求重放仍预期迁移 1–17 而在生产变更前安全停止；修正后本地定向检查 `8/8`，正式候选通过真实重放 `8/8`、发布守卫 `21/21`、内置浏览器冒烟 `2/2`、迁移演练、解析器缓存复用、Nginx 与最终验收。

## Production Evidence / 生产证据

- Guarded candidate SHA-256: `9dd564ef1f781618fb6a8ea7c5e0b11dc30a4fa9fcee77c19fd8b090caa38b7c`. Recoverable backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260911-030235`. / 受保护候选与可恢复备份均已保留。
- The initial backup manifest verified `309/309` entries with SHA-256 `93f4a329128c2c948cd65cdc58d9e72cb9c1ca683c5c0b5a6609d8d044a303c1`; the cutover snapshot verified `35/35` entries with SHA-256 `49defb5b38db088a5112dfb64fd252e849148d8df0f29488f5c0960c03dc9a35`. / 初始备份清单与切换快照均逐项复验通过。
- Public health returned healthy with parser readiness true. PM2 is online with restart count `0`; SQLite migrations are exactly `1–18`, `quick_check=ok`, and the new ledger plus five enforcement triggers are present. / 公网健康、解析器、PM2 和数据库均通过；迁移链精确为 `1–18`，新账本与 5 个强制触发器存在。
- Administrator login, identity, campaign read, M4 collaboration projection, and logout passed. The authenticated history route returned the expected `404 RECORD_NOT_FOUND` because production currently has no publication-custody rows; no business data was created for smoke testing. Anonymous access remains rejected. / 管理员登录、身份、活动读取、M4 投影和注销通过；因生产当前没有发布保管记录，登录态历史接口按预期返回 `404 RECORD_NOT_FOUND`，验收未创建业务数据；匿名访问继续拒绝。
- Public and local `app.js` SHA-256 matched at `9c15febe905fd9229aa80e0538330a7b51dfc0a0300d913806d72883033b5973`. The accepted source SHA-256 is `8031fbdcd8189a022422e45e05cfcfbf7e3455018be62c2fe3d97de4c9892e51`; frozen `ppt.js` remains unchanged. / 线上与本地脚本哈希一致，可信发布源身份已留存，冻结方案 PPT 未变。

## Delivery Cadence / 交付节奏

- Normal features now ship in the same development round after affected tests, necessary syntax/contracts, one independent review, a verified backup, and online feature smoke. Full suites are reserved for phase closeout or migration, authentication/authorization, shared infrastructure, external writes, and similarly broad-risk changes. / 普通功能完成受影响测试、必要语法/合同、一次独立审查和可验证备份后在当轮上线，并做线上功能冒烟；完整测试仅用于阶段收口或迁移、鉴权、共享基础设施、外部写入等高风险改动。
- Migration safety, rollback readiness, HIGH/CRITICAL findings, and production smoke remain hard blockers. / 迁移安全、回滚就绪、HIGH/CRITICAL 发现及生产冒烟继续作为硬阻断。

## Next Boundary / 下一边界

- Phase 7B still needs one explicitly approved social-data provider, scheduled collection, and auditable run history. Live Feishu writes remain a separate externally configured slice. / Phase 7B 仍需一个明确批准的社交数据 provider、定时采集与可审计运行历史；真实飞书写入继续作为独立外部配置切片。
