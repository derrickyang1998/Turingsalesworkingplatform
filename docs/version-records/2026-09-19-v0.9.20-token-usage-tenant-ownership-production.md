# v0.9.20 Token Usage Tenant Ownership Production Record / Token 用量租户归属生产记录

Date / 日期：2026-09-19
Status / 状态：Production deployed and independently reviewed / 已部署生产并通过独立审查

## Release Identity / 版本身份

- Version / 版本：`v0.9.20-token-usage-tenant-ownership`
- Branch / 分支：`codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
- Production source HEAD / 生产源码提交：`0504141c1df025cfaa6d08661d829a5f9face87f`
- Feature implementation / 功能实现：`b4d6ba076f5010689ed70573e4c6a662c109ed0e`
- Ledger replacement guard / 账本替换保护：`875f3d1`
- Pre-gate retention / 门禁前保留清理：`4cfe373`
- Recoverable verified retention / 可恢复可验证保留控制：`0504141c1df025cfaa6d08661d829a5f9face87f`
- Production run / 生产运行：`a3602d90516a4ed8b6befd2c9d3fee8d`
- Verified backup / 已校验备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260919-063108`
- Accepted evidence / 接纳证据：`/root/turingmarket/deployment-evidence/accepted-a3602d90516a4ed8b6befd2c9d3fee8d.json`

## Delivered / 已交付

- Added `token_usage.org_id` in schema v26 and deterministically backfilled every historical row from authoritative performance-audit ownership. A legacy fallback is allowed only when the database contains exactly one organization; unresolved multi-organization ownership aborts before DDL. / schema v26 为 `token_usage` 增加组织归属，并按权威绩效审计归属确定性回填全部历史记录；只有恰好单组织的旧库可兼容回填，多组织未解析会在 DDL 前中止。
- Every trusted server-side AI usage write now binds the active organization and user. Quota totals use organization+user scope, and ordinary reads return only the current user's rows in the active organization. / 所有可信服务端 AI 用量写入同时绑定活动组织和用户；配额按组织+用户计算，普通读取只返回当前活动组织内当前用户记录。
- Platform administrators can request cross-organization aggregates only with `admin_audit=global`; every such read writes a bounded `admin_list_token_usage` audit event. / 平台管理员只有通过 `admin_audit=global` 才能读取跨组织聚合，每次读取均写入字段有界的审计事件。
- Disabled browser/client token reporting. `POST /api/token-usage` returns `410 TOKEN_USAGE_CLIENT_REPORTING_DISABLED`; only trusted provider paths can record usage. / 已关闭浏览器/客户端 Token 上报，接口固定返回 410，只有可信服务端 provider 路径可记录用量。
- Added no-replace, no-update, and no-delete triggers. `INSERT OR REPLACE` cannot bypass ledger immutability even when recursive triggers are disabled. / 新增禁止替换、更新和删除的触发器，即使关闭递归触发器，`INSERT OR REPLACE` 也不能绕过账本不可变性。
- The Admin usage view now uses the explicit global-audit query and shows organization identity. The latest UI shell, CRM, M3/M4, AI/knowledge, proposal/report, and frozen PPT remain intact. / 管理控制室用量视图改用显式全局审计查询并显示组织；最新界面、CRM、M3/M4、AI/知识、方案/报告及冻结 PPT 保持不变。

## Migration And Ledger Safety / 迁移与账本安全

- Migration checksum / 迁移摘要：`1c7e70f5c2a2b432215407ceb1f6178943b0e847f308b65b4157659c47897bfb`。
- Production contains `126` token-usage rows; all `126` have an organization owner and null ownership is `0`. / 生产共有 126 条 Token 用量记录，全部具备组织归属，空归属为 0。
- SQLite `quick_check=ok`, foreign-key violations are `0`, and `token_usage_no_replace_insert`, `token_usage_no_update`, plus `token_usage_no_delete` are present. / SQLite 完整性与外键正常，三个不可篡改触发器齐全。
- Historical revoked memberships are accepted for deterministic migration evidence, while all new writes require a current active membership. / 历史已撤销成员可作为确定性迁移证据保留，新写入仍要求当前有效成员关系。

## Focused Verification And Independent Review / 定向验证与独立审查

- Token service and route core checks passed `22/22`; exact v25-to-v26 replay `1/1`; Campaign migration gate `3/3`; trusted manifest `1/1`; v1-to-v26 migration `1/1`; local deployment preflight passed. / Token 核心、精确重放、Campaign 迁移门禁、可信清单、v1→v26 迁移及本地发布预检全部通过。
- Release-retention affected checks passed `4/4` for source contracts and `4/4` for lifecycle cases; the full lifecycle file passed `40/40`. / 发布保留控制的源码合同与生命周期受影响用例均通过，完整生命周期文件 `40/40`。
- The guarded candidate passed production-copy migration/dependency gates, remote backend/CRM checks `76/76`, exact public Nginx behavior, read-only recheck, and Chromium deployment smoke `2/2`. / 受控候选通过生产副本迁移/依赖门禁、远端 `76/76`、公网 Nginx 行为、只读复核和 Chromium `2/2`。
- Independent review first returned `NO-GO` with two P1 findings: interrupted pre-release retention could strand the global quarantine, and incomplete backups could count toward the retention floor/cap. / 独立审查首轮因两个 P1 返回 NO-GO：预发布保留清理中断可能遗留全局隔离目录，未完成备份可能占用保留上下限。
- The controller now publishes `backup-ready.json` only after required files and the full manifest validate, counts only verified backups, enforces a hard cap of 20 with a rollback floor of 10, protects current/last-good/live-journal generations, and resumes the same retention journal after interruption. Re-review returned `GO` with no open P0/P1. / 控制器现仅在必需文件和完整清单通过后发布 `backup-ready.json`，只统计已验证备份，执行 20 份硬上限和 10 份回滚下限，保护当前/last-good/活动日志代际，并可在中断后恢复同一保留日志；复审结论 GO，无开放 P0/P1。
- This is a schema, tenant-authority, and shared release-infrastructure boundary, so the necessary extended gates were retained. Unrelated heavy suites were not added. / 本版涉及 schema、租户权威和共享发布基础设施，因此保留必要扩展门禁，未追加无关重型套件。

## Deployment Record / 部署记录

- The initial candidate gate exposed insufficient free space after verified historical backups accumulated. Retention was moved before the candidate gates; free space increased from approximately `3.2 GB` to `11.3 GB` before the accepted run. / 初始候选门禁暴露历史已验证备份累积后的空间不足；保留清理前移后，正式运行前可用空间由约 3.2 GB 提升至 11.3 GB。
- Accepted run `a3602d90516a4ed8b6befd2c9d3fee8d` returned `DEPLOY_OK` and `RETENTION_CLEANUP_OK`. / 正式运行成功返回部署与保留清理完成标记。
- Trusted source SHA-256 / 可信源码摘要：`286b80ac12c84a8370de483e8d7fcbc64482b8a3a13d5b9805a36ca5feb82c8b`。
- Candidate tree SHA-256 / 候选树摘要：`c1a05f282562165876b416771cc291810d248b17e6ee5fd4c7e0d83f8f16b936`。
- Acceptance facts SHA-256 / 验收事实摘要：`92c1e06a5286750df07512b54ff407b650e0a44bb0da71befaf42897717b6782`。
- Replay evidence SHA-256 / 重放证据摘要：`7f110cdc899259aef9c8eca4a881e1a42e66bd65e110aeb00448ab930297cd18`。
- Parser evidence SHA-256 / 解析器证据摘要：`de0faa82a8dc3ef81b50ea0fb46bd5d7c40176aee055a35bbb173b1ba6e8ab9d`；parser runtime / 解析器运行时：`7c280e80546627a82f29010a160ad1aa720c2ca03b447b0328d610b90b82023c`。

## Backup And Production Acceptance / 备份与生产验收

- `backup-ready.json` uses `tm-deployment-backup-ready-v1` and binds backup name, database SHA-256, and manifest SHA-256. / 备份就绪标记绑定备份名、数据库摘要和清单摘要。
- Initial manifest has `347` entries and SHA-256 `72e7461cd66bea1df89457f0dea0a0917ffb3a9ec0960905255f5e1b06dc9b55`; cutover manifest has `35` entries and SHA-256 `ce4796ebce8e8e35ebc52c9ecd4e97d42d349a7394dabeaa57a546522d4157fd`. The full backup passed `sha256sum -c`. / 初始清单 347 项、切换清单 35 项，完整备份校验通过。
- Initial database SHA-256 is `b382f135eec7dc4c5c9ea84f4b2ba8a605144627e1803f40520b0e4d3200f432`; cutover database SHA-256 is `16c32eb43187ba213a8c0f5708f65d28861a07b9eee833b4cf73af0acb6dcfda`. / 初始与切换数据库摘要均已记录。
- Public `/api/health` returned `200` with parser ready; PM2 is `online` with restart count `0`; schema is v26; SQLite and foreign keys pass. / 公网健康与解析器正常，PM2 在线且无重启，schema v26、SQLite 和外键均通过。
- Authenticated production smoke verified the `derrick` administrator login, explicit global token audit, `410` client-write rejection, and logout without exposing credentials or bearer tokens. Two audit events were recorded and active sessions returned to `0`. / 线上鉴权冒烟验证管理员登录、显式全局 Token 审计、客户端写入 410 及退出；未输出凭据或令牌，记录两个审计事件，活动会话恢复为 0。
- Frozen `ppt.js` remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 冻结 PPT 摘要未变化。

## Known Nonblocking Baseline Items / 已知非阻断基线项

- The broad source-contract file still has an unrelated historical assertion requiring an exact `.gitattributes` EOL rule for migration 025. A separate legacy release test still expects manifest count `66` while the current trusted inventory contains `127`. Neither was widened into this release. / 广域源码合同仍有一条与本版无关的 migration 025 EOL 旧断言，另一旧发布测试仍期望清单数量 66 而当前可信清单为 127；本版未扩大范围处理。

## Delivery Cadence / 交付节奏

Continue one independently useful feature per release. Ordinary features receive only affected tests, necessary safety checks, one independent review, a verified backup, immediate production deployment, and focused online acceptance. Heavy suites remain phase-closeout or risk-triggered. / 后续继续“一功能一上线”；普通功能只跑受影响测试、必要安全检查和一次独立审查，通过可验证备份后立即上线并做定向验收，繁重套件保留到阶段收口或风险触发。

## Next Boundary / 下一边界

Select the next bounded Phase 8 capability as a separate release. Plan entitlements and quota enforcement are the leading governance follow-up; electronic signature and real external delivery remain isolated scopes. / 下一版本继续选择一个有界的 Phase 8 能力独立上线；套餐权益与配额执行是优先治理后续，电子签和真实外部投递继续分离开发。
