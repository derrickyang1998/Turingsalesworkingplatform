# v0.7.0 Task 4A M5 AI Chat Client / M5 AI 对话可靠性

Date / 日期: 2026-08-26
Status / 状态: Production incremental slice / 生产增量切片已发布
Branch / 分支: `codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
Implementation commit / 实现提交: `b96cf6d`

## Delivery / 交付

- M5 submits the exact user-authored question to `/api/ai/chat`; local memory and brand-count text are no longer injected into persisted prompts. / M5 只提交用户实际输入的问题，不再把本地记忆或品牌数量写入持久化提示。
- The existing web-search checkbox now controls `allow_web`. Linked requests also carry the active Campaign and selected knowledge-entry IDs. / 现有联网开关真正控制 `allow_web`，关联请求继续携带当前 Campaign 与已选知识 ID。
- A linked ambiguous transport retry reuses one stable idempotency key while every attempt receives a fresh request ID. Explicit HTTP failure and successful completion rotate the operation key. / 关联请求在传输结果不明确时复用稳定幂等键，每次尝试使用新请求 ID；明确 HTTP 失败或成功后轮换新键。
- One in-flight request prevents simultaneous first messages from splitting into separate conversations. Campaign, authentication-generation, and conversation fences suppress stale responses. / 单飞阻止并发首条消息拆分会话；Campaign、登录代次和会话隔离阻止旧响应污染当前上下文。
- Campaign changes, login, auth expiry, logout, and clear-chat invalidate pending work and retry state. Backend errors render as text. / Campaign 切换、登录态变化、注销和清空对话会清除旧请求与重试状态；服务端错误按纯文本渲染。
- No HTML layout, backend service, database schema, or frozen PPT renderer changed. / 未修改 HTML 布局、后端服务、数据库结构或冻结 PPT renderer。

## Review And Focused Verification / 审查与定向验证

- Final affected matrix: 140 passed, 0 failed. It covers the M5 client contract, AI/RAG persistence and permissions, administrator conversation audit, Campaign AI, M3 demand/proposal/PPT paths, Phase 4 integration, CRM UI compatibility, and frontend architecture inventory. / 最终受影响矩阵 140/140 通过，覆盖 M5、AI/RAG、管理员对话审计、Campaign AI、M3 需求/方案/PPT、阶段 4 集成、CRM UI 兼容和前端架构清单。
- Independent code review returned `APPROVE` with no Critical, Important, or Minor findings. / 独立代码审查为 `APPROVE`，无 Critical、Important 或 Minor 发现。
- `node --check`, `git diff --check`, focused secret scanning, and frozen-PPT SHA verification passed. Browser automation was not run under the approved per-feature cadence. / JavaScript 语法、差异、聚焦敏感信息扫描与冻结 PPT 哈希通过；按已批准的单功能节奏未运行浏览器自动化。

## Scope / 范围

- Runtime file / 运行文件: `platform/app.js`.
- Contract files / 合同文件: `platform/server/tests/latest_ui_ai_campaign_client.test.js` and the active-definition inventory fixture / 及活动定义清单。
- Database migration / 数据库迁移: none / 无。
- Production App build marker / 生产 App build 标识: unchanged `20260811-v060-crm-sales-workspace`.
- Frozen PPT build / 冻结 PPT build: unchanged `20260702-v916-kb-bridge-client-cn`.
- Frozen PPT SHA-256 / 冻结 PPT 哈希: `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`.

## Production Deployment / 生产部署

- Verified release backup / 已验证发布备份: `/root/turingmarket/backups/v070-slice4a-m5-ai-client-20260826-061018`.
- Backup aggregate SHA-256 / 备份聚合哈希: `1fececb8069a73e42ceccef184a99962689853c0104707b1ed3127229fcdbd75`.
- Pre-release app SHA-256 / 发布前 App 哈希: `9a985f05759ba8f8acb28397601b5a31ccdb21d363f80f0463666c66a360163a`.
- Production app SHA-256 / 生产 App 哈希: `44e67c0a034d68629a450cd1691e0005183b4689df6564ddf9ccaa80d2ad24a6`.
- The verified backup contains the prior runtime files, a SQLite Backup API snapshot, PPT-cache snapshot, metadata, and checksum manifest. Cutover used staged syntax/hash checks, atomic replacement, a 180-second parser-aware health window, and automatic app-file rollback. / 备份包含旧运行文件、SQLite Backup API 快照、PPT 缓存、元数据与校验清单；切换采用暂存语法/哈希检查、原子替换、180 秒解析器健康窗口和自动 App 文件回滚。
- PM2 PID after release / 发布后 PM2 PID: `1400848`; Nginx active / 正常。
- No schema migration ran. Production data changes are limited to audited acceptance records and the administrator credential recovery below. / 未运行结构迁移；生产数据变化仅包含审计验收记录和下述管理员凭据恢复。

## Administrator Recovery / 管理员恢复

- Production login exposed that the sole active administrator's historical password hash had not received the earlier requested reset. / 生产登录验收发现唯一启用管理员的历史密码哈希未落实此前要求的重置。
- A separate consistent SQLite backup was created before recovery: `/root/turingmarket/backups/v070-slice4a-admin-reset-20260826-062343`; aggregate SHA-256 `4f651b0037aea4acd38cfccf420db8c2ac97c07f47c6f5fbc035377e49b9fb6c`. / 恢复前已创建独立一致性数据库备份及聚合哈希。
- The requested active administrator account was preserved, its password hash was reset through protected process input, stale sessions were revoked, and a redacted security audit was written. No account identifier or plaintext credential is stored in this record. / 保留用户指定的启用管理员账号，通过受保护进程输入重置密码哈希、撤销旧会话并写入脱敏安全审计；本记录不保存账号标识或明文凭据。
- Post-recovery bcrypt verification, login, `/api/auth/me`, logout, SQLite `quick_check`, and foreign-key verification passed. / 恢复后密码哈希、登录、身份读取、注销与数据库完整性均通过。

## Online Acceptance / 线上验收

- Public `/api/health` returned 200 with parser `ready=true` and manifest `44db310046efe65bd68c110313b4887995c73e276e7d58f65fe037c09a973c5b`. Public and local-Nginx `app.js` bytes match the production SHA-256. / 公网健康、解析器与静态字节哈希通过。
- Acceptance records / 验收记录: Campaign `1`, conversation `31`, assistant message `62`, archived knowledge summary `125`.
- The linked answer persisted eight authorized knowledge references and zero web results under `allow_web=false`; the archive, two Campaign links, one `link_attached` event, and one completed HTTP-200 idempotency ledger were verified directly in SQLite. / 关联回复持久化 8 条授权知识引用、0 条联网结果，并验证摘要归档、两条 Campaign 关联、单条关联事件及完成态幂等账本。
- Same-key replay was semantically identical, conversation detail was readable to the administrator, and logout removed the acceptance token. Two loopback sessions left by intentionally failed smoke assertions were removed with a redacted audit; recent loopback smoke sessions are zero. / 同键回放语义完全一致，管理员可读会话详情，注销移除验收令牌；两条由故意失败断言遗留的回环会话经脱敏审计清理，近期验收会话为 0。
- SQLite `quick_check=ok`, foreign-key violations `0`, PM2 online, and Nginx active. / SQLite、PM2 与 Nginx 均正常。

## Delivery Cadence / 发布节奏

This slice follows the approved accelerated policy: affected tests, one independent review, verified backup, immediate production deployment, and online health/login/feature smoke. Full regression remains reserved for Phase 6 closeout or broad-risk changes. / 本切片遵循已批准的加速策略：受影响测试、一次独立审查、可验证备份、立即上线和线上健康/登录/功能冒烟；完整回归保留到阶段 6 收口或高风险变更。

## Next Slice / 下一切片

Phase 6 Task 4B will complete knowledge-growth governance and audit UX: controlled Tavily augmentation, durable source display, promotion rules for high-value summaries, administrator-wide filters, and ordinary-user isolation without replacing the approved product shell. / 阶段 6 切片 4B 将收口知识成长治理与审计体验：受控 Tavily 联网、持久来源展示、高价值摘要晋升规则、管理员全量筛选和普通用户隔离，同时不替换已批准产品壳层。
