# v0.8.20 Payment Settlement Checkpoint Production Record / 收付款凭证与四眼结算生产记录

## Release / 发布

- Date / 日期: `2026-09-09`
- Scope / 范围: Phase 7 M4 manual receipts, creator payments, immutable evidence, and independent settlement / 阶段 7 M4 人工回款、达人付款、不可变凭证与独立结算
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Plan / 计划: `d221c89`
- Implementation / 实现: `80a2bd9`
- Review fix / 审查修复: `7dcfc8b`
- Database / 数据库: migrations `16/16`, schema `v16` / 迁移 `16/16`，schema `v16`
- Production deployment run / 生产发布运行: `bfc4e44c466241a2988bb95bea01e80e`

## Delivered / 已交付

- The existing M4 collaboration workspace now records client receipts and creator payments in multiple tranches, including amount, timestamp, method, reference, counterparty, tranche, note, void reason, and complete history. No page or approved shell was replaced. / 既有 M4 合作工作台现可分笔记录客户回款和达人付款，包括金额、时间、方式、参考号、对方、款项批次、备注、冲销原因及完整历史；未替换任何页面或已批准壳层。
- Version 2 orders can record evidence only after the signed-contract checkpoint. Settlement submission additionally requires approved publication and at least one creator payment. Amount variance and zero-value cases require explicit reasons. / v2 订单只有通过已签合同关口后才能记录凭证；提交结算还要求内容审核和发布已通过，并至少存在一笔达人付款；金额差异及零金额场景必须填写原因。
- The submitter and every recorder represented in the active payment digest are barred from reviewing the same settlement. Only the Campaign owner or organization administrator can independently approve or request changes. / 提交人及有效付款摘要中的每一位记录人都不得审核同一结算；只有 Campaign 负责人或组织管理员可独立通过或退回。
- Approval atomically projects the active creator-payment total to `cost_actual`, confirms it, attaches the existing `settlement` relation, and retains the complete append-only evidence chain. / 通过审核时，系统以原子方式把有效达人付款总额投影到 `cost_actual`、完成确认、建立既有 `settlement` 关系，并保留完整只追加凭证链。
- Payment, void, submission, and decision evidence uses the reserved `collaboration_payment_settlement` namespace, is Campaign-linked, and is excluded from RAG. Generic knowledge JSON, ingest, and upload paths cannot fabricate it. / 收付款、冲销、提交和决定凭证使用保留的 `collaboration_payment_settlement` 命名空间，关联 Campaign 且排除在 RAG 之外；通用知识 JSON、ingest 和上传入口均无法伪造。
- The legacy collaboration update endpoint rejects every v2 `cost_actual` mutation and settlement attempt with `SETTLEMENT_CHECKPOINT_REQUIRED`. Historical v1 and unlinked records retain their prior behavior. / 旧合作更新接口会以 `SETTLEMENT_CHECKPOINT_REQUIRED` 拒绝所有 v2 实际成本改写和结算绕过；历史 v1 及未关联记录保持原有行为。

## Focused Verification And Independent Review / 聚焦验证与独立审查

- The initial affected matrix passed `148/148`, covering payment/settlement service behavior, collaboration security, signed contracts, content review, routes, request policies, reserved knowledge paths, influencer workflow, and M4 client behavior. / 首轮受影响矩阵 `148/148` 通过，覆盖收付款/结算服务、合作安全、签约、内容审核、路由、请求策略、保留知识入口、网红工作流及 M4 客户端行为。
- Independent code/security review found one P1 bypass: a v2 caller could still mutate `cost_actual` without creating the settlement relation. The guard and a red-to-green regression test were added; the focused payment/security/M4 matrix passed `55/55`, and re-review returned `APPROVE` with no open blocking finding. / 独立代码/安全审查发现一项 P1 绕过：v2 调用方仍可在不建立结算关系时改写 `cost_actual`。补充服务端门禁及红转绿回归用例后，付款/安全/M4 聚焦矩阵 `55/55` 通过，复审为 `APPROVE`，无未关闭阻断项。
- JavaScript syntax, diff formatting, focused secret scan, frozen-PPT hash, and final local deployment preflight passed. / JavaScript 语法、差异格式、定向密钥扫描、冻结 PPT 哈希及最终本地发布预检均通过。
- The production candidate passed real Express replay `8/8`, release gate `21/21`, built-in browser smoke `2/2`, migration rehearsal, parser identity/readiness, capacity, and Nginx checks. / 生产候选通过真实 Express 回放 `8/8`、发布守卫 `21/21`、内置浏览器冒烟 `2/2`、迁移演练、解析器身份/就绪、容量和 Nginx 检查。

## Production Evidence / 生产证据

- Recoverable backup / 可恢复备份: `/root/turingmarket/backups/v060-crm-sales-workspace-20260909-105724`
- Backup checksum manifest / 备份校验清单: `297` entries, all reverified; SHA-256 `a1cc1ae00ccecd75d54ccedadc80931d87ce18248dfb6759e22897557607d76d` / 共 `297` 项且全部复验通过
- Candidate tree SHA-256 / 候选树摘要: `82f8d751f499286f9cd39d101d53abbea56f8a7a5fab54c9aed954f39d5778a7`
- Acceptance facts SHA-256 / 验收事实摘要: `86771ad9e85e98fe7e1b951a6d14cc82bf70e8ddbc7b7794705eff1bf5b256fa`
- Replay evidence SHA-256 / 回放证据摘要: `417d99395c76ee080cd8fcb91964f00898cec57aa8014c1b3a23c461b85145d5`
- Parser evidence SHA-256 / 解析器证据摘要: `86a0667137d4dcbb2631ac1cb141eaa7407b26652172e2334d035eda0e28062c`
- Accepted parser runtime SHA-256 / 已接受解析器运行时摘要: `04630b2ee928ba8031a4ea5ce6435519161c19e05884813e5ec623d12adb38ce`
- Accepted source SHA-256 / 已接受来源摘要: `6dd905df47767843683ebd29efbd4028ddab83b49411a4e7d29ca72c21e9d713`
- Public health, home, and `app.js` returned `200`. Administrator login, identity, all-conversation AI audit, M4 collaboration read, anonymous `401`, controlled `404` on historical unlinked payment/settlement custody, logout, and revoked-session `401` passed. / 公网健康、首页和 `app.js` 返回 `200`；管理员登录、身份读取、全部 AI 对话审计、M4 合作读取、匿名 `401`、历史未关联记录的收付款/结算受控 `404`、注销及会话撤销 `401` 均通过。
- A production-database transaction completed three active payment entries, five non-retrievable financial evidence entries, self-review rejection, and independent settlement approval. The entire transaction was rolled back; fixture residue is `0`. / 生产数据库事务内完成三条有效收付款、五条不可检索财务凭证、自审拒绝及独立结算通过；随后整笔回滚，验收夹具残留为 `0`。
- Six changed runtime files exactly match local SHA-256 values. PM2 is online with restart count `0`; Nginx is active. Migrations are `16/16`, `integrity_check=ok`, foreign-key violations are `0`, and active sessions are `0`. / 六个变更运行文件与本地 SHA-256 完全一致；PM2 online 且重启计数 `0`，Nginx active；迁移 `16/16`、`integrity_check=ok`、外键异常 `0`、活动会话 `0`。
- No fake customer, campaign, influencer, collaboration, payment, or settlement record remains, and no real Feishu write occurred. Frozen proposal `ppt.js` SHA-256 remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 未保留虚假客户、活动、网红、合作、付款或结算记录，也未执行真实飞书写入；冻结方案 `ppt.js` SHA-256 保持不变。

## Delivery Cadence And Next / 发布节奏与下一步

- Manual payment evidence and four-eyes settlement are accepted. Automated provider receipts, financial reconciliation import, approval attachments, e-signature, richer publication custody, and campaign review remain independent slices. / 人工收付款凭证与四眼结算已通过验收；自动支付回单、财务对账导入、审批附件、电子签约、更完整发布保管及项目复盘继续拆为独立切片。
- Normal features continue to use affected tests, one independent review, a recoverable backup, immediate production deployment, and online feature acceptance. Full suites remain risk-triggered. / 普通功能继续执行受影响测试、一次独立审查、可恢复备份、立即生产部署和线上功能验收；完整套件仅在风险触发时执行。
- Real Feishu delivery and automatic data providers remain gated by approved production configuration and a test campaign. / 真实飞书投递和自动数据服务商仍须具备获批生产配置与测试活动后单独验收。
