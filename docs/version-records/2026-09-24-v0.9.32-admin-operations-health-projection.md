# v0.9.32 Admin Operations Health Projection / 管理控制室运营健康投影

## Scope / 范围

Deliver one independently useful Phase 8 control-room slice: expose read-only operational health for provider runs, Feishu outbox, influencer imports, workflows, and security events through the existing platform-admin operations surface. / 交付一个独立可用的 Phase 8 管理控制室切片：在现有平台管理员运营事件入口增加 Provider、飞书 outbox、网红导入、工作流和安全事件的只读健康投影。

## Implementation / 实现

- Extended `admin_operations_service` with fixed-table/column operational health projections and defensive table/column existence checks for legacy-compatible startup.
- Extended the existing admin operations UI with a health summary region; no new navigation surface or write action was added.
- Preserved server-side platform-admin enforcement and excluded `admin_operations_read` from the security business-event count.

## Verification / 验证

- Admin operations service, route, and tenant-directory UI tests: `28 passed / 0 failed`.
- `node --check platform/server/services/admin_operations_service.js` passed.
- `node --check platform/app.js` passed.
- `git diff --check` passed.
- Independent code review: `APPROVE_WITH_NOTES`; no HIGH/CRITICAL issue. Note: the projection is all-time aggregate status, not a replacement for detailed per-organization operational drill-down.

## Release Boundary / 发布边界

No production deployment, credential change, password reset, credential rotation, or `derrick` smoke test was performed in this implementation round. Production release requires a timestamped backup, dedicated release-smoke identity, health/auth/core-path checks, and remote evidence. / 本实现轮未执行生产部署、凭据修改、密码重置、凭据轮换或 `derrick` 烟测。生产发布仍需时间戳备份、专用 release-smoke 身份、健康/鉴权/核心路径检查和远端证据。
