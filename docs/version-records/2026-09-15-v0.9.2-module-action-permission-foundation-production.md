# v0.9.2 Module/Action Permission Foundation Production Record / 模块操作权限策略底座生产记录

## Release / 发布

- Date / 日期: `2026-09-15`
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Scope / 范围: central server-side permission policy and policy-backed legacy administrator gate / 中央服务端权限策略与策略化既有管理员入口
- Feature implementation / 功能实现: `a857d022a4f8360e811f545247ca3ba54422404b`
- Security fix / 安全修复: `cc23692905b16451e624584de75f6aeb46090dc9`
- Independent review / 独立审查: final `APPROVE`, no remaining P0-P3 / 最终 `APPROVE`，无剩余 P0-P3
- Database / 数据库: schema remains `v20`, no migration / 保持 schema `v20`，无迁移
- Production feature source / 生产功能源码: `cc23692905b16451e624584de75f6aeb46090dc9`

## Delivered / 已交付

- Added a closed, fail-closed `platform_administration.manage` policy service. It re-reads the active user from SQLite and rejects ordinary, inactive, missing, mismatched, malformed, unknown, or unavailable authoritative identity facts. / 新增封闭、默认拒绝的 `platform_administration.manage` 策略服务；每次从 SQLite 重读有效用户，并拒绝普通、失活、缺失、角色不一致、畸形、未知或权威事实不可用的身份。
- Existing `adminOnly` now delegates to the policy service while preserving exactly HTTP `403` with `{ error: 'Admin only' }`. Existing route URLs, authentication payloads, service-level rechecks, and privileged audit behavior remain unchanged. / 既有 `adminOnly` 已委托策略服务，同时精确保留原 `403` 返回；路由、登录返回、服务层复核和特权审计行为不变。
- Roles are projected only from live platform, organization, and team records. Browser-supplied role/permission/context fields cannot grant platform access. Company owner and read-only are reserved but unassignable until an explicit persistence migration is delivered. / 角色只从实时平台、组织与团队记录投影；浏览器提交的角色、权限或上下文不能授予平台权限。企业所有者与只读角色在显式持久化迁移前保留但不可分配。
- The guarded deploy inventory contains the new service and focused test, and the remote candidate gate explicitly runs the test. No frontend, CRM, M3/M4, AI/knowledge, influencer, Feishu, workflow, export, database, or PPT source changed. / 受控发布清单包含新服务和聚焦测试，远端候选门禁会显式执行该测试；前端及其他业务模块、数据库和 PPT 源码均未改动。

## Focused Verification And Review / 聚焦验证与审查

- Controller verification passed `17/17`: permission rules `6/6`, tenant-directory compatibility `8/8`, real Express administrator/user integration `1/1`, and deployment inventory/gate contracts `2/2`. / 主控聚焦验证共 `17/17` 通过。
- JavaScript syntax, `git diff --check`, focused secret scan, and `deploy_v8.ps1 -ValidateLocalOnly` passed with `LOCAL_DEPLOY_PREFLIGHT_OK`. / JavaScript 语法、差异、聚焦凭据扫描和本地受控发布预检通过。
- The first independent review found one P2 coercion/getter boundary. Arrays, boxed strings, coercible objects, and throwing request/principal getters now deny without throwing; focused re-review returned `APPROVE`. / 首次独立审查发现一个 P2 强制转换/getter 边界；修复后相关畸形输入均无异常地拒绝，聚焦复审为 `APPROVE`。
- Candidate file SHA-256: permission service `16e784a9b3eecf9537d5c6ef3032952f8f7651c46886846ccd083764fe0cfefb`, `server.js` `28659826f0f659d972350321012cc2d619b25d65f60d3fd41ae6e7a8ab733f2d`, frozen `ppt.js` `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`.

## Production Evidence / 生产证据

- The guarded release completed with `DEPLOY_OK` under run `30e8cafd9c9041e4a82a8b93726c4849`. Trusted source SHA-256 is `7232eed9fe8043b82bfba8091225ba9517dafb353c8e6ac43e848d89871b633c`; accepted candidate tree SHA-256 is `e80fe61f4e442f12aaae3187b4017db70fe3331fccfa95e31b3b14dda65d6f6c`. / 受控发布以该运行 ID 完成并明确返回 `DEPLOY_OK`，可信源码与验收候选树摘要如上。
- Recoverable backup `/root/turingmarket/backups/v060-crm-sales-workspace-20260915-135244` was completed before mutation. Its `327`-entry manifest SHA-256 is `c18ae78f4add78d5e80abeb0ec313cf98bee48750ea06c360ae51278b83ed612`; the `35`-entry cutover snapshot manifest SHA-256 is `0244e6d253734f971cc863c13d6bc38c5416e81fa63e550e4fe9b28b1579ad3c`. / 生产变更前已完成可恢复备份，初始清单与切换快照均已复验。
- Remote candidate gates passed policy tests `6/6`, replay gates `8/8`, release-guard tests `21/21`, and browser smoke `2/2`. Public `/api/health` returned `200` with parser ready. / 远端候选权限、重放、发布守卫及浏览器冒烟全部通过，公网健康接口为 `200` 且解析器就绪。
- Authenticated online smoke passed for administrator `derrick`: login `200`, administrator directory `200`, logout `200`, and revoked token `401`. A live ordinary user with a forged administrator claim was reloaded from SQLite and denied with the exact `403 { error: 'Admin only' }` contract; the temporary test session was removed. / 管理员登录、目录、注销及令牌撤销通过；普通用户即使携带伪造管理员声明仍按实时数据库身份被精确拒绝，临时会话已清除。
- PM2 is `online` with restart count `0` and PID `38776`; Nginx is `active`. SQLite remains schema `v20`, `quick_check=ok`, foreign-key violations `0`, and active sessions `0`. Production SHA-256 matches local for the permission service `16e784a9b3eecf9537d5c6ef3032952f8f7651c46886846ccd083764fe0cfefb`, `server.js` `28659826f0f659d972350321012cc2d619b25d65f60d3fd41ae6e7a8ab733f2d`, and frozen `ppt.js` `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 进程、数据库、会话与线上文件一致性均通过验收。

## Next Slice / 下一切片

- Persist explicit organization-scoped company-owner and read-only profiles, then adopt named module/actions one bounded business module at a time without replacing existing resource custody. / 持久化组织范围的企业所有者和只读档案，再逐个有界业务模块接入命名权限，同时保留既有资源归属校验。
