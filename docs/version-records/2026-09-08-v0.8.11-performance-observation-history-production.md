# v0.8.11 Performance Observation History Production Record

## Release

- Date: `2026-09-08`
- Scope: Phase 7B.1g, single-video observation history and adjacent deltas
- Status: production deployed and verified
- Implementation commits: `031a0a8`, `8205719`
- Database: schema `v14`, no migration in this slice

## Delivered

- Added a collapsed history section to the existing performance data-entry dialog; no new page or replacement UI was introduced.
- Added campaign/content-scoped read API `GET /api/campaigns/:id/performance/contents/:contentId/observations` with page size `1-50`, a default of `20`, and stable `observed_at DESC, id DESC` business ordering.
- Recalculated core interaction rate for each immutable snapshot. Deltas exist only when both adjacent snapshots contain the same valid field; missing values remain unknown and cumulative decreases are labeled as correction/rollback events.
- Bound every continuation cursor to the first-page observation watermark so late backfills cannot alter an in-progress pagination sequence.
- Canonicalized new observation timestamps to millisecond UTC, sorted legacy mixed-precision timestamps chronologically, and retained duplicate replay recognition for prior no-millisecond imports.
- Restricted public history to views, impressions, likes, comments, saves, shares, clicks, conversions, and derived core interaction rate. Privileged correction notes retain the existing commercial-data permission boundary.

## Verification And Independent Review

- Affected performance service, route, and frontend contracts: `50/50` passed.
- Request-boundary contracts: `32/32` passed.
- Release contracts: `37/37` passed. Combined focused evidence: `119/119`.
- JavaScript syntax, focused secret scan, `git diff --check`, and `deploy_v8.ps1 -ValidateLocalOnly` passed.
- Initial independent review returned `REQUEST_CHANGES` for pagination consistency, timestamp ordering, and an over-broad metric allowlist. Each issue received a targeted regression test and implementation fix; final independent verdict was `APPROVE` with no Important/Critical finding open.
- Guarded candidate acceptance passed real Express replay `8/8`, release guard `21/21`, built-in browser smoke `2/2`, migration rehearsal, parser/runtime checks, capacity checks, and Nginx validation.

## Production Evidence

- Backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260908-015011`
- Backup manifest SHA-256: `4f73e2cf081cd010191d454594d7262147fff06eac573d4a86f53de9d55200c2`; full manifest verification passed.
- Candidate tree SHA-256: `de354969bfd5483dd31031f4fe9da6d56550556487e4f5006cd21dee87b0a594`.
- Public health and `/performance-monitor`: `200`; anonymous observation-history route: `401`.
- Authenticated campaign/content observation-history request: `200`, contract `performance-observation-history-v1`, request ID echoed, and acceptance session removed afterward.
- PM2: `online`, restart count `0`; Nginx: `active`.
- SQLite: migration version `14`, `quick_check=ok`, foreign-key violations `0`.
- Remote `app.js`, performance styles, service, route, request contract, and server hashes exactly matched the reviewed local commit. Remote JavaScript syntax passed.
- Frozen `ppt.js` SHA-256: `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`.

The first production command invocation was rejected locally before SSH because the required server environment variable was absent; after supplying the approved host value, the guarded deployment completed normally. Production contained one monitored content record and zero metric observations, so acceptance validated the genuine empty history without creating test business data.

## Next

Prioritize the commercial four-eyes approval flow and preserve the last approved KPI while a new draft is pending. Real Feishu projection, scheduled provider collection, and media-content analysis remain separately authorized releases.
