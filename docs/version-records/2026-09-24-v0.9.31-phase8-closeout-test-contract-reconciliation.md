# v0.9.31 Phase 8 Closeout Test Contract Reconciliation / 阶段 8 收口测试契约对账

## Scope / 范围

Verification-only checkpoint on the authoritative production delivery branch. It does not change runtime behavior, schema bytes, credentials, or production deployment. / 本版本仅对权威生产交付分支进行验证契约收口，不改变运行时代码、schema 字节、凭据或生产部署。

## Evidence / 证据

- `knowledge_tenant_ownership_migration.test.js`: `9 passed / 0 failed`.
- `organization_campaign_access.test.js`: `35 passed / 0 failed / 1 skipped`.
- `node --check` passed for both changed test files; `git diff --check` passed.
- No production deploy, credential change, password reset, credential rotation, or `derrick` smoke test was performed.

## Changes / 变更

- Updated the migration startup assertion from historical v24 to current v31 while retaining v24 behavior checks. / 将迁移启动断言从历史 v24 更新到当前 v31，同时保留 v24 行为检查。
- Isolated malformed activity-flag fixtures and added a current database-boundary projection assertion. / 隔离非法 activity flag 夹具，并新增当前数据库边界投影断言。
- The historical multi-state fixture remains explicitly skipped because restoring an invalid BLOB state through the full governance trigger graph is not a valid production transaction. / 历史多状态夹具明确 skip，因为在完整治理触发器图中恢复非法 BLOB 状态不是有效生产事务。

## Open Phase 8 Boundaries / 阶段 8 未完成边界

Phase 8 remains open. Unified provider/import/workflow/security-event control-room coverage, remaining module-by-module tenant isolation, production-shaped restore/rollback proof, browser journeys, performance, accessibility, and final independent approvals still require separate evidence. / 阶段 8 仍未完成。服务商/导入/工作流/安全事件统一控制室、剩余模块逐项租户隔离、类生产恢复/回滚证明、浏览器旅程、性能、无障碍和最终独立批准仍需分别形成证据。
