+# v0.7.0 Task 2 M3 Campaign Demand Client / M3 活动需求分析接入

Date / 日期: 2026-08-26
Status / 状态: Production incremental slice / 生产增量切片已发布
Branch / 分支: `codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
Implementation commits / 实现提交: `6f99a69`, `3bc7952`

## Delivery / 交付

- The latest M3 demand-analysis UI now sends the active `campaign_id`, a fixed knowledge-only policy (`allow_web=false`), `Idempotency-Key`, and request ID to the existing audited backend. / 最新 M3 需求分析界面向既有审计后端发送当前 Campaign、固定仅知识库策略、幂等键和请求 ID。
- Duplicate clicks share one in-flight request and one idempotency key. Changing or resetting workflow context invalidates the prior generation so a stale response cannot overwrite the new Campaign. / 重复点击合并为同一在途请求和幂等键；切换或重置上下文会使旧代响应失效。
- Knowledge selection is scoped by Campaign. Unlinked legacy analysis keeps its prior request shape and behavior. / 知识选择按 Campaign 隔离，未关联旧流程保持兼容。
- The response stores conversation, message, and knowledge-reference IDs in the demand context and renders an escaped compact “知识依据与 AI 审计” block. / 回包中的会话、消息和知识引用 ID 写入需求上下文，并以转义后的紧凑审计区块显示。
- No proposal/PPT runtime, database migration, or application shell replacement is included in this slice. / 本切片不修改方案/PPT 运行时、不迁移数据库，也不替换产品壳层。

## Provider Format Compatibility / Provider 格式兼容

- Production diagnosis proved that DeepSeek can return explanatory text around one valid JSON object even when JSON-only output is requested. / 生产诊断确认 DeepSeek 即使收到仅返回 JSON 的指令，仍可能在合法对象前后附加说明。
- Demand analysis now extracts and validates one JSON object from that bounded response before normalization. Plain JSON remains the fast path; invalid text and arrays remain rejected. / 需求分析现从受限回包中提取并验证一个 JSON 对象；纯 JSON 仍走直接路径，无效文本和数组继续拒绝。
- The change is scoped to the demand-analysis compatibility adapter and does not weaken shared linked-chat persistence, Campaign authorization, RAG, or idempotency controls. / 改动仅位于需求分析兼容适配层，不放宽共享关联会话的持久化、Campaign 权限、RAG 或幂等控制。

## Scope / 范围

- Runtime files / 运行文件:
  - `platform/app.js`
  - `platform/server/services/latest_ui_compat_service.js`
- Focused tests and fixture / 聚焦测试与夹具:
  - `platform/server/tests/latest_ui_demand_campaign_client.test.js`
  - `platform/server/tests/latest_ui_ai_audit.test.js`
  - `platform/server/tests/fixtures/frontend-active-definitions.json`
- Database migration / 数据库迁移: none / 无。
- Production App build marker / 生产 App build 标识: unchanged `20260811-v060-crm-sales-workspace`.
- Frozen PPT build / 冻结 PPT build: unchanged `20260702-v916-kb-bridge-client-cn`.
- Frozen PPT SHA-256 / 冻结 PPT 哈希: unchanged `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`.

## Verification / 验证

- Client, audit, architecture, and public-static focused matrix / 前端、审计、架构与公开静态聚焦矩阵: 29 passed, 0 failed.
- Final provider-format and client matrix / 最终 Provider 格式与客户端矩阵: 15 passed, 0 failed.
- JavaScript syntax and diff validation / JavaScript 语法与差异校验: passed.
- Independent client review / 前端独立审查: `APPROVE`; no remaining P0/P1/P2 after stale-response, duplicate-submit, knowledge-scope, and async-behavior fixes.
- Independent provider-format review / Provider 格式独立审查: `APPROVE`; no P0/P1/P2.
- Browser automation / 浏览器自动化: not run for this narrow slice under the approved accelerated cadence; production API and served-byte checks are recorded below. / 按已批准轻量节奏，本窄切片未运行浏览器自动化，改以生产 API 与实际静态字节验收。

## Production Deployment / 生产部署

- UI backup / 前端备份: `/root/turingmarket/backups/v070-slice2-m3-demand-client-20260826-015448`.
- Service and database backup / 服务与数据库备份: `/root/turingmarket/backups/v070-slice2-m3-demand-json-20260826-021550`.
- Production `app.js` SHA-256: `79c5900f06d9b48f571247cb9da93817b1c44f76e95de22ce757b2801c40b4bd`.
- Production `latest_ui_compat_service.js` SHA-256: `ce8c7ce9a66f9103d949245ff5367aee24726ecb567e77293c4b94ac32e1d86e`.
- The first service restart used an incorrect 20-second health window and interrupted the parser startup self-test, so the script automatically restored the prior service file. The retained job was verified as the exact generated BMP self-test with no active systemd unit or user data, then removed through the project’s trusted no-follow cleanup controller. / 首次服务重启错误使用 20 秒健康窗口并中断解析器启动自检，脚本因此自动恢复旧服务；残留任务经核验为无活动单元、无用户数据的精确 BMP 自检任务，并通过项目受信 no-follow 清理控制器回收。
- The detailed parser self-test then passed, the prior platform recovered with the accepted manifest, and the corrected 180-second deployment completed successfully. / 随后详细解析器自检通过，旧平台按固定清单恢复健康，修正为 180 秒窗口的发布成功完成。

## Online Acceptance / 线上验收

- Public and loopback health are `ok`; parser `ready=true` with manifest `44db310046efe65bd68c110313b4887995c73e276e7d58f65fe037c09a973c5b`; PM2 and Nginx are online; SQLite `quick_check=ok`. / 公网、回环、解析器、PM2、Nginx 和 SQLite 完整性均通过。
- Administrator login, `/api/auth/me`, Campaign 1 read, linked demand analysis, and explicit logout passed. / 管理员登录、身份读取、Campaign 读取、关联需求分析和显式注销通过。
- The accepted response contains 8 knowledge references, no web search, and `archived_summary_id=null`. / 验收回包包含 8 条知识引用、未联网，且未归档未确认摘要。
- Replaying the same body and idempotency key returned the same conversation and message IDs. / 相同请求体与幂等键重放返回相同会话和消息 ID。
- Direct database evidence records conversation 22, message 44, exactly 2 messages, 8 references, 1 Campaign `ai_run` link, a completed 200 idempotency row, no archived summary, and zero recent admin smoke sessions after logout. / 数据库证据记录会话 22、消息 44、2 条消息、8 条引用、1 条业务关联、完成态 200 幂等记录、无摘要归档，且注销后近期测试会话为 0。

## Next Slice / 下一切片

Task 3 will connect Campaign-scoped knowledge and audited demand context to the proposal-draft and PPT pre-generation path while preserving AI draft -> human confirmation -> final archive and the frozen PPT bridge. / 切片 3 将把 Campaign 范围知识与已审计需求上下文接入方案草稿和 PPT 生成前链路，并继续保持 AI 草稿 -> 人工确认 -> 正式归档及冻结 PPT bridge。

