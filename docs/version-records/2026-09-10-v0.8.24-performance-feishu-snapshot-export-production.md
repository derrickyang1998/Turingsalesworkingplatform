# v0.8.24 Performance Feishu Snapshot Export Production Record / 效果飞书快照导出生产记录

## Release / 发布

- Date / 日期: `2026-09-10`
- Scope / 范围: approved Feishu field projection and current performance CSV export inside the existing Content Monitor / 既有内容监控内的已批准飞书字段投影与当前效果 CSV 导出
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Implementation / 实现: `13b1420`
- Production run / 生产运行: `6fa57d30566241118639599a07b95d42`
- Candidate / 候选: `v060-crm-sales-workspace-20260910-191704`
- Database / 数据库: migrations `17/17`, schema `v17` unchanged / 保持不变

## Delivered / 已交付

- Added “下载当前效果快照 CSV” to the existing campaign-scoped Feishu connection panel without adding a page or replacing the current product shell. The action names the approved mapping version; a pending draft cannot silently alter the export. / 在既有活动飞书连接面板增加当前效果快照 CSV 下载，不新增页面或替换当前产品壳层；操作明确显示已批准映射版本，待审草稿不会静默改变导出。
- The backend reads only `active_configuration` and obtains all current performance rows inside one SQLite deferred read transaction. It rejects inconsistent totals or duplicate publication identities and supports datasets beyond the normal list page boundary. / 后端只读取已批准配置，并在一个 SQLite 延迟读事务中取得全部当前效果行；总数不一致或内容编号重复时失败关闭，正常支持超过列表单页的数据集。
- Only publications with a latest observation are projected. Missing fields remain absent, unobserved publications are excluded instead of becoming zero, and the export has an explicit 5,000-record ceiling. / 仅投影已有最新观测值的内容；缺失字段保持缺失，未观测内容不会被填为零，并设置明确的 5,000 条上限。
- The CSV uses UTF-8 BOM and escapes formulas, newlines, NUL bytes, and quotes. Authentication and campaign-management permission remain server enforced. This release does not write to Feishu or enable external provider collection. / CSV 使用 UTF-8 BOM，并处理公式注入、换行、空字符和引号；登录及活动管理权限继续由服务端强制执行。本版本不写入飞书，也不启用外部数据采集。

## Focused Verification And Review / 聚焦验证与审查

- Affected projection, performance-service, route, request-contract, and frontend-contract checks passed `91/91`; the focused projection/manual/frontend subset passed `50/50`, and deployment inventory passed `1/1`. / 受影响投影、效果服务、路由、请求合同和前端合同共 `91/91` 通过；投影/人工效果/前端子集 `50/50`、部署清单 `1/1` 通过。
- JavaScript syntax, diff formatting, focused credential scan, and local deployment preflight passed. / JavaScript 语法、差异格式、定向凭据扫描和本地发布预检通过。
- Independent review re-checked stale cross-campaign downloads, multi-page snapshot stability, and approved-versus-draft mapping clarity. All three prior P2 findings are closed; final verdict is `PASS`. / 独立复审重新核验跨活动旧下载、多页快照稳定性及已批准/待审映射提示，三项既有 P2 均已关闭，最终结论为 `PASS`。

## Production Evidence / 生产证据

- Guarded candidate SHA-256 is `583764c2f974484fdfb22491ecf117bcbf928a4dd3559320d351cfc943d7543f`. Real Express replay passed `8/8`, release guard `21/21`, deployment browser smoke `2/2`, migration rehearsal, parser readiness/cache reuse, Nginx validation, and final acceptance. / 受保护候选通过真实 Express 回放、发布守卫、内置浏览器冒烟、迁移演练、解析器就绪/缓存复用、Nginx 与最终验收。
- Recoverable backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260910-191704`. Initial and cutover manifests contain 302 and 35 entries; SHA-256 values are `5303ee2e9cd30a63745a4e39d0ae0ac463c9514ac717f6d5ceb4192d58ce0e19` and `cd79934de0b622b4bedc0f3f072fd2200519d1f9d43c9a3916761a4ce1ae48c9`. / 可恢复备份的初始与切换清单均已保留并复核。
- Public `/api/health`, `/`, and `/app.js` returned `200`; the new export route returned `401` without authentication. Production administrator login, `/api/auth/me`, and logout passed. PM2 is online with restart count `0`, and Nginx is active. / 公网健康、首页、脚本、未登录权限拒绝、生产管理员登录/身份/注销、PM2 与 Nginx 均通过。
- SQLite reports migrations `17/17`, `quick_check=ok`, and zero foreign-key violations. The deployed projection service matches the reviewed local SHA-256 `3ba681eb374b52a8cfbadbd09dc74ca498a94baff99a01fbc215a6d8116e7246`. / SQLite 迁移、一致性和外键检查通过；线上投影服务与已审查本地文件哈希一致。
- Frozen `ppt.js` remains SHA-256 `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 冻结方案 PPT 保持不变。

## Cadence And Next / 节奏与下一步

- Next slice: campaign performance refresh queue and visible freshness state; then one explicitly approved provider, followed by live idempotent Feishu synchronization when production configuration is ready. / 下一切片为活动效果刷新队列与可见新鲜度状态；随后接入一个明确批准的数据服务商，并在生产飞书配置齐备后接入真实幂等同步。
- Continue focused checks, one independent review, verified backup, immediate production deployment, and online feature smoke for each normal slice. / 普通功能继续执行定向检查、一次独立审查、可验证备份、立即上线和线上功能冒烟。
