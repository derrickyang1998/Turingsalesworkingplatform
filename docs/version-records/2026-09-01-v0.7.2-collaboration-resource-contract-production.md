# v0.7.2 Collaboration Resource Contract Production Record

## Scope / 范围

- Implementation and release commits: `01c202e`, `0fe9990`, `002e75a`.
- M4 orders now submit `turingmarket.collaboration-order.v1`: project, product, order type, resource/deliverable, integer quote, and contract/PO reference are distinct fields.
- A v1 resource locks its canonical quote after order creation and rejects an accompanying `proposal_notes` value. This prevents price drift and dual sources of truth.
- Legacy resources without a schema or with `legacy.v0` continue to use the existing persistence, note, and quote fallbacks. Linked v1 orders archive canonical resource data, quote, and business context; normalized replay remains idempotent.

## Verification / 验证

- Focused tests: resource contract `7/7`; campaign collaboration security/idempotency `22/22`; influencer workflow `32/32`; M4 client `5/5`; frontend architecture and browser-baseline tooling `28/28`.
- JavaScript syntax checks, `git diff --check`, `deploy_v8.ps1 -ValidateLocalOnly`, the focused deployment inventory contract, and the focused parser-cache release contract passed.
- Independent review verdicts: feature re-review `APPROVE`; deployment source inventory `APPROVE`; parser-cache cap change `APPROVE`.

## Production Evidence / 生产证据

- Guarded deployment completed successfully after two safe pre-mutation candidate aborts: first, the new service was absent from the deploy inventory; second, a cold isolated parser dependency cache exceeded its former 30-minute hard limit. The final release adds the service to the explicit inventory and changes only the isolated cache unit cap to 60 minutes; source/hash checks, cache completion proof, and cleanup remain enforced.
- Backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260901-210048`; top-level `SHA256SUMS` verification passed; manifest SHA-256 `a0b5e8b48470f15a157d0d7ff626d8d32db69a7dec5fae1c8487444c70f0f894`.
- Public and loopback `/api/health`: `200`, `status=ok`, parser ready; public `/m4`: `200`; PM2 and Nginx online; deployment lock cleared; SQLite `quick_check=ok`; `foreign_key_check` returned zero rows.
- No production M4 create, update, or state-transition smoke was performed because it would write to real business records. The online acceptance proves release health and route availability, not a synthetic business transaction.

## Follow-up / 后续

- Evaluate a separately reviewed parser-cache reuse or prewarm slice to reduce cold-release time without weakening verification.
- Keep Feishu Bitable writes and real test-campaign transaction acceptance as isolated, independently releasable slices.
