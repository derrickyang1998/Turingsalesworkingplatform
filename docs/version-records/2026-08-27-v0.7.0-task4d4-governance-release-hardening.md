# v0.7.0 Task 4D4 Governance Release Hardening / 知识治理发布加固

Date / 日期: 2026-08-27
Status / 状态: Production incremental slice / 生产增量切片已发布
Branch / 分支: `codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
Implementation commit / 实现提交: `f65fcf47d51fdd3487843211c5cc566ee3835823`

## Delivery / 交付

- The trusted migration contract now explicitly accepts exact managed source schemas `1`, `6`, and `7`. Schema v1 and v6 run the pinned two-pass preservation verifier to v7; exact managed v7 follows the verified no-op path. A tampered migration 007 that changes existing business data is rejected. / 可信迁移合同显式接受受管 schema `1`、`6`、`7`；v1/v6 通过固定双跑保持性验证迁移到 v7，受管 v7 走已验证 no-op 路径，篡改 007 并改写既有业务数据会被拒绝。
- `purgeEphemeralKnowledgeEntries` is the only supported deployment-smoke cleanup path. It accepts only exact `deployment_smoke` identities, rejects referenced or campaign-custodied entries and incomplete lineages, removes FTS/governance/entry projections in one nested savepoint, and proves no residual rows. / `purgeEphemeralKnowledgeEntries` 是唯一支持的部署烟测清理入口；仅接受精确验收身份，拒绝已有引用、Campaign 托管或不完整血缘，并在同一嵌套保存点内清理 FTS、治理和知识投影后证明无残留。
- Campaign knowledge candidates, direct attachment, list, detail, and AI-use paths now share the same governance eligibility. Rejected, expired, superseded, or non-current knowledge cannot be linked, listed, opened, or counted as AI use. / Campaign 知识候选、直接关联、列表、详情和 AI 使用统一治理资格；已拒绝、过期、被替代或非当前知识不能关联、列出、打开或增加 AI 使用计数。
- The production bootstrap installs and enables an exact root-owned `pm2-root.service`; deployment verifies its immutable entrypoints and enabled state before persisting the PM2 dump. Restart and rollback recreate the exact ecosystem projection without stale managed environment values. / 生产 bootstrap 安装并启用精确 root 所有的 `pm2-root.service`；发布在保存 PM2 dump 前验证固定入口和 enabled 状态，重启与回滚均从 ecosystem 重建精确投影，不继承陈旧环境变量。
- The guarded branch is the active v0.7 delivery branch. Per-feature delivery uses affected tests, one independent review, verified backup, immediate production deployment, and online smoke; full non-browser and browser matrices remain phase-closeout or risk-trigger gates. The v0.6 shell and frozen PPT renderer were not modified. / 受控分支更新为当前 v0.7 交付分支；单功能采用受影响测试、一次独立审查、可验证备份、立即上线和线上冒烟，完整非浏览器与浏览器矩阵保留到阶段收口或风险触发。v0.6 壳层和冻结 PPT renderer 未修改。

## Review And Focused Verification / 审查与定向验证

- Final independent review: `APPROVE`, with zero Critical, Important, or Minor findings. The review closed the nested-transaction partial-cleanup risk, Campaign candidate/direct-attach governance gaps, stale v0.6 release assertions, and inaccurate FTS/PM2 incident evidence. / 最终独立复审为 `APPROVE`，Critical、Important、Minor 均为 0；嵌套事务半清理、Campaign 候选/直接关联治理缺口、旧 v0.6 发布断言及不准确 FTS/PM2 事件记录均已关闭。
- Focused evidence includes deployment-source contract `57/57`, public static boundary `9/9` after moving the temporary fixture to D: because C: lacked space, runtime hardening `58 passed / 3 Linux-only skipped`, cleanup rollback `4/4`, trusted v6/v7 paths `5/5`, and the final Campaign negative case `1/1`. / 定向证据包括发布源合同 `57/57`、将临时夹具移至 D 盘后的公网静态边界 `9/9`、运行时加固 `58 通过 / 3 个仅 Linux 跳过`、清理回滚 `4/4`、可信 v6/v7 路径 `5/5` 及最终 Campaign 负向用例 `1/1`。
- JavaScript, Bash, and PowerShell syntax, `git diff --check`, focused secret scan, all 50 trusted-manifest files, frozen frontend boundaries, and real `-ValidateLocalOnly` passed. Browser automation remains deferred to Phase 6 closeout under the approved cadence. / JavaScript、Bash、PowerShell 语法、diff、聚焦敏感信息扫描、50 文件可信清单、冻结前端边界及真实本地发布预检均通过；浏览器自动化按批准节奏保留到阶段 6 收口。

## Production Deployment / 生产部署

- GitHub and deployed implementation SHA / GitHub 与部署实现 SHA: `f65fcf47d51fdd3487843211c5cc566ee3835823`.
- Payload SHA-256 / 增量包哈希: `47538fe9ff84e82fca3e71d688118992c7846640e184370d0a1f3dcea3cb0053`.
- Payload manifest SHA-256 / 增量清单哈希: `bc6f6155293074fa292dd6adeeea1243cfb1251e03cd4da3db64946948582ddd`.
- Verified backup / 可验证备份: `/root/turingmarket/backups/v070-slice4d4-governance-hardening-20260827-112449`.
- Backup aggregate SHA-256 / 备份聚合哈希: `6eaa544d52701b80aa91b0f4cf81a7b8d8ec03b19815a1a497b3d898be83c2b8`.
- Release stage / 发布阶段目录: `/root/turingmarket/releases/v070-slice4d4-governance-hardening-20260827-112449`.
- Capacity / 容量: required `188,096 KiB`; available `8,159,448 KiB`.
- The backup contains the nine replaced files, a consistent SQLite Backup API copy, PM2 unit/dump evidence, exact checksums, and an executable rollback script. A separate post-verifier rechecked the complete backup manifest and rollback syntax. / 备份包含 9 个替换文件、一致 SQLite Backup API 副本、PM2 unit/dump 证据、精确校验和及可执行回滚脚本；独立后验脚本再次验证完整备份清单与回滚语法。

## Online Acceptance / 线上验收

- Internal and public health are `ok`; parser readiness is true, parser manifest remains `44db310046efe65bd68c110313b4887995c73e276e7d58f65fe037c09a973c5b`, PM2 PID is `7177`, `pm2-root.service` is enabled/active, and Nginx is active. / 内外健康均为 `ok`，解析器 ready 且清单不变，PM2 PID 为 `7177`，PM2 systemd unit 已启用并运行，Nginx active。
- Production remains schema v7 with knowledge/governance `123/123` and knowledge chunks/FTS `857/857`; SQLite `quick_check=ok`, foreign-key violations are zero, and FTS integrity passes. / 生产保持 schema v7，知识/治理 `123/123`、知识分块/FTS `857/857`，SQLite 完整性、外键及 FTS 完整性均通过。
- Real HTTP smoke passed administrator and ordinary-user login, candidate defaults, authorization rejection, confirmation, supersession, retention, active retrieval, Campaign candidate filtering, and direct inactive-knowledge attach rejection without a link or idempotency ledger. / 真实 HTTP 冒烟通过双角色登录、候选默认、权限拒绝、确认、替代、保留、活动检索、Campaign 候选过滤及失效知识直接关联拒绝，且不产生链接或幂等账本。
- Final cleanup left zero active smoke users, zero smoke sessions, zero `deployment_smoke` knowledge, and exact `857/857` FTS cardinality. Remote `/tmp` upload artifacts were removed after independent post-verification. / 最终启用验收账号、会话、`deployment_smoke` 知识均为 0，FTS 精确保持 `857/857`；独立后验通过后已清理远端 `/tmp` 上传件。
- Installed/public `app.js` SHA-256 remains `ed304b9d21ab2fbee46f7449eb2b3af432b8bdb6a37e77434f8ae8ccc03a5c93`; frozen `ppt.js` SHA-256 remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 安装与公网客户端字节一致，冻结 PPT 哈希保持不变。

## Next Slice / 下一切片

Task 4E will unify confirmed proposals, PPT artifacts, project reviews, and other approved outputs behind the deduplicated business-knowledge ingestion contract, then run the remaining Phase 6 closeout gates. / Task 4E 将确认方案、PPT 成品、项目复盘及其他确认产物统一接入去重业务知识入库合同，随后执行阶段 6 剩余收口门禁。
