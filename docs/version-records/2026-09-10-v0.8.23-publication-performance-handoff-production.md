# v0.8.23 Publication Performance Handoff Production Record / 发布凭证与效果追踪生产记录

## Release / 发布

- Date / 日期: `2026-09-10`
- Scope / 范围: final publication custody and performance-monitor handoff inside the existing M4 workflow / 既有 M4 流程内的最终发布保管与效果监控衔接
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Implementation / 实现: `b0d5693`, `496b882`
- Production run / 生产运行: `97d1b77ab63b43918483a3edd1544f2a`
- Candidate / 候选: `v060-crm-sales-workspace-20260910-172113`
- Database / 数据库: migrations `17/17`, schema `v17`

## Delivered / 已交付

- Added a dedicated final-publication confirmation endpoint and a compact modal in the existing M4 collaboration workspace. Each confirmation contains 1-20 explicit deliverable keys, final public URLs, publication times, and optional notes; review draft URLs are never inferred as final links. / 在既有 M4 合作工作区新增专用最终发布确认接口及紧凑弹窗；一次支持 1-20 条明确交付物、最终公开链接、发布时间和备注，不会把审核草稿链接推断为正式链接。
- Added append-only `collaboration_publication_custody` evidence that binds collaboration, deliverable, approved review evidence, Campaign publication relation, tracked content, and a non-RAG knowledge record. Updates and deletes are blocked by SQLite triggers. / 新增只追加发布保管证据，串联合作、交付物、已批准审核、Campaign 发布关系、监控内容及不可参与 RAG 的知识凭证，并由 SQLite 触发器禁止更新和删除。
- Existing canonical campaign publications are reused only when unbound and owned by the same creator. Duplicate deliverables, duplicate content, cross-creator binding, stale versions, unauthorized writes, and generic publication bypasses fail closed. / 既有规范化活动内容仅在未绑定且属于同一网红时复用；重复交付物、重复内容、跨达人绑定、旧版本、越权及通用接口绕过均失败关闭。
- Server-projected `can_publish` controls the UI. Confirmed links appear in the collaboration row and open the existing performance monitor without replacing the current shell, AI/knowledge flow, Feishu fallback, or frozen proposal PPT. / 服务端投影 `can_publish` 控制前端操作；确认链接显示在合作行并进入既有效果看板，不替换当前壳层、AI/知识、飞书降级或冻结方案 PPT。

## Focused Verification And Review / 聚焦验证与审查

- The latest publication/content-review service file passed `10/10`; migration/settlement compatibility passed `5/5`; affected M4, request-contract, release-contract, syntax, diff, credential, frozen-PPT, and local deployment preflight checks passed. / 最新发布与内容审核服务 `10/10`、迁移与结算兼容 `5/5` 通过；受影响 M4、请求合同、发布合同、语法、差异、凭据、冻结 PPT 及本地发布预检通过。
- Independent review found one P2 creator-ownership gap when reusing an existing tracked publication. The service now rejects mismatched `creator_id`, with an atomic rollback regression test; re-review returned `APPROVE`. / 独立审查发现一项复用既有监控内容时的达人归属缺口；新增 `creator_id` 冲突门禁与原子回滚用例后复审为 `APPROVE`。
- The first remote candidate stopped before production mutation because one replay assertion still expected migrations 1-16. The assertion was updated and its focused replay passed before the final deployment. / 首个远端候选因一处回放断言仍只接受迁移 1-16 而在生产变更前停止；更新断言并定向通过后才执行正式发布。

## Production Evidence / 生产证据

- Guarded candidate SHA-256 is `0ab75d602bdcf44a444ee7a59b73aec675b912725d10bac57414c1e6a41798c0`. Real Express replay passed `8/8`, release guard `21/21`, deployment browser smoke `2/2`, migration rehearsal, parser readiness, Nginx validation, and final acceptance. The parser runtime emitted `PARSER_RUNTIME_CACHE_REUSED`. / 受保护候选完成真实回放、发布守卫、浏览器冒烟、迁移预演、解析器、Nginx 与最终验收，并首次实际命中解析器运行时缓存。
- Recoverable backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260910-172113`. Initial and cutover manifests contain 297 and 35 entries; their SHA-256 values are `8b72d031c5e8eb2c1b1aa2ef3e618ca044ce48f4a72a8b508a2fb633a01a32d4` and `902816e7f530126ee0f767e260322cc81b1b59bd5ff236e1b00c6359ff790aef`. / 可恢复备份的初始与切换清单均已保留并复核。
- Public `/api/health`, `/`, and `/app.js` returned `200`; unauthenticated publication confirmation returned `401`. PM2 is online with restart count `0`, and Nginx is active. / 公网健康、首页、脚本、未登录权限拒绝、PM2 与 Nginx 均通过。
- SQLite reports `quick_check=ok`, zero foreign-key violations, migrations `17/17`, one custody table with four enforcement triggers, and knowledge chunks/FTS `914/914`. Production hashes match the reviewed local runtime files. / SQLite 一致性、外键、迁移、保管表/触发器、知识索引及线上文件哈希均通过。
- Frozen `ppt.js` remains SHA-256 `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 冻结方案 PPT 保持不变。

## Cadence And Next / 节奏与下一步

- Continue the approved feature-by-feature cadence: affected checks, one independent review, verified backup, immediate production deployment, and online smoke. Broader suites remain reserved for broad-risk changes and phase closeout. / 继续采用单功能受影响检查、一次独立审查、可验证备份、立即上线和线上冒烟；广域测试保留给高风险变更或阶段收口。
