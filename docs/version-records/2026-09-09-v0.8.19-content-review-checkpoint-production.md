# v0.8.19 Content Review Checkpoint Production Record / 内容审核凭证关口生产记录

## Release / 发布

- Date / 日期: `2026-09-09`
- Scope / 范围: Phase 7 M4 content submission, independent review, immutable history, and publication gate / 阶段 7 M4 内容提交、独立审核、不可变历史与发布门禁
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Plan / 计划: `bc45876`
- Implementation / 实现: `aced357`
- Database / 数据库: schema `v16`, migrations `16/16` / schema `v16`，迁移 `16/16`
- Production deployment run / 生产发布运行: `ea249bccf56042cf9f3dc1965e71e45f`

## Delivered / 已交付

- Existing M4 collaboration rows now provide evidence-backed content submission, independent approval or change requests, resubmission, and publication gating without adding or replacing a page. / 既有 M4 合作行现已支持带凭证的内容送审、独立通过或退回、重新提交和发布门禁，未新增或替换页面。
- Submissions require a canonical HTTPS URL, version label, and note. Decisions require the Campaign owner or organization administrator, and the submitter cannot review the same submission. / 送审要求规范 HTTPS 链接、版本标识和说明；审核决定仅限 Campaign 负责人或组织管理员，提交人不得审核自己的同一版本。
- Every submission and decision is stored as append-only Campaign-linked knowledge evidence with deterministic identity, team visibility, and full chain validation. Raw URLs and notes remain in protected metadata and are marked `retrieval_eligible:false`. / 每次提交和决定均保存为只追加、确定性标识、团队可见且完整校验的 Campaign 关联知识凭证；原始链接和说明仅在受控元数据中保存，并标记为 `retrieval_eligible:false`。
- The reserved `collaboration_content_review` source and entry namespaces cannot be fabricated through generic JSON, ingest, or upload knowledge paths. / 保留的 `collaboration_content_review` 来源与条目命名空间无法通过通用 JSON、ingest 或上传知识路径伪造。
- Ordered history is read in bounded pages without the former 40-event lockout and is rendered in an escaped, scroll-bounded disclosure in M4. / 有序历史改为分页读取，不再受原 40 条事件上限锁死，并在 M4 中以转义、限高可滚动的展开区展示。
- The API projects `can_submit` and `can_decide`; M4 only exposes valid actions. Historical completed v2 execution records without publication evidence can explicitly enter the review recovery path. / API 投影 `can_submit` 与 `can_decide`，M4 只展示可执行操作；缺少发布凭证的历史已完成 v2 执行记录可显式进入审核修复路径。
- Existing imports, filters, saved views, exports, Feishu controls, commercial terms, contract confirmation/PDF custody, AI/knowledge flows, and frozen proposal PPT remain unchanged. / 既有导入、筛选、保存视图、导出、飞书控制、商业条款、签约确认/PDF 保管、AI/知识链路及冻结方案 PPT 均保持不变。

## Focused Verification And Independent Review / 聚焦验证与独立审查

- The approved lightweight matrix passed `54/54`: content-review service `7/7`, M4 client `17/17`, request pipeline `20/20`, reserved JSON paths `3/3`, reserved upload path `1/1`, route forwarding `1/1`, and key collaboration security compatibility `5/5`. / 已批准的轻量矩阵 `54/54` 通过：内容审核服务 `7/7`、M4 客户端 `17/17`、请求管线 `20/20`、保留 JSON 路径 `3/3`、保留上传路径 `1/1`、路由转发 `1/1`、关键合作安全兼容 `5/5`。
- JavaScript syntax passed for `12` affected files. Diff formatting, focused secret scanning, frozen PPT hash verification, and local deployment preflight passed. / `12` 个受影响 JavaScript 文件语法通过；差异格式、定向密钥扫描、冻结 PPT 哈希和本地发布预检通过。
- Independent review initially found five release blockers: namespace takeover, incomplete history UI, the 40-event lockout, an unrecoverable historical completed-v2 action, and permission-blind review controls. All were fixed and the focused re-review returned `APPROVE`, with no open P0-P2 finding. / 独立审查首次发现五项发布阻断：命名空间抢占、不完整历史界面、40 条事件锁死、历史已完成 v2 无法修复，以及未按权限展示审核操作；全部修复后聚焦复审结论为 `APPROVE`，无 P0-P2 未关闭项。
- The production candidate passed real Express replay `8/8`, release gate `21/21`, built-in browser smoke `2/2`, migration rehearsal, parser runtime, capacity, and Nginx checks. / 生产候选通过真实 Express 回放 `8/8`、发布守卫 `21/21`、内置浏览器冒烟 `2/2`、迁移演练、解析器运行时、容量和 Nginx 检查。

## Production Evidence / 生产证据

- Recoverable backup / 可恢复备份: `/root/turingmarket/backups/v060-crm-sales-workspace-20260909-000619`
- Backup checksum manifest / 备份校验清单: `297` entries, all reverified; SHA-256 `2f4779b8ecc9b2acdbb5f11906695e882a5e4a7154aa4062985e6279bbd555ab` / 共 `297` 项且全部复验通过
- Candidate tree SHA-256 / 候选树摘要: `5a34156667e2411d18945ba6a9e98d955ca8eda160d20f229725d64ba23a5781`
- Acceptance facts SHA-256 / 验收事实摘要: `f2b81e864e898452b4101cb138c5991db7d9bb11f59520d84bd5f3e410117c60`
- Replay evidence SHA-256 / 回放证据摘要: `91f1bada0f4c25bbf24a0c24202beb43cd1ccbbc9fd9d690b0940d2d7c9e3564`
- Parser evidence SHA-256 / 解析器证据摘要: `249fe8907e0255874f40686084dbca1832d8643986687f3f6fe965012fc67c2d`
- Accepted parser runtime SHA-256 / 已接受解析器运行时摘要: `04630b2ee928ba8031a4ea5ce6435519161c19e05884813e5ec623d12adb38ce`
- Accepted source SHA-256 / 已接受来源摘要: `31365fcb5f60b1a71f6b7807ca4e9bdbb2dbcb4aab9ef4a4716ffc930c3eed66`
- Public health, home, and `app.js` returned `200`. Administrator login, identity, all-conversation AI audit, M4 collaboration read, logout, anonymous `401`, and controlled authenticated `404` on unavailable review custody passed. / 公网健康、首页和 `app.js` 返回 `200`；管理员登录、身份读取、全部 AI 对话审计、M4 合作读取、注销、匿名 `401` 及无可用审核保管记录时的受控登录态 `404` 均通过。
- The deployed UI contains the review-history and capability projections, and the five changed runtime files exactly match local SHA-256 values. / 已部署界面包含审核历史与权限能力投影，五个变更运行文件的 SHA-256 与本地完全一致。
- PM2 is online and Nginx is active. SQLite migrations are `16/16`, schema is `v16`, `integrity_check=ok`, foreign-key violations are `0`, and active sessions are `0`. / PM2 online、Nginx active；SQLite 迁移 `16/16`、schema `v16`、`integrity_check=ok`、外键异常 `0`、活动会话 `0`。
- No production content-review evidence or real Feishu write was created for acceptance. Frozen proposal `ppt.js` SHA-256 remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 验收未创建生产内容审核凭证或真实飞书写入；冻结方案 `ppt.js` SHA-256 保持不变。

## Delivery Cadence And Next / 发布节奏与下一步

- The content-review evidence and publication gate are accepted. Review attachments/annotations, customer approval portals, e-signature, richer publication custody, payment, settlement, and campaign review remain independent releases. / 内容审核凭证与发布门禁已通过验收；审核附件/批注、客户审核门户、电子签约、更完整发布保管、付款、结算和项目复盘继续拆为独立版本。
- Normal features continue to use affected tests, one independent review, a recoverable backup, immediate production deployment, and online feature smoke. Full suites remain risk-triggered. / 普通功能继续执行受影响测试、一次独立审查、可恢复备份、立即生产部署和线上功能冒烟；完整套件仍仅由风险触发。
- Real Feishu delivery and automatic data providers remain gated by approved production configuration and a test campaign. / 真实飞书投递和自动数据服务商仍须具备获批生产配置与测试活动后单独验收。
