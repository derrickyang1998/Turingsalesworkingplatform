# v0.8.1a Performance Manual Foundation Production Record

## Scope / 范围

- Implementation chain / 实现链路: `7ed55a6`, `8cca48a`, `ff69396`, `ff303e3`, `6a5f273`, `6e4bcd3`, `b2470ca`, `640c72c`.
- Phase 7B now has separate content-monitoring and performance-dashboard workspaces. Authorized campaign users can register video links, import CSV/XLSX with explicit header mapping, search/filter rows, and maintain manual performance data. / Phase 7B 现有独立的内容监控和效果看板工作区。已授权活动用户可登记视频链接、使用显式表头映射导入 CSV/XLSX、搜索/筛选内容行，并维护手工表现数据。
- The v1 model persists platform, creator, tags, video URLs, reach/play/engagement/click, cost, revenue, and confirmation state. The server computes engagement rate, CPM, CPC, ROI, and ROAS; unconfirmed commercial values never enter project finance KPIs. / v1 持久化平台、达人、标签、视频链接、曝光/播放/互动/点击、花费、收入和确认状态；服务端计算互动率、CPM、CPC、ROI 和 ROAS；未确认的商业数值不会进入项目财务 KPI。
- Campaign access and server-side capability checks protect import, manual inputs, and dashboard reads. This slice does not fabricate trends, third-party metrics, or AI findings. / 活动访问与服务端能力检查保护导入、手工输入和看板读取。本切片不虚构趋势、第三方指标或 AI 结论。
- `/performance-monitor` and `/performance-dashboard` are explicit public SPA paths, so a direct visit or browser refresh loads the product shell rather than returning a static-boundary 404. / 两个性能页面已成为明确的公开 SPA 路由，直接访问或刷新会加载产品壳层，不再返回静态边界 404。

## Focused Verification And Independent Review / 定向验证与独立审查

- Final performance service, route, and frontend contracts: `11/11`, including commercial correction redaction for team members and chronological `observed_at` selection. JavaScript syntax and `git diff --check` passed. / 最终性能服务、路由和前端合同 `11/11`，覆盖团队成员商业纠正原因脱敏与按 `observed_at` 的最新观测选择；语法和差异检查通过。
- The guarded candidate completed trusted-source checks, isolated migration rehearsal, real Express replay `8/8`, release gate `21/21`, and deployment browser smoke `2/2`. / 受控候选完成可信源、隔离迁移演练、真实 Express 重放 `8/8`、发布守卫 `21/21` 和部署浏览器烟测 `2/2`。
- The primary feature review and final remediation re-review both returned `APPROVE / CLEAR`. / 主功能审查与最终修复复审均为 `APPROVE / CLEAR`。

## Production Evidence / 生产证据

- Recoverable backup / 可恢复备份: `/root/turingmarket/backups/v060-crm-sales-workspace-20260902-094609`.
- Backup `SHA256SUMS` was verified inside the backup directory; manifest SHA-256: `c1c8ed0ad8be6693471d20e29575b3035222941e087be331401bb4794686e193`. / 已在备份目录内验证 `SHA256SUMS`；清单 SHA-256 如上。
- Public `/api/health` returned `ok` with parser ready. Public `/performance-monitor` and `/performance-dashboard` returned `200`; PM2 and Nginx are online and the deployment lock is cleared. Read-only SQLite verification returned `quick_check=ok`, zero foreign-key violations, schema `10`, and a passing knowledge FTS projection check. / 公网健康为 `ok` 且 Parser ready；两个性能页面均为 `200`，PM2/Nginx online 且发布锁已清理；只读 SQLite 校验为 `quick_check=ok`、外键异常 `0`、schema `10`，知识库 FTS 投影校验通过。
- A verified SQLite incident snapshot was retained. Only redundant remote release backups and inactive deploy locks were archived; no customer, campaign, or knowledge business records were deleted or rolled back. / 已保留可验证的 SQLite 事故快照；仅归档过量远端发布备份和失效部署锁，未删除或回退客户、活动、知识库业务记录。
- Production acceptance did not create campaign, video, or commercial records. / 生产验收未创建活动、视频或商业数据。

## Next Boundary / 后续边界

- Normal slices now follow: usable feature -> affected-scope verification -> one independent review -> verifiable backup -> same-cycle production deployment -> online smoke. Shared infrastructure, migrations, security/permission changes, and systemd changes retain the full guarded release path. / 普通功能现按“可用功能 -> 受影响范围验证 -> 一次独立审查 -> 可验证备份 -> 当轮生产上线 -> 线上冒烟”推进；共享基础设施、迁移、安全/权限与 systemd 变更仍使用完整受控发布路径。
- The next slice should define provider polling/manual update batches and Feishu projection before AI content analysis and customer-facing review reports. / 下一切片应先定义 provider 轮询/手工更新批次与飞书投影，再进入 AI 内容分析和面向客户的复盘报告。
