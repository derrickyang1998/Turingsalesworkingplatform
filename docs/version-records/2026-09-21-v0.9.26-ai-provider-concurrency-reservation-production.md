# v0.9.26 AI Provider Concurrency Reservation Production Record / AI Provider 持久并发预留生产记录

Date / 日期：2026-09-21  
Status / 状态：Production deployed and independently reviewed / 已部署生产并通过独立审查

## Release Identity / 版本身份

- Version / 版本：`v0.9.26-ai-provider-concurrency-reservation`
- Branch / 分支：`codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
- Accepted production source / 生产接纳源码：`43bb85b846b50c010f5c14a388950319e0ce59d6`
- Production run / 生产运行：`fe52e805ed4b445db80da1a5b2a87c49`
- Verified release backup / 已校验发布备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260921-085332`
- Schema / 数据库结构：`v30`

## Delivery And Scope / 交付与范围

- Schema v30 adds append-only organization concurrency policies, durable provider reservations, and append-only lifecycle events. The default limit is `10`, `0` disables new AI work, and `64` is the hard maximum. / schema v30 新增组织级仅追加并发策略、持久 Provider 预留和仅追加生命周期事件；默认上限为 10，0 停止新 AI 工作，64 为硬上限。
- One logical AI operation consumes one slot. Sequential Tavily and DeepSeek calls share it; completed idempotent replay uses no slot. Reservations survive process restarts and use a 120-second deadline, a 15-second grace period, and random fencing ownership. / 一次逻辑 AI 操作占用一个槽位；顺序执行的 Tavily 与 DeepSeek 共享预留，已完成幂等重放不占槽位。预留可跨进程重启，并由 120 秒期限、15 秒宽限和随机 fencing 所有权保护。
- Brand enrichment, strategy, demand/proposal/PPT-outline AI, chat, and performance analysis now acquire a permit before RAG, web search, provider dispatch, or business-result writes. A full organization receives `429 AI_ORGANIZATION_CONCURRENCY_LIMIT_REACHED` with no business, Token, or web-cache writes. / 品牌增强、策略、需求/方案/PPT 大纲、对话和效果分析均在 RAG、联网、Provider 或业务结果写入前取得许可；组织满载时返回类型化 429 且无业务、Token 或联网缓存写入。
- Member read and platform-admin versioned update APIs are live, with a compact Admin organization-directory editor. Administrators do not bypass the limit. Monthly Token quota remains independent. / 成员读取、平台管理员版本化更新 API 及管理控制室紧凑编辑器均已上线；管理员不绕过并发上限，月度 Token 配额继续独立执行。
- Queues, priority, force release, billing, provider pricing, Token pre-charge, and local PPTX render concurrency are excluded. The accepted CRM, M3/M4, knowledge, proposal/report, and frozen PPT experience remain intact. / 本版不含队列、优先级、强制释放、账单、Provider 定价、Token 预扣或本地 PPTX 渲染并发；既有业务界面保持不变。

## Focused Verification And Independent Review / 定向验证与独立审查

- The affected matrix passed `103/103`; migration exactness passed `23/23`; migration replay passed `8/8`; trusted-source targets passed `3/3`; release inventory passed `5/5`. Sanitized migration checks passed 188 with 10 platform-specific skips and zero failures; candidate Chromium smoke passed `3/3`. / 受影响矩阵、迁移精确性、重放、可信源码、发布清单、脱敏迁移及候选浏览器冒烟均通过。
- Schema, central provider admission, and shared release-control changes triggered bounded migration, replay, integrity, fail-closed, and reversible-production gates. Unrelated CRM, influencer, Feishu, and broad business suites were not run. / 因涉及 schema、中央 Provider 准入和共享发布控制，保留有界迁移、重放、完整性、失败关闭及可逆生产门禁；未运行无关 CRM、网红、飞书和广域业务套件。
- Candidate checks found trusted-entry ordering, a stale schema assertion, two omitted candidate test dependencies, and missing production runtime bindings for the acceptance process. Each issue failed closed and was minimally fixed before release. One post-mutation attempt rolled back cleanly before the accepted run. / 候选检查发现可信入口排序、旧 schema 断言、两个候选测试依赖遗漏及生产验收运行环境缺失；均在接纳前失败关闭并最小修复，一次已变更尝试在最终接纳前完成干净回滚。
- Independent final review verdict was `APPROVE` with no remaining P0-P2. / 独立终审结论为批准，无剩余 P0-P2。

## Deployment Record / 部署记录

- Source SHA-256 / 源码摘要：`7817fe422d3d7d96b4ffbb2f6bd72d50f3f434478618d3b9b5c703a8e5dbafd2`。
- Candidate tree SHA-256 / 候选树摘要：`d6afc607a7f5afe2ba28d57f970793e36f318603ea87f5537b669b64e2e61e26`。
- Acceptance facts SHA-256 / 验收事实摘要：`0c2aa15f6bc2e0eed58bee5eb74d72bfc6705235173495912d68def36745423c`。
- Replay evidence SHA-256 / 重放证据摘要：`2e109fa99b25077fbf34d7c744fbc81ca550cff1d4acb1f39755d2c43d17e0c8`。
- Parser evidence SHA-256 / 解析器证据摘要：`f92f2974e034e84f9b2ec277a30c73e28aa7a61fd026042ec08d2ba4ed16678e`。
- Parser runtime SHA-256 / 解析器运行时摘要：`24905b29e26fe68791a32e931d92322a2ee87df0ae08baded7dec70874455cba`。
- Nginx SHA-256 / Nginx 摘要：`fba808bdbdb53b54aac9bf9cc98f9a8bd4bf637aca400a8028e0a8d433d3d220`。
- Accepted run returned `DEPLOY_OK`, `RETENTION_CLEANUP_OK`, `PUBLIC_TRAFFIC_RESTORED`, `FINAL_ACCEPTANCE_FACTS_OK`, and `PUBLIC_RELEASE_GUARD_VERIFIED`. / 正式运行返回部署、保留清理、公网恢复、最终验收事实及公网发布守卫全部通过。

## Backup And Production Acceptance / 备份与生产验收

- Release-backup manifest SHA-256 is `c13b9c0657a839c554fdce2f35995c9f79124ef5670bee7ee16949bce4002751`; database SHA-256 is `51c879028fc96eeb2d3e6149f1a0e73d780827a3423ce74eedd1cfb2c060dd06`. / 发布备份清单和数据库摘要已复核。
- Public health returned `200` with parser ready; schema is v30; SQLite `quick_check=ok`; foreign-key violations are zero; PM2 is online with restart count zero; Nginx is active. App SHA-256 is `42a2295fc08332e7590c366d09b330306b3f143e0764b6225cf59c995dd74b53`; frozen PPT SHA-256 is `1fc70495e7ce641dadc76d751a49eab6ed261640293d2b8e691cea8bd78a6821`. / 公网健康、数据库、PM2、Nginx、应用及冻结 PPT 摘要均正常。
- Production acceptance temporarily set organization 1 to limit 1, held one permit, and proved a second authenticated `/api/ai/chat` request returned the typed 429 before any business or Token write. A controlled lifecycle recorded exactly `acquired`, `dispatch_authorized`, `provider_completed`, and `released`, then restored limit 10 with zero active reservations. / 线上验收临时设置上限 1 并占用一个许可，第二个认证请求在任何业务或 Token 写入前返回类型化 429；受控生命周期精确记录四个事件，最终恢复上限 10 且活动预留为 0。
- Root-only evidence is `/root/turingmarket/deployment-evidence/ai-concurrency-acceptance-fe52e805ed4b445db80da1a5b2a87c49.json`, SHA-256 `e1bfb38ddc7e7418daf585c13cbee2c5c366c976700ba9f834fef1b64060fed8`, owner root, mode `0600`. It contains no reservation identifier or fencing value. / 并发验收证据仅 root 可读，且不含预留标识或 fencing 值。

## Administrator Access Follow-up / 管理员访问复核

- Post-release login smoke detected that the previously user-designated `derrick` credential hash was not retained. Before correction, a protected SQLite backup was created at `/root/turingmarket/backups/credential-rotation-20260921-010735`, SHA-256 `418a967d9fe78ae7d96e7a7ada319657ddbc6791c8ee3c3bfe4c53d5d9f6c173`. / 发布后登录冒烟发现此前指定的 `derrick` 凭据哈希未保留；修复前已创建受保护 SQLite 备份并记录摘要。
- The requested credential was restored through an audited operator override, all existing sessions were revoked, and login, `/api/auth/me`, logout, final session count zero, database integrity, and audit presence were verified. The value is not stored in this record. / 已通过受审计运维覆盖恢复用户指定凭据、撤销旧会话，并验证登录、身份、退出、零最终会话、数据库完整性及审计记录；本记录不保存凭据值。
- The designated legacy value is outside the current 12-character-plus-symbol self-service policy. Future rotation should use a policy-compliant value so the supported rotation workflow can be used without an operator override. / 该既定旧值不满足当前至少 12 位且含符号的自助策略；后续轮换应使用合规值，以便通过标准受支持流程完成。

## Delivery Cadence And Next Boundary / 发布节奏与下一边界

- Each completed function receives affected tests, only its necessary risk gates, one independent review, a verified rollback point, immediate production deployment, and focused online smoke. Heavy suites remain phase-closeout or risk-triggered. / 每个完成功能仅执行受影响测试、必要风险门禁、一次独立审查、可验证回滚点、立即生产部署和线上定向冒烟；重型套件只在阶段收口或风险触发时运行。
- The next independent Phase 8 release is billing, followed by Phase 8 closeout and the separately approved dynamic-catalog boundary. / 下一独立第 8 阶段版本为账单，其后进行阶段收口及另行批准的动态目录边界。

No password, session token, provider key, cookie, reservation identity, or fencing token appears in this record. / 本记录不含密码、会话令牌、Provider 密钥、Cookie、预留标识或 fencing 值。
