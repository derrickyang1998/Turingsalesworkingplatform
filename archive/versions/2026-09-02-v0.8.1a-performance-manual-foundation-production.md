# v0.8.1a Phase 7B 内容监控与效果看板基础生产归档

- 实现提交：`7ed55a6`；部署链修复：`8cca48a`、`ff69396`、`ff303e3`、`6a5f273`、`6e4bcd3`；直接路由修复：`b2470ca`；生产隐私与观测时序修复：`640c72c`。
- 活动范围内可登记视频链接、导入 CSV/XLSX、明确映射表头、按字段搜索/筛选内容，并在独立的“内容监控”和“效果看板”页面处理数据。
- v1 持久化平台、达人、标签、内容链接、展示/播放/互动/点击、花费、收入和确认状态；服务端计算互动率、CPM、CPC、ROI、ROAS。未确认商业数据不进入项目财务 KPI，商业字段仅授权用户可维护。
- 导入、手工输入和看板读取均受活动访问和服务端权限约束；本切片不虚构趋势、第三方抓取数据或 AI 判断。
- 直接访问和刷新 `/performance-monitor`、`/performance-dashboard` 均为 `200`，不会再被公共静态边界拒绝。
- 最终性能服务/路由/前端合同 `11/11`，覆盖团队成员商业纠正原因脱敏与迟到回填按 `observed_at` 取最新观测；真实 Express 重放 `8/8`、发布守卫 `21/21`、部署浏览器烟测 `2/2` 通过。
- 主功能审查和修复后的独立复审均为 `APPROVE / CLEAR`。
- 最终备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260902-094609`；`SHA256SUMS` 已验证，清单 SHA-256：`c1c8ed0ad8be6693471d20e29575b3035222941e087be331401bb4794686e193`。
- 线上验收：公网健康 `ok`、Parser ready、两个性能页面均为 `200`、PM2/Nginx online、发布锁清理；SQLite `quick_check=ok`、外键异常 `0`、schema `v10`、知识库 FTS 投影校验通过。未在真实活动中创建视频或商业数据。
- 发布恢复：保留可校验 SQLite 事故快照；仅归档过量远端发布备份和失效部署锁，未清理或回退客户、项目、知识库业务数据。
- 后续普通功能均按“完成切片 -> 定向验证 -> 独立审查 -> 备份 -> 当轮上线 -> 线上冒烟”推进；迁移、权限/安全、systemd 与跨模块基础设施继续使用完整发布门禁。下一切片先补齐视频数据 provider/手工更新批次与飞书投影合同，再以可核验事实生成 AI 内容洞察和客户项目复盘。
