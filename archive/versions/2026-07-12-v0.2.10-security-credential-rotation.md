# v0.2.10 Security Credential Rotation / 安全凭据轮换

- Date / 日期: 2026-07-12
- Branch / 分支: `codex/ai-knowledge-foundation`
- Scope / 范围: credential rotation, session/JWT invalidation, deployment hardening, and incident closure / 凭据轮换、会话与 JWT 失效、部署加固和事件闭环

## Result / 结果

- 11/11 active user passwords rotated atomically; old administrator password rejected. / 11/11 活动账号原子轮换，旧管理员密码已失效。
- JWT signing secret rotated; default administrator password removed; sessions remaining 0. / JWT 密钥已轮换，默认管理员密码已移除，会话剩余 0。
- Three plaintext environment backups removed; no `.env` entered the general release backup. / 清理 3 份明文环境备份，常规备份不含 `.env`。
- DeepSeek and Tavily preserved after evidence classification and passed live smoke; Feishu remains unconfigured. / DeepSeek/Tavily 经证据分类后保留并通过线上冒烟；飞书未配置。

## Verification / 验证

- Local tests: 66/66; production tests: 66/66; dependency vulnerabilities: 0.
- Production smoke passed for new administrator/team credentials, auth, influencer template, DeepSeek, Tavily, AI audit, PPT markers, logout, and zero sessions.
- Sensitive public paths: 7/7 returned 404.
- Critical production file hashes: 13/13 matched local.
- Latest PPT bridge remained unchanged.
- Final minimal-change, code, and application-security reviews all approved; release gate opened.

## Private Evidence / 私有证据

Credentials and exact protected deployment coordinates are retained only under the ACL-protected local `99-private` directory. This archive contains no secret values. / 凭据和精确受保护部署坐标仅保存在 ACL 受限的本机私有目录，本归档不包含敏感值。

Non-blocking follow-up: harden or remove the unused `server_full.js` legacy entrypoint. / 非阻断后续：加固或移除未启用的旧入口 `server_full.js`。
