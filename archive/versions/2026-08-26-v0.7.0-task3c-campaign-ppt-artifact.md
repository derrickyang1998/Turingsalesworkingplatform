# v0.7.0 Task 3C Campaign PPT Artifact / Campaign PPT 成品归档

Date / 日期: 2026-08-26
Status / 状态: Production incremental slice / 生产增量切片已发布
Branch / 分支: `codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
Implementation commits / 实现提交: `3988c47`, `899be8a`

## Delivery / 交付

- The latest M3 PPT download path submits the active Campaign, an explicitly Campaign-matched demand record, and the exact human-confirmed proposal version to the existing `/api/proposal/generate-ppt` endpoint. / 最新 M3 PPT 下载链路把当前 Campaign、明确匹配该 Campaign 的需求记录和人工确认后的精确方案版本提交给既有 PPT 接口。
- The returned PPTX is treated as an immutable artifact. Only after a verified binary response is received does the client retain the Campaign `ppt` link and completed knowledge-archive state. / PPTX 被视为不可变成品；仅在收到已验证二进制响应后，客户端才保留 Campaign 的 `ppt` 关联和完成态知识归档状态。
- The client uses a stable business-input idempotency key and a fresh request ID. Duplicate downloads are single-flight and replay byte-identical output. / 客户端使用稳定业务幂等键和每次新请求 ID；重复下载保持单飞并重放字节完全一致的成品。
- Campaign, actor, confirmed-editor content, and request-generation fences prevent stale responses from mutating a newer context. A demand ID is reused only with explicit same-Campaign provenance. / Campaign、用户、已确认编辑内容和请求代次共同隔离旧响应；需求 ID 只有在具备同 Campaign 明确来源时才复用。
- Archive success, failure, and retry state remains attached to the current editor session. CSP-translated controls are supported, while the exact unlinked legacy path remains unchanged. / 归档成功、失败和重试状态保留在当前编辑会话；兼容 CSP 翻译控件，未关联旧版路径保持原行为。
- Frozen `platform/ppt.js` and all backend service files remain byte-for-byte unchanged. The only deployed runtime file is `platform/app.js`. / 冻结 PPT 与全部后端服务文件均未改变；本切片唯一发布的运行文件为 `platform/app.js`。

## Review And Focused Verification / 审查与定向验证

- Initial independent review returned two Important findings and one Minor finding: a same-Campaign request race, demand reuse without explicit Campaign match, and CSP-translated download-button lookup. / 首轮独立审查发现 2 项 Important 和 1 项 Minor：同 Campaign 请求竞态、缺少明确 Campaign 匹配的需求复用，以及 CSP 翻译后下载按钮定位问题。
- Commit `899be8a` closed all three findings with deterministic RED/GREEN contracts. The same reviewer re-reviewed the final diff and returned `APPROVE` with no Critical, Important, or Minor findings. / `899be8a` 以确定性 RED/GREEN 合同关闭全部问题；同一审查者复审最终差异并给出 `APPROVE`，无剩余 Critical、Important 或 Minor。
- Final focused matrix: 75 passed, 0 failed, covering the new Campaign artifact contract, Campaign PPT RAG, PPT service/idempotency, Phase 4 integration, and frontend architecture inventory. / 最终定向矩阵 75/75 通过，覆盖 Campaign 成品合同、Campaign PPT RAG、PPT 服务与幂等、阶段 4 集成和前端架构清单。
- `node --check`, frozen-PPT SHA verification, `git diff --check`, and focused secret scanning passed. Browser automation was not run under the approved incremental cadence. / JavaScript 语法、冻结 PPT 哈希、差异和聚焦敏感信息检查通过；按已批准的增量节奏未执行浏览器自动化。

## Scope / 范围

- Runtime file / 运行文件: `platform/app.js`.
- Focused contract / 定向合同: `platform/server/tests/latest_ui_ppt_campaign_artifact.test.js` plus affected Campaign PPT and integration suites / 及受影响的 Campaign PPT 与集成套件。
- Database migration / 数据库迁移: none / 无。
- Production App build marker / 生产 App build 标识: unchanged `20260811-v060-crm-sales-workspace`.
- Frozen PPT build / 冻结 PPT build: unchanged `20260702-v916-kb-bridge-client-cn`.
- Frozen PPT SHA-256 / 冻结 PPT 哈希: `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`.

## Production Deployment / 生产部署

- Release ID / 发布编号: `v070-slice3c-campaign-ppt-artifact-20260826-130804`.
- Verified backup / 已验证备份: `/root/turingmarket/backups/v070-slice3c-campaign-ppt-artifact-20260826-130804`.
- Backup aggregate SHA-256 / 备份聚合哈希: `bcffa0ae7958e66d75b645829ae0ef607128c6f267c89abe473ffcef9477001a`.
- The backup contains the pre-release `app.js`, a SQLite Backup API snapshot, PPT-cache snapshot, per-domain manifests, and an aggregate manifest; all checksum checks and backup `quick_check` passed. / 备份包含发布前 `app.js`、SQLite Backup API 快照、PPT 缓存快照、分域清单与聚合清单，全部校验和及备份库完整性通过。
- Two deployment preparations stopped before mutation because their temporary verification commands used an incorrect dependency path and an unsupported `.next` extension. No production byte changed; the third preparation used verified `.js` staging files and completed with automatic app-file rollback armed. / 两次发布准备分别因临时验证命令使用错误依赖路径和不受支持的 `.next` 扩展而在生产变更前安全停止；线上字节未改变，第三次使用已验证的 `.js` 暂存文件并在启用应用文件自动回滚的情况下完成发布。
- Production `app.js` SHA-256 / 生产 `app.js` 哈希: `9a985f05759ba8f8acb28397601b5a31ccdb21d363f80f0463666c66a360163a`.
- PM2 PID after release / 发布后 PM2 PID: `1398738`; Nginx active / 正常。

## Online Acceptance / 线上验收

- Public `/api/health` returned 200 with `status=ok`; parser `ready=true` with manifest `44db310046efe65bd68c110313b4887995c73e276e7d58f65fe037c09a973c5b`; the public `app.js` bytes match the deployed SHA-256. / 公网健康、解析器及静态字节哈希均通过。
- Administrator login, `/api/auth/me`, active Campaign read, linked demand, exact confirmed proposal, PPTX generation, same-key replay, and logout passed. / 管理员登录、身份读取、Campaign 读取、需求关联、精确确认方案、PPTX 生成、同键重放与注销通过。
- Acceptance records / 验收记录: Campaign `1`, demand `1`, proposal `29`; proposal-content SHA-256 `dd5e5d4d8a6c3edb82fe3c3ba083a1fc3fda5d7e38e3f27a8fbe46800c576ab5`.
- Generated PPTX / 生成成品: 29,460 bytes; SHA-256 `800b35c07401cf098c343b5722d7f9c1f03bd1c099032a3f2edfc0b0f67ca026`; same-key replay returned identical bytes. / 同键重放返回完全相同字节。
- Read-only database evidence found exactly one active demand link, proposal link, PPT link, `campaign_ppt` archive, `ppt_link/link_attached` event, and completed binary idempotency ledger. The ledger records HTTP 200, the exact PPTX MIME type, bytes, and SHA-256. / 只读数据库证据显示需求、方案、PPT 关联、成品知识归档、关联事件及完成态二进制幂等账本均各 1 条，且账本字节、MIME 与哈希精确一致。
- SQLite `quick_check=ok` and recent loopback smoke sessions are zero after logout. / SQLite 完整性通过，注销后近期回环验收会话为 0。

## Delivery Cadence / 发布节奏

This slice follows the approved per-feature release policy: affected tests, one independent approval, verified backup, immediate production deployment, and online health/login/feature smoke. Full regression remains reserved for Phase 6 closeout or broad-risk changes. / 本切片遵循已批准的单功能发布策略：受影响测试、一次独立批准、可验证备份、立即上线和线上健康/登录/功能冒烟；完整回归保留到阶段 6 收口或高风险变更。

## Next Slice / 下一切片

The next Phase 6 slice will close the self-growing AI-chat and knowledge-governance path: permission-filtered retrieval, controlled Tavily web augmentation, durable citations and run audit, high-value summary promotion, and administrator-wide conversation review without changing the approved product shell. / 下一切片将收口自成长 AI 对话与知识治理链路：权限过滤检索、受控 Tavily 联网、持久引用与运行审计、高价值摘要晋升，以及管理员全量对话审计，同时不替换已批准产品壳层。
