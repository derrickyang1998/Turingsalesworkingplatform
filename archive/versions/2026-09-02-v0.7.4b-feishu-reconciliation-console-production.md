# v0.7.4b M4 飞书回执核对操作台生产归档

- 实现提交：`7b65140`。
- M4 当前飞书面板会读取所选活动的待核对 Bitable Outbox 批次；活动 Owner 或组织管理员可选择批次、逐行录入已核验且唯一的 Record ID，并通过既有受保护接口完成对账。普通协作者只看到权限说明，不能提交确认。
- 客户端会在请求前校验 Record ID 数量、唯一性和长度；服务端仍负责 Owner/组织管理员授权、待核对状态终态化、远端 ID 唯一和终态不可变。
- 单飞锁按“活动 + 批次”保留，跨活动切换或手动刷新失败不会产生第二个确认请求。遇到其他授权操作员先完成导致的 `409` 时，客户端会刷新最新回执，不保留陈旧待核对状态。
- 不包含自动重发、更新、补偿或真实飞书写入；CSV 兜底、Webhook、受控 Bitable 创建与持久化 Outbox 保持兼容。
- 定向 M4/飞书/Outbox 矩阵 `18/18`，新增操作台客户端 `8/8`，两轮独立审查均为 `APPROVE`；语法、页面标记、差异检查和本地发布预检通过。
- 受控候选通过隔离 Express 重放 `8/8`、发布门禁 `21/21`、部署浏览器冒烟 `2/2` 并完成生产切换。备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260902-030830`；备份目录内 `SHA256SUMS` 验证通过，清单 SHA-256：`4710320e2c37f722aa93173e6fd68691779b5b43927a834c36726a1006f05421`。
- 线上验收：公网/回环健康 `ok`、Parser ready、`/m4` 为 `200`、公网 `app.js` 已有对账处理器和跨活动锁、PM2/Nginx online，SQLite `quick_check=ok`、外键异常 `0`、schema `v8`。
- 本轮没有用真实飞书 Bitable 记录进行写入、更新、补发或对账。后续独立设计显式更新/重试/补偿合同，不做静默重发；随后进入 Phase 7B 内容效果看板、飞书同步和 AI 复盘。
