# v0.7.3 飞书多维表格批量写入生产归档

- 提交：`06c7dd1`。
- 已选网红可在服务器端显式开启 Bitable 写入时批量创建；默认关闭，未配置或关闭时保持 CSV 兜底，Webhook 不受影响。
- 每次写入要求裸 UUID 幂等键并传给飞书 `client_token`；服务端重新读取网红数据，完整 20 列远端表头必须存在。联系人列默认不发送，只有独立开关开启时才传值。
- 严格校验飞书数值成功码、500 条上限、返回数量和远端记录 ID。v0.7.3 不包含远端 ID 持久化、Outbox、自动重试、更新或对账，后续拆分为 v0.7.4。
- 定向验证：provider `13/13`、客户端重放 `1/1`、同步路由 `2/2`、飞书路由 `3/3`；语法、diff 和本地部署预检通过。独立审查整改两项阻断后为 `APPROVE`。
- 生产备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260901-224913`；`SHA256SUMS` 已验证，清单 SHA-256 为 `acdae3dea97776e4ec2a0f3928db144a10267875051429b31f9a55f136c38ea0`。
- 线上验收：公网/回环健康 `ok`、Parser ready、`/m4` 与 `/app.js` 为 `200`、PM2/Nginx online。飞书仍为未配置安全态，未执行真实外部写入。
