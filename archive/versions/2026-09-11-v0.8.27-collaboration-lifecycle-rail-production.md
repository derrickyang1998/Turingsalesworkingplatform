# v0.8.27 Collaboration Lifecycle Rail Production Record / 合作履约生命周期轨道生产记录

## Release / 发布

- Date / 日期: `2026-09-11`
- Scope / 范围: compact lifecycle visibility inside the existing M4 collaboration workspace / 既有 M4 合作工作区内的紧凑履约可视化
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Implementation / 实现: `ce72e22`
- Production run / 生产运行: `c1c508a9f7d34282a95d13dc8c0ef6a1`
- Candidate / 候选: `v060-crm-sales-workspace-20260911-000730`
- Database / 数据库: migrations `17/17`, schema `v17` unchanged / 保持不变

## Delivered / 已交付

- Added a compact seven-stage rail to each collaboration row: order, contract, execution, review, publication, tracking, and settlement. It stays inside the current M4 table and does not add a page or replace the approved product shell. / 为每条合作记录增加下单、签约、执行、审核、发布、追踪和结算七阶段紧凑轨道；保留在当前 M4 表格内，不新增页面，也不替换已批准产品壳层。
- The rail consumes only existing server projections for collaboration state, signed-contract evidence, review, publication, performance tracking, and settlement. It shows completed count and current stage without adding an API, migration, or client-side business write. / 轨道只消费既有服务端合作状态、签约凭证、审核、发布、效果追踪及结算投影，并显示完成数量与当前阶段；不新增 API、迁移或客户端业务写入。
- Cancelled collaborations stop at the first unfinished stage instead of appearing active. Legacy live collaborations do not incorrectly wait on v2 signed-contract evidence, while new v2 orders retain their hard contract gate. / 已取消合作停在首个未完成阶段，不再显示活跃；历史已执行合作不会错误等待 v2 签约凭证，新版 v2 订单仍保留合同强制门禁。
- Immutable review, publication, and tracking evidence keeps reached stages complete even when current action permissions are closed, preserving lifecycle truth across terminal states. / 即使当前操作权限关闭，不可变审核、发布和追踪证据仍会保留已到达阶段，避免终态投影抹去履约事实。

## Focused Verification And Review / 聚焦验证与审查

- The affected M4 client contract passed `22/22`; `node --check app.js`, diff formatting, and the local deployment preflight passed. No unrelated full-suite rerun was performed for this presentation-only slice. / M4 前端受影响合同 `22/22` 通过；`app.js` 语法、差异格式和本地发布预检通过。本次仅为展示层切片，未重复执行无关全量测试。
- Independent review found two release risks: cancelled rows still looked active after relationship cleanup, and legacy live orders could appear stuck at signing. Production-shaped regressions were added and both issues were fixed; final verdict was `PASS`. / 独立审查发现取消订单在关联清理后仍可能显示活跃、历史执行订单可能错误卡在签约两项风险；补充生产形态回归并修复后，最终结论为 `PASS`。
- The production candidate passed real Express replay `8/8`, release guard `21/21`, built-in browser smoke `2/2`, migration rehearsal, parser runtime reuse, Nginx checks, and final acceptance. / 生产候选通过真实 Express 回放 `8/8`、发布守卫 `21/21`、内置浏览器冒烟 `2/2`、迁移演练、解析器运行时复用、Nginx 与最终验收。

## Production Evidence / 生产证据

- Guarded candidate SHA-256: `3e2f5cfd40fbf3605e753069133a25a36189dd4e2b074af95e48819636056fb4`. Recoverable backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260911-000730`. / 受保护候选与可恢复备份均已保留。
- The initial backup manifest verified `309/309` entries with SHA-256 `a1a881d6225a5ed785f30f8487e893eefdcf613f05bde56054f008fd11d81d26`; the cutover snapshot verified `35/35` entries with SHA-256 `e40885eeddf01bdcc012aa7a940e90777ef9cb653049db3ce6d4083362fc81eb`. / 初始备份清单与切换快照均逐项复核通过。
- Public health and `app.js` returned `200`, parser readiness was true, and the public client SHA-256 matched local `e7e6e779eddd83a5f20024e4e14532cec3dc3d63aa8ce82441889a85325b7413`. The deployed client contained the lifecycle calculator, rail styles, and visible lifecycle label. / 公网健康、脚本、解析器及线上脚本标记均通过，公开脚本哈希与本地一致。
- Administrator login, `GET /api/collaborations?include_campaign_context=1`, and logout returned `200`; the production account retained the administrator role. PM2 is online with restart count `0`, Nginx is active, and schema remains v17. / 管理员登录、合作列表读取和注销均为 `200`；管理员角色保持，PM2、Nginx 与数据库版本正常。
- Frozen `ppt.js` remains SHA-256 `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 冻结方案 PPT 保持不变。

## Release Incident / 发布事件

- The first candidate completed offline validation but stopped before production mutation because available disk was below the parser safety threshold. The deployment controller removed the candidate and restored its control plane; production stayed on v0.8.26. / 首次候选完成离线验证后，因磁盘余量低于解析器安全阈值而在生产变更前停止；发布控制器清理候选并恢复控制面，线上继续运行 v0.8.26。
- The incomplete backup and only rebuildable root development caches were removed after canonical-path, ownership, mount, lock, and protected-marker checks, recovering `808,787,968` bytes. Current/last-good backups, production data, uploads, parser runtime, and credentials were not touched. / 在规范路径、归属、挂载、发布锁和保护标记核验后，仅清理未完成备份及可重建开发缓存，共回收 `808,787,968` 字节；当前/上一稳定备份、生产数据、上传文件、解析器运行时和凭据均未改动。

## Next Boundary / 下一边界

- The next recommended M4 slice is audited publication correction, pause, and resume with immutable version history. Because it introduces schema v18 and shared custody semantics, it will use the heavier migration release gate. / 下一建议 M4 切片为带审计的发布纠错、暂停与恢复，并保留不可变版本历史；该功能将引入 schema v18 和共享保管语义，因此按迁移高风险门禁执行。
- Phase 7B still needs one explicitly approved social-data provider and scheduled collection. Live Feishu writes remain separately gated on production configuration and external-write verification. / Phase 7B 仍需一个明确批准的社交数据 provider 与定时采集；真实飞书写入继续由生产配置及外部写入验收单独门禁。
