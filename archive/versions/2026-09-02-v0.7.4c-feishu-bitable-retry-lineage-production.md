# v0.7.4c M4 飞书失败批次显式重试生产归档

- 实现提交：`d2b1f17`；部署回放夹具修复：`e949e18`。
- M4 仅为已终态失败且属于已知未写入错误的 Bitable 批次提供显式重试。活动 Owner 或组织管理员必须填写重试原因；普通协作者无法发起。
- 重试以原始不可变快照创建新的子投递回执，原失败回执、重试人、原因和子回执以只追加血缘关联；不修改历史回执、不自动重试。
- 待核对子回执进入人工核对；配置不可用或 Provider 再次失败会刷新 M4 回执，原失败批次不再显示为可重试。相同 UUID 只有在原因一致时才重放，不同原因明确冲突。
- schema v9 已增加不可变重试血缘，可信迁移与部署合同支持 v9 no-op 验证，后续发布/恢复不会拒绝当前生产结构。
- 定向 M4/飞书/Outbox/发布矩阵 `55/55`、迁移验证 `15/15`、可信来源 `32/32`、真实回放 `8/8`、发布门禁 `21/21`、部署浏览器烟测 `2/2` 均通过；两次独立复审均为 `CLEAR`。
- 首个候选因旧 v1-v8 回放夹具在生产变更前被安全拦截；修正为 v1-v9 后通过完整候选与生产切换。
- 备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260902-045118`；备份目录 `SHA256SUMS` 已验证，清单 SHA-256：`d6f25d8724419a7f5d66ba286f2231cdfa7cdb685c97a944b670b0e81e3a5b8e`。
- 线上验收：公网/回环健康 `ok`、Parser ready、`/m4` 为 `200`、PM2/Nginx online，SQLite `quick_check=ok`、外键异常 `0`、schema `v9`。未用真实飞书 Bitable 记录执行写入、更新、补发或核对。
- 后续：真实外部投递需经批准测试活动；下一产品切片进入 Phase 7B 内容监控、效果看板、飞书同步与 AI 复盘。
