# v0.8.9 Customer Report PPT Production Archive / 客户复盘 PPT 生产归档

**Date / 日期:** 2026-09-07  
**Status / 状态:** Deployed and verified / 已部署并验证  
**Feature / 功能:** Phase 7B.3e customer-safe retained PPT delivery  
**Commits / 提交:** `b76f018`, `51d4686`, `2983531`

## Scope / 范围

- 活动负责人或组织管理员可从效果看板为任一已封存 `customer_safe_v1` 报告生成/下载客户版 PPT；只读成员不可操作。
- 每个快照只保留一份 `customer-report-ppt-v1` 成品，重复请求返回同一身份和字节；后续指标变化不会改写旧 PPT。
- Schema 升至 `v14`，新增只追加 `customer_report_ppt_artifacts`。客户报告 PPT 使用独立私有命名空间，既有方案 PPT 生成器和最新界面均未替换。
- 客户版仅呈现已脱敏七章节，不含原始链接、达人联系方式、商业敏感数据、内部 AI 提示词、飞书凭据或外部投递信息。
- 本轮未调用 DeepSeek、联网、视频 provider、飞书或定时任务，也未在生产制造真实客户业务成品。

## Verification / 验证

- 定向功能矩阵 `37/37`；存储/服务/回放 `29` 通过、`0` 失败、`2` 环境跳过；可信来源 `32/32`；发布合同 `37/37`；备份清单 `1/1`。
- 实际受控 PPT 渲染成功；语法、凭据扫描、差异格式、本地预检、v1→v14 双次迁移和 SQLite 完整性通过。
- 两次候选阻断均发生在生产切换前：补齐 schema v14 校验，并隔离客户报告成品命名空间/升级备份清单。整改后回放 `8/8`、发布门禁 `21/21`、部署浏览器烟测 `2/2`。
- 功能、安全、迁移、运行时和回滚独立审查均为 `APPROVE`。

## Production / 生产

- Backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260907-221037`
- Backup manifest SHA-256: `947df0435e58d6a16f3bf9f10bf0a980696028d0b4bb0b47a9b5092136562d86`
- Candidate tree SHA-256: `09261102a89959aad38dd2e5d4e36861cc1057eb3ffa0182cee862889001ea5d2`
- 公网健康与效果看板均为 `200`；匿名 PPT 接口为 `401`；Nginx active；PM2 online、重启计数 `0`。
- SQLite 为 schema `v14`、`quick_check=ok`、外键异常 `0`，快照表与 PPT 成品表存在。冻结 `ppt.js` 哈希保持不变。

## Residual Boundary / 剩余边界

未使用真实账号执行生产写入/下载，避免创建不可删除的业务成品。下一功能继续按“轻量定向验证、独立审查、备份、完成即上线”交付；外部写入和视频分析仍需独立授权与高风险门禁。
