# v0.8.31 Organization Methodology Promotion Production Record / 组织方法论晋升生产记录

## Release / 发布

- Date / 日期: `2026-09-11`
- Scope / 范围: two-person organization methodology promotion, immutable custody, organization-scoped RAG, and support-admin audit / 双人组织方法论晋升、不可变归属、组织级 RAG 与平台支持管理员审计
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Feature implementation / 功能实现: `b1bf061`
- Deployment guard fixes / 发布门禁修正: `949378c`, `d312561`
- Independent review / 独立审查: `APPROVE`, no remaining P0/P1/P2 / `APPROVE`，无剩余 P0/P1/P2
- Production run / 生产运行: `6dbe8da7021349aa8e2c2dffc5f1fe56`
- Candidate / 候选: `v060-crm-sales-workspace-20260911-112905`
- Database / 数据库: migrations `1-20`, schema `v20` / 迁移 `1-20`，schema `v20`

## Delivered / 已交付

- Added an additive organization-methodology section to the existing Performance Dashboard. A confirmed project AI review or authorized content-analysis conclusion can be submitted for organization reuse; a different active organization administrator with active team membership must approve or reject it. / 在现有效果数据看板中增量加入组织方法论区域。已确认的项目 AI 复盘或授权内容分析结论可申请组织复用，必须由另一位具备有效团队身份的组织管理员批准或拒绝。
- Request, decision, and organization custody ledgers are append-only. Structured product, category, platform, audience, hypothesis, and method dimensions support deterministic deduplication and explicit supersession without copying raw evidence. / 申请、决定和组织归属账本均为只追加；通过产品、品类、平台、受众、假设和方法等结构化维度实现确定性去重与显式替代，不复制原始证据。
- Approved methods are stored as confirmed organization knowledge and become available to Performance AI reviews across campaigns in the same organization. Campaign-scoped retrieval is bound to the campaign organization, including for users belonging to multiple organizations and for platform administrators. / 获批方法以已确认的组织知识保存，可供同组织不同项目的效果 AI 复盘检索。活动级检索严格绑定活动所属组织，包括同时属于多个组织的用户和平台管理员。
- A verified active platform administrator can inspect the promotion state as read-only support audit, but cannot request, approve, reject, or bypass the second-approver rule. / 经验证的有效平台管理员可只读查看晋升状态，但不能申请、批准、拒绝或绕过第二审批人规则。
- Existing Campaign methodology, authorized content analysis, customer reports, HTML/PPT output, CRM, and M4 collaboration paths remain intact. / 既有 Campaign 方法论、授权内容分析、客户报告、HTML/PPT、CRM 与 M4 协作路径保持不变。

## Focused Verification And Review / 聚焦验证与审查

- Core migration, permissions, service, routes, and frontend contracts passed `81/81`; affected knowledge and linked AI RAG tests passed `48/48`; the production predecessor migration, sanitizer profile, and trusted-source checks passed `5/5`; production replay passed `8/8`; release contract passed `38/38`. / 核心迁移、权限、服务、路由和前端契约 `81/81`，受影响知识库与 AI RAG `48/48`，生产前序迁移、脱敏配置与可信源检查 `5/5`，生产回放 `8/8`，发布合同 `38/38` 均通过。
- JavaScript syntax, diff formatting, live-style Tavily credential scanning, trusted-manifest checksums, and local deployment preflight passed. The frozen proposal PPT was not changed. / JavaScript 语法、差异格式、Tavily 真实密钥形态扫描、可信清单摘要及本地发布预检通过；冻结方案 PPT 未修改。
- Independent review found two P1 authorization gaps: cross-organization retrieval for multi-organization users and missing read-only audit for a bare platform support administrator. Both received regression tests and fixes; re-review returned `APPROVE`. / 独立审查发现两个 P1 权限缺口：多组织用户的跨组织检索，以及纯平台支持管理员缺少只读审计。两项均补充回归并修复，复审为 `APPROVE`。
- Two candidates stopped before production mutation because old deployment assertions still expected schema v19. Both controls restored cleanly. The final candidate passed the same replay and migration gates with v20. / 两个候选因发布断言仍固定期待 v19 而在生产变更前停止，控制面均完整恢复；最终候选以 v20 通过相同回放与迁移门禁。

## Production Evidence / 生产证据

- Accepted candidate SHA-256: `516dfd0d91269eb97b376c13eee52cf2852a4b254ed958604c83c50aacc13090e`; accepted source SHA-256: `d22b6ab343f41ab0fbee097d15acd46a36ce1ca307d3f14a4efd0305c2019d4a`. / 已验收候选与可信发布源摘要已固定记录。
- Recoverable backup: `/root/turingmarket/backups/v060-crm-sales-workspace-20260911-112905`. The initial manifest verified `318` entries with SHA-256 `a6ebdbcc88e52e63845bd9d06cfe7b309b588f1b86dbba52023c9f3b37f02f33`; the cutover snapshot verified `35` entries with SHA-256 `1ebed9298fe0dde346d765afdd65845705493693427d6280cfc83f5ea7e66766`. / 可恢复备份、初始清单和切换快照均逐项复验通过。
- Public health, index, app script, and component styles returned `200`; the new anonymous endpoint returned `401`. Administrator login, identity, campaign list, Performance Dashboard, organization-methodology list, global AI-conversation audit, logout, and revoked-token rejection passed. / 公网健康、首页、应用脚本及组件样式为 `200`，新接口匿名访问为 `401`；管理员登录、身份、活动列表、效果看板、组织方法论列表、全量 AI 对话审计、注销及撤销令牌拒绝均通过。
- Production assets match local SHA-256. PM2 is `online` with zero restarts, Nginx is `active`, schema is `v20`, SQLite `quick_check=ok`, foreign-key violations are `0`, and all three organization-methodology tables exist. / 生产资源与本地 SHA-256 一致；PM2 `online` 且重启数为零，Nginx `active`，schema 为 `v20`，SQLite `quick_check=ok`、外键异常 `0`，三张组织方法论表均存在。

## Cadence And Next Slice / 节奏与下一切片

- Normal feature slices now use affected tests, necessary syntax/contracts and secret checks, one independent review, a verified backup, same-cycle production deployment, and online feature smoke. Heavy regression remains risk-triggered for migrations, authentication/authorization, shared infrastructure, external writes, broad changes, and phase closeout. / 普通功能按受影响测试、必要语法/合同与密钥检查、一次独立审查、可验证备份、当轮生产部署和线上功能冒烟执行；迁移、鉴权、共享基础设施、外部写入、大范围改动和阶段收口仍按风险触发较完整验证。
- The next independent slice should continue the approved roadmap without batching unrelated features into this release. / 下一独立切片继续按已批准路线图推进，不把无关功能合并进本版本。

