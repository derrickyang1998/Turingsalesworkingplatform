# v0.7.2 M4 合作资源下单合同生产归档

- 提交：`01c202e`、`0fe9990`、`002e75a`。
- M4 下单启用 `turingmarket.collaboration-order.v1`：项目、产品、合作类型、资源/交付、整数报价、合同/PO 分列；创建后报价锁定，拒绝与 `proposal_notes` 双写。
- 历史无 schema 或 `legacy.v0` 资源、备注与报价回退保持兼容；关联活动的 v1 合同会按规范数据归档并支持幂等重放。
- 定向验证：资源合同 `7/7`、活动协作 `22/22`、网红工作流 `32/32`、M4 客户端 `5/5`、前端基线 `28/28`；语法、diff、本地部署预检及两项部署合同均通过。三项独立审查均为 `APPROVE`。
- 生产备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260901-210048`；`SHA256SUMS` 已验证，清单 SHA-256 `a0b5e8b48470f15a157d0d7ff626d8d32db69a7dec5fae1c8487444c70f0f894`。
- 线上验收：公网/回环健康 `ok`、Parser ready、`/m4` 为 `200`、PM2/Nginx online、SQLite `quick_check=ok`、外键异常 `0`。为避免污染真实记录，未执行 M4 生产写入烟测。
- 上线中两次候选均在生产变更前安全中止，最终仅补全文件清单并将隔离解析器冷缓存上限从 30 分钟调为 60 分钟；后续缓存复用/预热另行审查实施。
