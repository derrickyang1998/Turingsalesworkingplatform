# v0.9.18 Knowledge Tenant Isolation Production Record / 知识库租户隔离生产记录

Date / 日期：2026-09-19
Status / 状态：Production deployed and independently accepted / 已部署生产并通过独立验收

## Release Identity / 版本身份

- Version / 版本：`v0.9.18-knowledge-tenant-isolation`
- Branch / 分支：`codex/v0.7.0-ai-knowledge-proposal-ppt-loop-production`
- Production implementation HEAD / 生产实现提交：`4df9104c0c18911a66a51277fd0a774aabe7ff35`
- Implementation range / 实现提交范围：`dbbceb3` through `4df9104`
- Production run / 生产运行：`4ea0c0d5c1d146568e28cd9f8a4b3591`
- Verified backup / 已校验备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260919-012935`
- Accepted evidence / 接纳证据：`/root/turingmarket/deployment-evidence/accepted-4ea0c0d5c1d146568e28cd9f8a4b3591.json`

## Delivered / 已交付

- Added schema v24 `knowledge_entries.org_id`, deterministic historical backfill, organization-scoped identity/search indexes, and six fail-closed ownership/custody triggers. / 新增 schema v24 的 `knowledge_entries.org_id`、确定性历史回填、组织范围身份/检索索引及六个失败关闭的归属/保管触发器。
- Scoped ordinary knowledge ingest, upload, duplicate detection, search, categories, similar lookup, usage updates, AI/RAG, proposal/PPT context, and business-artifact producers to the authenticated organization. / 普通知识入库、上传、去重、检索、分类、相似查询、使用计数、AI/RAG、方案/PPT 上下文及业务产物生产者均限定到当前鉴权组织。
- Preserved platform-admin global knowledge and AI-conversation audit views, while every AI generation remains restricted to the administrator's active organization. / 平台管理员继续可在显式审计界面查看全局知识和全部 AI 对话，但管理员发起的 AI 生成仍只调用当前活动组织知识。
- Preserved all historical knowledge IDs, chunks, FTS rows, source hashes, source identities, the accepted latest UI, M3/M4 flows, proposal/report behavior, and frozen PPT bytes. / 保留历史知识 ID、切片、FTS、来源哈希和来源身份，并保持已验收最新界面、M3/M4、方案/报告行为及冻结 PPT 字节不变。
- Request bodies cannot select knowledge ownership. Server-owned authentication, Campaign, organization custody, and the unique legacy default organization are the only accepted ownership facts. / 请求体不能指定知识归属；仅服务端鉴权、Campaign、组织保管关系及唯一历史默认组织可作为权威归属事实。
- Legacy direct service calls remain compatible only when the database contains exactly one organization; ambiguous multi-organization calls fail closed. / 旧版直接服务调用仅在数据库恰有一个组织时兼容推断，多组织歧义场景全部失败关闭。

## Migration Safety / 迁移安全

- Backfill precedence is Campaign custody, organization-methodology custody, organization business link, then the unique legacy default organization. Mutable membership is never treated as historical ownership. / 回填优先级为 Campaign 保管、组织方法论保管、组织业务关联、唯一历史默认组织；可变成员关系不作为历史归属依据。
- Production's historical v23 `knowledge_entries` SQL differed only by formatting, while v24 deliberately replaced exactly three indexes. The verifier now admits only that checksum-bound legacy shape, semantically verifies every unchanged index, and rejects any additional drift. / 生产 v23 的 `knowledge_entries` 建表 SQL 仅存在历史格式差异，而 v24 有意替换恰好三个索引；验证器仅接纳该摘要绑定的旧结构，对全部未变索引做语义校验，并拒绝任何额外漂移。
- Production migration rehearsal ran twice with identical output, `quick_check=ok`, zero foreign-key violations, legacy rows preserved, and pre-migration restore verified. / 类生产迁移演练两次输出一致，SQLite 完整性正常、外键异常为 0、旧数据保持且迁移前恢复已验证。

## Focused Verification And Independent Review / 定向验证与独立审查

- Migration v24 passed `9/9`; two-organization knowledge/RAG runtime passed `8/8`; the affected combined regression passed `134/134`; focused compatibility checks passed `22/22`. / v24 迁移 `9/9`、双组织知识/RAG 运行时 `8/8`、受影响组合回归 `134/134`、定向兼容检查 `22/22` 全部通过。
- Release inventory passed `5/5`, trusted-source/deployment pins `3/3`, migration-gate exactness `18/18`, migration verifier `3/3`, Phase 4 target `1/1`, and v24 release target `4/4`. / 发布清单、可信来源、迁移精确性及候选目标检查均通过。
- The guarded candidate independently passed the exact production v23-to-v24 migration rehearsal, remote CRM/customer checks `76/76`, Nginx validation, read-only guards, and Chromium deployment smoke `2/2`. / 受控候选通过真实生产 v23→v24 迁移演练、远端 CRM/客户检查 `76/76`、Nginx、只读门禁及 Chromium 部署冒烟 `2/2`。
- Independent review identified stale v23 release assertions, which were fixed before deployment. Final review returned `APPROVED`, with no open P0-P2 findings and no incorrect constraint relaxation. / 独立审查发现旧 v23 发布断言，部署前已修复；终审结论为 `APPROVED`，无未关闭 P0-P2，亦未错误放宽约束。
- Nonblocking follow-up: add one negative whitelist regression for each of the three approved historical index replacements. / 非阻断后续项：可为三个获准替换的历史索引分别增加一条负向白名单回归。

## Deployment Record / 部署记录

- Rejected candidates stopped before production mutation when the historical v23 SQL/index framing and stale v23 gate assertions were detected. Each issue received an exact regression and independent re-review before retry. / 历史 v23 SQL/索引结构及旧 v23 门禁断言被发现时，候选均在生产变更前停止；每项问题均补充精确回归并独立复审后才重试。
- One interrupted pre-mutation candidate was recovered with the supported recovery controller; only candidate state was cleaned and production remained unchanged. / 一次变更前中断候选通过正式恢复控制器清理，仅移除候选状态，生产未被修改。
- Accepted run `4ea0c0d5c1d146568e28cd9f8a4b3591` returned `DEPLOY_OK` and `RETENTION_CLEANUP_OK`. / 正式运行返回 `DEPLOY_OK` 与 `RETENTION_CLEANUP_OK`。
- Trusted source SHA-256 / 可信源码摘要：`ced82bf58651f5c878764010875da7b6c9210d0fd0f461aab797d957f1f77cd2`。
- Candidate tree SHA-256 / 候选树摘要：`8e20165a6b97f3d586bc2ee2fcfbf55c8fc3af9828b5953657a41f22de5dd934`。
- Acceptance facts SHA-256 / 验收事实摘要：`2d0bb1bd6bb6beeab52b787f85a129df758fa08519db61bd5c61aebbe8c509cf`。
- Replay evidence SHA-256 / 重放证据摘要：`0a926d666a6368b7f721afea9f80309b6fecc3f4d12e1b6e71d84e373522e7df`。

## Backup And Production Acceptance / 备份与生产验收

- Initial backup manifest has `340` entries and SHA-256 `9949019f984f5bd93e7a6fbbbfc364e7ab0dbad55b3f47aac8029b479f5e9dbf`; cutover manifest has `35` entries and SHA-256 `a94e28cc5ac05b509d53efcace0368665f20f51d52b3ed955e5f9a4ba0f9457a`. Both full manifests passed verification. / 初始备份清单 340 项、切换清单 35 项，两份完整清单均通过校验。
- Initial database SHA-256 is `26b9f6745733a463c3548f36c03a50b387f32803dc265f313dfeffbc6ca4d4e4`; cutover database SHA-256 is `f2d863b54e1fd59c239cb04609baae9588aedd57158a162151978f468dbc54c5`. / 初始与切换数据库摘要均已记录。
- Public `/api/health` returned `200` with parser ready; PM2 is `online` with restart count `0`; Nginx is active and valid. / 公网健康与解析器正常，PM2 在线且重启次数为 0，Nginx 正常。
- Production schema is `v24`; all `128` knowledge entries have `org_id=1`, null owners are `0`, organization count is `1`, five organization-aware indexes and six ownership triggers are present, `quick_check=ok`, and foreign-key violations are `0`. / 生产 schema 为 v24；128 条知识全部归属组织 1，空归属为 0，组织数为 1，组织范围索引和六个归属触发器齐全，SQLite 完整性及外键均正常。
- Authenticated production smoke verified administrator login, `/api/auth/me`, organization-scoped knowledge search, administrator AI-conversation audit visibility, and logout through Nginx. No credential is stored in this record. / 经 Nginx 完成管理员登录、身份、组织范围知识检索、管理员 AI 对话审计可见性及退出验收；本记录不保存任何凭据。
- Remote and local `app.js` SHA-256 remain `748223da49f003a786ee535e08f9236f406d4cd54958bb2e1291d29c98f7b8d1`; frozen `ppt.js` remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 最新界面文件保持一致，冻结 PPT 未回退。

## Delivery Cadence / 交付节奏

Continue one independently useful feature per release. Ordinary features receive exact affected tests, necessary safety checks, one independent review, a verified backup, immediate production deployment, and focused online acceptance. Heavy suites remain reserved for phase closeout or broad-risk boundaries such as schema, tenant authority, authentication, shared infrastructure, and real external writes. / 后续继续“一功能一上线”；普通功能执行精确受影响测试、必要安全门禁、一次独立审查、可验证备份、立即部署及线上定向验收，繁重套件仅由阶段收口或高风险边界触发。

## Next Boundary / 下一边界

Select the next bounded Phase 8 permission or tenant-governance capability as an independent release. AI-conversation ownership migration, plan entitlements, quotas, electronic signature, and real external delivery remain separate scopes. / 下一版本从 Phase 8 选择一个有界权限或租户治理能力独立上线；AI 对话归属迁移、套餐权益、配额、电子签及真实外部投递继续分离开发。
