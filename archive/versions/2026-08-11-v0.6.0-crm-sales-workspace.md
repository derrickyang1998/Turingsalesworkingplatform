# v0.6.0 CRM Sales Workspace / CRM 销售工作台

Date / 日期: 2026-08-11
Candidate update / 候选更新: 2026-08-17
Status / 状态: Release candidate, not production / 发布候选，尚未生产切换
Branch / 分支: `codex/v0.6.0-crm-sales-workspace`

## Scope / 范围

- Separate customer dashboard and customer detail interfaces. / 客户看板与客户明细双界面。
- Organization-safe CRM reads and governed customer/opportunity lifecycle. / 组织安全的 CRM 读取与客户/商机生命周期。
- Team/private scopes, contacts, tasks, evidence, public pool, claim/release, and append-only knowledge-backed follow-up. / 团队/个人作用域、联系人、任务、证据、公海、领取/释放和知识库追加式跟进。
- Schema 6 migration and v1-to-v6 trusted release chain. / Schema 6 迁移与 v1-to-v6 可信发布链。
- Latest UI and frozen PPT contract preserved. / 最新 UI 与冻结 PPT 合同保持不回退。
- Customer notes writes now support exact atomic `expected_notes` compare-and-set protection; stale concurrent writes return a bounded 409 and release smoke restoration cannot overwrite a newer value. / 客户备注写入新增精确原子 `expected_notes` 比较更新保护；并发陈旧写入返回受限 409，发布冒烟恢复不会覆盖更新值。

## Release-Candidate Runtime Addendum / 发布候选运行时补充

- The upload parser is now a sealed appliance for knowledge, influencer, and demand-file routes. It is built for Linux `x86_64` with Node.js `v20.20.2` and Python `3.14.4`; the exact tree is 4,213 files, 491 directories, 716,157,800 bytes, SHA-256 `30fbfca170772f23071d6216eea8a83b86d27aad18c2b22b1a1e0e9ae773d7cd`. / 上传解析器现为知识库、网红和需求文件路由使用的密封设备；其平台、规模和哈希如前所列。
- Exact Node closure / 精确 Node 闭包: `read-excel-file@9.2.0`, `@xmldom/xmldom@0.9.11`, `fflate@0.8.3`, `unzipper-esm@0.13.3`, `graceful-fs@4.2.11`, `node-int64@0.4.0`; lock SHA-256 `e1d6e5ababef1fb6f0aa69363be328e4da42283c96e268378c2f83076722fc46`.
- Exact Python closure / 精确 Python 闭包: `certifi==2026.7.22`, `charset-normalizer==3.5.0`, `colorlog==6.12.0`, `flatbuffers==25.12.19`, `idna==3.18`, `numpy==2.5.2`, `omegaconf==2.4.0.dev13`, `onnxruntime==1.28.0`, `opencv-python==5.0.0.93`, `packaging==26.3`, `pillow==12.3.0`, `protobuf==7.35.1`, `pyclipper==1.4.0`, `PyMuPDF==1.27.2.3`, `pypdf==6.14.2`, `PyYAML==6.0.3`, `rapidocr==3.9.2`, `requests==2.34.2`, `shapely==2.1.2`, `six==1.17.0`, `tqdm==4.70.0`, `typing_extensions==4.16.0`, `urllib3==2.7.0`; hashed lock SHA-256 `22f72070ee7c62b428261b64435fbb0b24d87f0aaa7a4d0ac0d8e7a45bfb44df`.
- Build, install, snapshot, and rollback stay under root-owned lifecycle paths. Jobs run as locked `turingmarket-parser` in a systemd 257+ `RootDirectory` chroot with private namespaces, no capabilities or network/socket access, a read-only root, bounded writable paths, and resource ceilings. / 构建、安装、快照和回滚限定在 root 所有生命周期路径；任务在上述 systemd/chroot 隔离中运行。
- An unprivileged build unit initializes real RapidOCR against the staged dependency tree before sealing. Raster-image contract tests cover RapidOCR 3 object and legacy tuple output, and installation requires all 21 self-tests, including XLSX, PPTX, OCR inference, isolation, and pressure checks. / 非特权构建单元在密封前针对暂存依赖树初始化真实 RapidOCR；栅格图像合同测试覆盖新旧输出，安装要求包含 XLSX、PPTX、OCR 推理、隔离与压力检查在内的 21 项自检全部通过。
- The trusted-source manifest now pins 46 files, including an independent public release watchdog and the migration cleanup helper/unit; candidate dependency and test writes are constrained by a 6 GiB / 262,144-inode tmpfs. / trusted-source 清单现固定 46 个文件并纳入独立公网发布守护器及迁移清理 helper/unit；候选依赖与测试写入受 6 GiB、262,144 inode 的 tmpfs 总量约束。

## Cutover Controls / 切换控制

- Cleanup-control replay accepts only the canonical absent/enabled topologies, idempotently resolves the one exact `.restore.next` residue, validates all control-path parents before mutation, and binds effective PID 1 fragment/Names/DropInPaths across every systemd search root. Public watchdog deadlines are operation-specific: 120 seconds for initial cutover and 7,200 seconds for accepted finalization plus rollback/resume; identity is revalidated before each public link swap and disarm. / 清理控制回放仅接受两种 canonical topology，并在首次变更前验证父链及 PID 1 实际配置；公网 watchdog 的操作专属窗口为初始切换 120 秒、接受态 finalization 与回滚/恢复 7,200 秒，公网切换及解除前均复核 active identity。
- `/var/lib/turingmarket/ppt-cache` is the fifth external state target and is a direct root-only directory, not a release-tree symlink. A first install records the absent origin, creates an empty `root:root 0700` directory only after writers stop, and removes it during pre-mutation recovery only if it remains empty. / 该路径是第五个外置状态目标，为直接使用的 root-only 目录而非发布树软链接；首次安装只在停写后创建，预变更恢复仅在仍为空时删除。
- The per-device capacity gate covers database and PPT snapshots, existing and staged parser runtime, rollback staging, and `node_modules` growth. It requires `max(10%, 512 MiB)` margin and `CUTOVER_CAPACITY_OK` before maintenance or mutation. / 按设备计算的容量门禁覆盖数据库/PPT 快照、现有与待装解析器、回滚 staging 和依赖增量，并在维护或变更前要求上述余量及成功标记。
- Cutover enters all-traffic maintenance, drains parser admissions/spool/units/cgroup, stops and quiesces PM2 plus SQLite, drains the parser again, then prepares the cache and snapshot. / 切换先进入全流量维护并排空解析器，再停止并静默 PM2 与 SQLite，随后二次排空，最后准备缓存和快照。
- `/api/health` must report `status=ok`, `parser.ready=true`, and the exact parser manifest SHA-256. Schema 3 accepted markers bind installed self-test evidence, acceptance facts, and the runtime-tree hash before public traffic can return. / 健康接口必须报告应用与解析器 ready 及精确清单哈希；schema 3 验收标记在恢复公开流量前绑定安装自检、验收事实和运行时树哈希。

## S6 Checkpoint Evidence / S6 检查点证据

- Accepted S6 commit: `90713b23f417602045d144ca24e80555ff1580b2`.
- Independent verdicts: Code Review `APPROVE`, QA `GO`.
- Verification: 342/342 final review matrix; 373/373 fresh pre-commit matrix.
- Full-history bundle: `phase5-v060-90713b23f417-full-source.bundle`.
- Bundle SHA-256: `fd4b925d82056d1eb53a5c65cc67461bc3eadd9ba88f47020ed37343d5dae887`.
- App build/query: `20260811-v060-crm-sales-workspace` / `20260811v060crmsalesworkspace`.
- Frozen PPT SHA-256: `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`.

The S6 verdicts did not cover the later parser and cutover changes. Its Windows non-browser regression and review results are retained as historical phase-closeout evidence. The latest feature slice must produce its own focused tests, independent review, backup, deployment, and production smoke evidence; Playwright and the full matrix run at phase closeout or when a broad risk boundary requires them. / S6 结论不覆盖后续解析器与切换变更；其 Windows 非浏览器回归和审查结果仅作为历史阶段收口证据保留。最新功能切片必须生成自己的定向测试、独立审查、备份、部署和生产冒烟证据；Playwright 与完整矩阵在阶段收口或跨越高风险边界时执行。

## Current Verification Snapshot / 当前验证快照

- Local post-remediation gates / 本地修复后门禁: Task 1 `182 total / 163 pass / 0 fail / 19 Linux skips`; Task 2 `135 / 119 / 0 / 16`; Task 3 `81 / 79 / 0 / 2`.
- AppSec remediation / AppSec 整改: historical focused evidence reported public guard `9/9`, source contract `40/40`, lifecycle takeover `22/22`, and trusted-source plus v0.6 contracts `47/47`; that historical result does not replace the retained current run. / 历史 AppSec 聚焦结果保留，但不替代当前证据。
- Historical phase-closeout evidence / 历史阶段收口证据: the 71-file Windows non-browser aggregate remains `1,875 / 1,784 / 0 / 91`; earlier public-guard attempts are retained without reclassifying failures and do not represent the latest feature-slice bytes. / 71 文件 Windows 非浏览器汇总与早期公网守护尝试继续按原分类保留，但不代表最新功能切片字节。
- Incremental cadence / 增量节奏: one feature uses exact affected tests, one independent review, verified backup, immediate guarded deployment, and production health/login/core-path smoke. Full regression and Playwright move to phase closeout, scheduled release windows, or broad-risk changes; HIGH/CRITICAL, migration, backup/rollback, and production-smoke failures still block release. / 单功能采用精确定向测试、一次独立审查、可校验备份、立即受控上线和生产健康/登录/核心路径冒烟；完整回归与 Playwright 改到阶段收口、计划窗口或高风险跨模块改动，关键风险仍阻断上线。
- DevOps rounds 5-10 / DevOps 第 5-10 轮: exact no-follow transaction-lock inventory, trusted transaction-locked reads, phase/RunId-bound four-family unit drain, explicit lock-only `absent` recovery, post-watchdog disarm recheck, durable parent fsync, immutable root/www-data GIDs, guard-before-PM2 accepted finalization, and a dedicated bounded 7,200-second accepted-finalization deadline close the reviewed takeover, recovery, and slow-start timeout races. / 上述机制封闭了已审查的接管、扫描顺序、解除守卫、接受态恢复及合法慢启动超时竞态。
- Current parent-focused evidence / 当前父级定向证据: lifecycle takeover `31/31`, deployment source contract `44/44`, selected public-guard concurrency/read/timeout tests `4/4`; PowerShell AST, Bash syntax, trusted hashes, diff check, and focused secret scan pass. Independent final review is recorded before deployment. / 生命周期接管 31/31、发布源合同 44/44、公网守护关键用例 4/4，语法、哈希、diff 与聚焦敏感信息检查通过；独立终审在部署前记录。
- Incremental release-wrapper evidence / 增量发布包装器证据: seven dynamic success/failure scenarios and focused customer service/HTTP tests `3/3` pass; strict environment isolation, remote evidence syntax, real local deploy validation, backup ordering, GitHub dry-run, authenticated preflight, automatic rollback orchestration, and secret scans pass. Independent verdict: `APPROVE`. / 7 条动态成功/失败场景和客户服务/HTTP 定向测试 3/3 通过；环境隔离、远端证据语法、真实本地部署校验、备份顺序、GitHub 预推送、认证预检、自动回滚编排与敏感信息检查通过，独立结论为 `APPROVE`。
- Playwright / Playwright 门禁: the local all-files attempt found no installed Playwright Chromium executable, so browser-only tests did not start and no browser was installed implicitly. The gate remains pending and is not recorded as passed. / 本机缺少所需浏览器，可视化用例未启动；未擅自安装，本门禁仍不记为通过。
- Production / 生产: verified v0.4 remains untouched; no v0.6 production mutation or cutover has started. / 已验证的 v0.4 保持原样，尚未发生 v0.6 生产变更或切换。

## Release State / 发布状态

Production is still verified v0.4, and the current v0.6 feature slice is not yet production-approved. This archive becomes a production record after the slice's focused tests and fresh review pass, authoritative preflight and GitHub synchronization complete, a verified backup and capacity result exist, guarded deployment succeeds, parser-aware remote API/access plus health/login/core-path checks pass, rollback evidence is retained, and archive synchronization completes. Full regression and Playwright evidence are additionally required only at phase closeout, scheduled release windows, or when the broad-risk trigger applies. / 生产仍为已验证的 v0.4，当前 v0.6 功能切片尚未获得生产批准；只有该切片的定向测试与最新复审、权威工作区预检、GitHub 同步、可校验备份与容量结果、受控部署、解析器感知的远端 API/权限及健康/登录/核心路径检查、回滚证据和归档同步全部完成后，才更新本归档为正式发布记录。完整回归与 Playwright 仅在阶段收口、计划发布窗口或高风险触发时追加要求。
