# v0.7.4a 飞书多维表格 Outbox 生产归档

- 提交：`feedfba`；发布夹具修复：`5d4c683`。
- M4 Bitable 批量投递已具备活动范围的持久化 Outbox：保存 UUID、精确快照、执行者、活动、状态和安全回执。相同 UUID 重放不会再次发送。
- 模糊外部响应或本地回执落库失败保持 `pending`，不自动重发；仅活动 Owner 或组织管理员可凭已核实且唯一的远端记录 ID 手工对账完成。
- 数据库拒绝终态回执更新、删除、`INSERT OR REPLACE` 覆盖和重复远端记录 ID；CSV 兜底、Webhook 与最新 M4 界面保持兼容。
- 定向 Feishu/Outbox/网红工作流 `31/31`、迁移/脱敏 `3/3`、可信来源 `32/32`、部署合同 `108/108`、真实 Express 重放 `8/8` 通过；两轮独立审查均为 `APPROVE`。
- 首个候选在生产变更前因遗留 v7 重放夹具安全中止，修复并复审后成功发布。备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260902-021014`；`SHA256SUMS` 已校验，清单 SHA-256：`41473cddec70ea67e0fa96d3a7fc9daa931ac63e9c560b1f695b19fa15bd4395`。
- 线上验收：公网/回环健康 `ok`、Parser ready、`/m4` 为 `200`、PM2/Nginx online、数据库 `quick_check=ok`、外键异常 `0`、schema `v8` 和 Outbox 表存在。
- 本轮没有真实写入飞书多维表格，后续先补 Owner/组织管理员对账 UI，再单独设计更新/重试合同，不做静默重发。
