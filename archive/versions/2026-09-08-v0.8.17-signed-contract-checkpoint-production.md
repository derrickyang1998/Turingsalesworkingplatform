# v0.8.17 Signed Contract Checkpoint Production Record / 已签合同凭证关口生产记录

## Release / 发布

- Date / 日期: `2026-09-08`
- Scope / 范围: Phase 7 M4 signed-contract evidence checkpoint and live-state gate / 阶段 7 M4 已签合同凭证关口与上线状态门禁
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Plan / 计划: `6d507cd`
- Implementation / 实现: `e199afb`
- Deployment policy fix / 发布策略修正: `4936034`
- Database / 数据库: schema remains `v15`; no migration / schema 保持 `v15`，无迁移
- Production deployment run / 生产发布运行: `4dc1fa55e228436f97e3a5031969d875`

## Delivered / 已交付

- The approved M4 interface now exposes a `contract_sent` checkpoint labelled “合同待回签” and a compact signed-contract confirmation action without adding or replacing a page. / 已批准的 M4 界面新增标记为“合同待回签”的 `contract_sent` 阶段和紧凑的已签合同确认操作，未新增或替换页面。
- A confirmation records the contract or PO reference, counterparty, signed timestamp, and confirmation note, then creates immutable Campaign-linked knowledge evidence. / 确认操作记录合同或采购单编号、签约对方、签署时间和确认说明，并生成关联 Campaign 的不可变知识凭证。
- New `collaboration.resource.v2` orders can enter `contracted` only through the dedicated confirmation endpoint and can enter `live` only after valid signed evidence exists. Historical v1 orders and already-live/completed records remain compatible. / 新版 `collaboration.resource.v2` 订单只能通过专用确认接口进入 `contracted`，且只有存在有效已签凭证后才能进入 `live`；历史 v1 订单及既有已上线/已完成记录保持兼容。
- Indexed knowledge text contains only a generic signed-contract conclusion. Sensitive contract fields remain in controlled metadata with `retrieval_eligible:false`, so RAG cannot surface them. / 知识索引正文仅包含通用的合同已签结论；敏感合同字段保存在受控元数据中并标记 `retrieval_eligible:false`，RAG 不会检索这些字段。
- The endpoint `POST /api/collaborations/:id/contract-confirmations` uses the existing authorization, idempotency, audit, and business-event contracts. / `POST /api/collaborations/:id/contract-confirmations` 沿用既有授权、幂等、审计和业务事件合同。
- The latest M4 shell, import/search/filter/export/Feishu controls, AI/knowledge links, and frozen proposal PPT remain unchanged. / 最新 M4 壳层、导入/搜索/筛选/导出/飞书控制、AI/知识链路和冻结方案 PPT 均保持不变。

## Focused Verification And Independent Review / 聚焦验证与独立审查

- Feature contract tests passed `21/21`; the structural sanitizer contract passed `1/1`; trusted-source contracts passed `3/3`. / 功能合同测试 `21/21`、结构脱敏合同 `1/1`、可信来源合同 `3/3` 通过。
- After the deployment-policy correction, a fresh focused matrix passed `7/7`, including the explicit v14-reject/v15-accept sanitizer boundary. / 发布策略修正后，新鲜定向矩阵 `7/7` 通过，其中包含明确的 v14 拒绝、v15 接受脱敏边界。
- JavaScript syntax, `git diff --check`, focused secret scanning, frozen PPT hash, and guarded local deployment preflight passed. / JavaScript 语法、`git diff --check`、定向密钥扫描、冻结 PPT 哈希和受保护本地发布预检均通过。
- Independent review rechecked evidence authenticity, state transitions, compatibility, knowledge exposure, and versioned sanitization policy. The final verdict was `APPROVE`, with no remaining finding. / 独立审查复核凭证真实性、状态流转、兼容性、知识暴露和版本化脱敏策略；最终结论为 `APPROVE`，无剩余发现。
- The formal candidate passed real Express replay `8/8`, release gate `21/21`, and built-in browser smoke `2/2`. These production safeguards remained mandatory because they protect release trust and recovery. / 正式候选通过真实 Express 回放 `8/8`、发布守卫 `21/21` 和内置浏览器冒烟 `2/2`；这些生产安全门禁用于保护发布信任和恢复能力，因此仍为强制项。

## Controlled Deployment Recovery / 受控发布恢复

- The first two deployment attempts stopped before cutover, so the accepted production version and business traffic stayed intact. / 前两次发布尝试均在切换前停止，因此已接受生产版本和业务流量保持完整。
- Diagnostic evidence identified a trusted-source failure: `contracted` had changed the frozen historical structural policy without a matching schema-v15 manifest. / 诊断证据定位到可信来源失败：`contracted` 改动了冻结的历史结构策略，却没有对应的 schema v15 清单。
- The correction restored the historical base policy and introduced the new status only in `STRUCTURAL_COLUMN_POLICY_V15`, with exact v14 rejection and v15 acceptance tests. / 修正恢复历史基础策略，只在 `STRUCTURAL_COLUMN_POLICY_V15` 中加入新状态，并用精确测试固定 v14 拒绝和 v15 接受行为。
- Pre-cutover diagnostic evidence is retained at `/root/turingmarket/deployment-evidence/v0817-candidate-diagnostic-20260908T090805Z`. No production business data was lost. / 切换前诊断证据保存在 `/root/turingmarket/deployment-evidence/v0817-candidate-diagnostic-20260908T090805Z`，没有生产业务数据丢失。

## Production Evidence / 生产证据

- Recoverable backup / 可恢复备份: `/root/turingmarket/backups/v060-crm-sales-workspace-20260908-174124`
- Backup checksum manifest / 备份校验清单: `296` entries, all reverified; SHA-256 `6f2c3bc8e7b226be198a819e0ac3888b88c13ded94efc14bfdc890616e3d7479` / 共 `296` 项且全部复验通过
- Candidate tree SHA-256 / 候选树摘要: `4f09be5fed90d92df7b7bd1b9904ce7660b3b6ce63b278735f952ce040e64794`
- Accepted parser runtime SHA-256 / 已接受解析器运行时摘要: `04630b2ee928ba8031a4ea5ce6435519161c19e05884813e5ec623d12adb38ce`
- Acceptance facts SHA-256 / 验收事实摘要: `63416e1b1e6e392d6660db60ce62d3147abf5c14c1ddfd5f95bd7f22c4965194`
- Replay evidence SHA-256 / 回放证据摘要: `69cc51589febd40478985c238114186ab99e43b508462be9a95de698fcf1503c`
- Parser evidence SHA-256 / 解析器证据摘要: `de2122de4ebc960ccc22d527b52222200b00dca839d68fa6eab8a1571b6706a0`
- Public and loopback `/api/health` and M4 returned `200`; PM2 `turingmarket` is `online`; Nginx configuration passed. / 公网与回环 `/api/health` 和 M4 均返回 `200`；PM2 `turingmarket` 为 `online`；Nginx 配置校验通过。
- Authenticated checks confirmed administrator login, all-conversation AI audit read, three existing collaboration projections, and controlled `400 INVALID_COLLABORATION_ID` rejection. The acceptance session was logged out and active sessions returned to `0`. / 登录态检查确认管理员登录、全部 AI 对话审计读取、既有三条合作记录投影和受控 `400 INVALID_COLLABORATION_ID` 拒绝；验收会话已注销，活动会话恢复为 `0`。
- SQLite migrations are `15/15`, schema is `v15`, `integrity_check=ok`, and foreign-key violations are `0`. No real signed-contract evidence or Feishu write was created for acceptance. / SQLite 迁移为 `15/15`、schema 为 `v15`、`integrity_check=ok`、外键异常 `0`；验收未创建真实已签合同凭证或飞书写入。
- Frozen proposal `ppt.js` SHA-256 remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 冻结方案 `ppt.js` 摘要保持不变。

## Delivery Cadence And Next / 发布节奏与下一步

- Normal features use the approved small-batch cadence: exact affected tests, syntax/diff/secret checks, one independent review, recoverable backup, immediate production deployment, and online feature smoke. / 普通功能采用已批准的小步节奏：精确受影响测试、语法/差异/密钥检查、一次独立审查、可恢复备份、立即生产部署和线上功能冒烟。
- Full suites are reserved for schema/data changes, authentication or security, shared runtime, external writes, broad cross-module work, scheduled windows, and phase closeout. / 完整套件仅用于 schema/数据变更、鉴权或安全、共享运行时、外部写入、大范围跨模块工作、计划窗口和阶段收口。
- Signed-contract evidence is accepted. Contract file upload or e-signature, content review, publishing, payment, settlement, and review remain independent Phase 7 slices. / 已签合同凭证已通过验收；合同文件上传或电子签约、内容审核、发布、付款、结算和复盘仍是阶段 7 的独立切片。
- Real Feishu delivery remains gated by approved production configuration and a test campaign. / 真实飞书投递仍须具备获批生产配置和测试活动后单独验收。
