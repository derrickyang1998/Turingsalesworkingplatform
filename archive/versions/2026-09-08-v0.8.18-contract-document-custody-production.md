# v0.8.18 Contract Document Custody Production Record / 合同文件保管生产记录

## Release / 发布

- Date / 日期: `2026-09-08`
- Scope / 范围: Phase 7 M4 signed-contract PDF upload, immutable custody, and confirmation binding / 阶段 7 M4 已签合同 PDF 上传、不可变保管与确认绑定
- Status / 状态: production deployed and verified / 已部署生产并完成验收
- Plan / 计划: `6926b1a`
- Implementation / 实现: `acba5b3`
- Release-gate correction / 发布门禁修正: `7181867`
- Database / 数据库: schema `v16`, migrations `16/16` / schema `v16`，迁移 `16/16`
- Production deployment run / 生产发布运行: `0f0c2c80edcf43c08ce83e996f5b94ac`

## Delivered / 已交付

- The approved M4 collaboration modal now supports PDF upload, document listing, and attachment download without adding or replacing a page. / 已批准的 M4 合作弹窗新增 PDF 上传、文档清单和附件下载，未新增或替换页面。
- Migration `016` adds strict append-only `collaboration_contract_documents` custody with safe filename, byte size, SHA-256, uploader, timestamps, and Campaign/collaboration ownership. / 迁移 `016` 新增严格只追加的 `collaboration_contract_documents` 保管表，记录安全文件名、字节大小、SHA-256、上传者、时间及 Campaign/合作归属。
- Each collaboration accepts at most five PDFs. Duplicate hashes are rejected; canonical base64, PDF header/end markers, and the 32-byte to 8-MiB size boundary are enforced. / 每个合作最多接收五份 PDF；重复摘要会被拒绝，并强制执行规范 base64、PDF 头尾标记和 32 字节至 8 MiB 大小边界。
- Active PDF content, including JavaScript, OpenAction, AA, XFA, and ObjStm patterns, is rejected. Downloads are attachment-only with `Cache-Control: no-store` and `X-Content-Type-Options: nosniff`. / JavaScript、OpenAction、AA、XFA、ObjStm 等 PDF 主动内容会被拒绝；下载仅使用附件方式，并固定 `no-store` 与 `nosniff`。
- Team Campaign readers and administrators can list or download documents. Other users receive a masked `404`, preventing record enumeration. / Campaign 团队读取者及管理员可查看或下载文档；其他用户统一获得不可枚举的 `404`。
- New v2 signed confirmations must bind a same-Campaign contract document ID and exact SHA-256. Legacy v1 evidence remains readable. / 新版 v2 已签确认必须绑定同 Campaign 合同文档 ID 和精确 SHA-256；历史 v1 凭证继续可读。
- Contract bytes never enter the knowledge base or RAG. Generic contract evidence is also marked `retrieval_eligible:false`, including general RAG, Campaign RAG, and explicitly selected knowledge paths. / 合同原始字节永不进入知识库或 RAG；通用合同凭证也标记为 `retrieval_eligible:false`，覆盖通用 RAG、Campaign RAG 和显式选择知识路径。
- The latest M4 shell, guided import, search/filter/column workspace/export/Feishu entry points, AI/knowledge links, and frozen proposal PPT remain unchanged. / 最新 M4 壳层、引导式导入、搜索/筛选/列工作区/导出/飞书入口、AI/知识链路和冻结方案 PPT 均保持不变。

## Focused Verification And Independent Review / 聚焦验证与独立审查

- Contract migration and service tests passed `7/7`; M4 client tests passed `16/16`; AI/knowledge tests passed `8/8`; knowledge-governance tests passed `10/10`. / 合同迁移与服务测试 `7/7`、M4 客户端 `16/16`、AI/知识 `8/8`、知识治理 `10/10` 通过。
- PDF structural sanitization passed `24` checks with `1` Linux-only environment skip; the focused v16 sanitizer passed `1/1`. / PDF 结构脱敏通过 `24` 项检查，`1` 项 Linux 环境检查跳过；v16 定向脱敏测试 `1/1` 通过。
- Migration exactness passed `15/15`; release contracts passed `37/37`; trusted-source manifest tests passed `3/3`; JavaScript syntax passed across `23` files. / 迁移精确性 `15/15`、发布合同 `37/37`、可信来源清单 `3/3`、`23` 个 JavaScript 文件语法检查通过。
- `git diff --check`, focused secret scanning, frozen PPT hash verification, and local deployment preflight passed. / `git diff --check`、定向密钥扫描、冻结 PPT 哈希校验和本地发布预检通过。
- Independent review found six issues covering PDF constraints, RAG exclusion, team read access, active-content bypasses, stale schema replay expectations, and client retry collisions. All were fixed and re-reviewed. Final verdict: `APPROVE`, with no open P0-P2 finding. / 独立审查发现 PDF 约束、RAG 排除、团队读取、主动内容绕过、陈旧 schema 回放预期和前端重试碰撞六项问题；全部修复并复审，最终结论为 `APPROVE`，无 P0-P2 未关闭项。

## Controlled Deployment Recovery / 受控发布恢复

- The first remote candidate stopped before cutover because the real Express replay still expected migrations `1..15`. Production was not mutated and only the unaccepted candidate was cleaned up. / 首个远端候选因真实 Express 回放仍只期望迁移 `1..15` 而在切换前停止；生产未被修改，仅清理未接受候选。
- The release assertion was corrected to `1..16`. A fresh local replay passed `8/8`, then the accepted remote candidate passed real Express replay `8/8`, release gate `21/21`, built-in browser smoke `2/2`, migration rehearsal, parser runtime, and Nginx checks. / 发布断言修正为 `1..16`；新鲜本地回放 `8/8` 通过后，正式远端候选继续通过真实 Express 回放 `8/8`、发布守卫 `21/21`、内置浏览器冒烟 `2/2`、迁移演练、解析器运行时和 Nginx 检查。
- The remaining release-time bottleneck was a trusted parser dependency-cache rebuild on a slow package mirror, not an application deadlock. Cache reuse is deferred to a dedicated infrastructure slice with exact lockfile, runtime identity, platform, and permission matching. / 本次剩余发布耗时来自慢速镜像上的可信解析器依赖缓存重建，并非应用死锁；缓存复用延后至独立基础设施切片，且只在锁文件、运行时身份、平台和权限精确一致时启用。

## Production Evidence / 生产证据

- Recoverable backup / 可恢复备份: `/root/turingmarket/backups/v060-crm-sales-workspace-20260908-212127`
- Backup checksum manifest / 备份校验清单: `296` entries, all reverified; SHA-256 `5bdc921281adb0d294368890e0b98ffc81bba6b2c2992566f5240f281f74febb` / 共 `296` 项且全部复验通过
- Candidate tree SHA-256 / 候选树摘要: `c0c226657a8592d354def8360efd7f670bca7a080004a0d976a029de475e32ab`
- Acceptance facts SHA-256 / 验收事实摘要: `2f408f2519f93825497f45b05b3959ffae24aa0341a3959ac1cc0cd943633ae6`
- Replay evidence SHA-256 / 回放证据摘要: `b0927bebf0f98b04c48e72dab54ea649d52d5c8df767c35fd722855ac436e673`
- Parser evidence SHA-256 / 解析器证据摘要: `48a0afd8e357445b232c8786f1fd3977e1f7e80a5fabac3a1d2df7dbe2061d78`
- Accepted parser runtime SHA-256 / 已接受解析器运行时摘要: `04630b2ee928ba8031a4ea5ce6435519161c19e05884813e5ec623d12adb38ce`
- Accepted source marker SHA-256 / 已接受来源标记摘要: `ff065e85bcf00a880a96490911068710a595f70200f75ccd49be05bbca5fe30e`
- Public `/api/health`, home, and `/app.js` returned `200`; PM2 `turingmarket` is online and Nginx is active. / 公网 `/api/health`、首页和 `/app.js` 返回 `200`；PM2 `turingmarket` online，Nginx active。
- Authenticated checks confirmed administrator login, `/api/auth/me`, all-conversation AI audit read, M4 collaboration read, controlled invalid-contract rejection, and logout. / 登录态检查确认管理员登录、`/api/auth/me`、全部 AI 对话审计读取、M4 合作读取、异常合同路由受控拒绝和注销。
- Production hashes exactly match local accepted files for `app.js`, campaign collaboration service, knowledge service, RAG service, and migration `016`. / `app.js`、合作服务、知识服务、RAG 服务和迁移 `016` 的生产哈希与本地接受文件完全一致。
- SQLite migrations are `16/16`, schema is `v16`, `integrity_check=ok`, foreign-key violations are `0`, active sessions are `0`, and no contract document was created for acceptance. / SQLite 迁移为 `16/16`、schema 为 `v16`、`integrity_check=ok`、外键异常 `0`、活动会话 `0`，验收未创建合同文档。
- No real Feishu write was performed. Frozen proposal `ppt.js` SHA-256 remains `f311a7b33ee28e64c8e19a14bae436101272dd17bf2f4f8c5d181d57dd0e291e`. / 未执行真实飞书写入；冻结方案 `ppt.js` SHA-256 保持不变。

## Delivery Cadence And Next / 发布节奏与下一步

- Normal features use the approved small-batch cadence: exact affected tests, syntax/diff/secret checks, one independent review, recoverable backup, immediate production deployment, and online feature smoke. / 普通功能采用已批准的小步节奏：精确受影响测试、语法/差异/密钥检查、一次独立审查、可恢复备份、立即生产部署和线上功能冒烟。
- Full suites are reserved for schema/data changes, authentication or security, shared runtime, external writes, broad cross-module work, scheduled windows, and phase closeout. / 完整套件仅用于 schema/数据变更、鉴权或安全、共享运行时、外部写入、大范围跨模块工作、计划窗口和阶段收口。
- PDF contract custody is accepted. E-signature, content review, publishing, payment, settlement, and review remain independent Phase 7 slices. / PDF 合同保管已通过验收；电子签约、内容审核、发布、付款、结算和复盘仍是阶段 7 的独立切片。
- Real Feishu delivery remains gated by approved production configuration and a test campaign. / 真实飞书投递仍须具备获批生产配置和测试活动后单独验收。
