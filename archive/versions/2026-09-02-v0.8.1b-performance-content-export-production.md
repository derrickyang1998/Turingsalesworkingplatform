# v0.8.1b Phase 7B 内容效果 CSV 导出生产归档

- 实现提交：`4799328`。
- 内容监控现在可导出“当前筛选”或“全部内容”。当前筛选复用活动内搜索、平台与标签条件；全部内容只移除这些内容过滤，不会跨活动或跨组织。
- 导出为 UTF-8 BOM CSV，服务端限制为 5,000 行并记录 `performance_content_export` 审计事件；前端在导出期间禁用两个按钮，避免重复请求。
- 只有具有商业查看权限的用户能导出花费、收入、ROI、ROAS 和商业纠正原因；团队成员导出只含公开内容和观测数据。
- CSV 转义覆盖任意前置 JavaScript 空白后的公式起始字符，避免业务文本被表格软件解释为公式。
- 定向性能服务、路由与前端合同 `17/17` 通过；JavaScript 语法、`git diff --check` 和本地发布预检通过。首轮独立审查发现 Unicode 空白公式绕过，先加入失败回归再修复，复审结论为 `APPROVE`。
- 生产候选完成可信来源、隔离迁移演练、真实 Express 重放 `8/8`、发布守卫 `21/21` 和部署浏览器烟测 `2/2`。
- 最终备份：`/root/turingmarket/backups/v060-crm-sales-workspace-20260902-112211`；`SHA256SUMS` 已验证，清单 SHA-256：`85a92add0a47549f048693d644e95179da2ec65961924e54f082c0f09e51f128`。
- 线上验收：公网与回环健康 `ok`、Parser ready、`/performance-monitor` 与 `/performance-dashboard` 均为 `200`、PM2/Nginx online；五个发布文件线上哈希与本地一致；SQLite `quick_check=ok`、外键异常 `0`、schema `v10`。
- 首个候选在生产变更前网络传输重置，内置恢复仅清理候选并确认生产未变更；第二次受控发布完成 `DEPLOY_OK`。未在真实活动中创建视频或商业数据。
- 下一切片为 `7B.2a`：已批准来源能力矩阵与飞书字段映射预览，不启用真实 provider 抓取或飞书写入；随后再实现定期更新、事实驱动 AI 复盘和客户项目报告。
