# v0.1.0 - 交接与版本治理基线

日期：2026-06-30  
仓库路径：`C:\Users\29272\Documents\在线商务平台`  
Obsidian 路径：`D:\主盘\图灵集市\图灵商务平台开发`  
GitHub 目标：`https://github.com/derrickyang1998/Turingsalesworkingplatform`

## 本次目标

把当前商务平台开发信息整理为可交接资料：版本信息、运行方式、服务器线索、密钥管理方式、团队版本迭代规范，并准备同步到 GitHub。

## 当前状态

- 本地 Git 仓库此前没有提交记录。
- 当前仓库没有配置远端。
- `gh` 已安装，但当前机器未登录 GitHub CLI。
- 当前代码是静态前端版本，入口为 `site_home.html`。
- 业务模块位于 `js/modules/`。
- 数据存储使用浏览器 `localStorage`。
- 当前工作区未发现 `.env`、后端服务、数据库配置或真实 API key。

## 本次新增/整理

- `README.md`：项目入口说明。
- `CHANGELOG.md`：版本历史。
- `.env.example`：公开环境变量模板。
- `.gitignore`：排除密钥、本地环境和临时文件。
- `docs/HANDOFF.md`：新设备/新人交接手册。
- `docs/OPERATIONS.md`：服务器、部署与密钥说明。
- `docs/DEVELOPMENT_WORKFLOW.md`：团队版本迭代规范。
- `docs/version-records/2026-06-30-v0.1.0-handoff-baseline.md`：本次版本记录。
- Obsidian 归档目录：同步公开文档，并单独保存私有迁移资料副本。

## 服务器与密钥归档

- 远端健康检查 `http://<protected-production-host>/api/health` 在 2026-06-30 返回 `status=ok`。
- 历史迁移资料显示远端目录为 `/root/turingmarket/platform`，历史 Node 端口为 `3002`，Nginx 代理端口为 `80`。
- 真实密钥不写入 GitHub。私有副本放在 Obsidian 的 `99-private` 目录。

## 验证

```powershell
node --check js/shared/utils.js
node --check js/shared/dom.js
node --check js/app.js
node --check js/modules/m0-customer.js
node --check js/modules/m1-brand.js
node --check js/modules/m2-strategy.js
node --check js/modules/m3-demand.js
node --check js/modules/m4-influencer.js
node --check js/modules/m5-assistant.js
node --check js/modules/admin.js
node --check js/modules/kb.js
```

结果：全部通过。

## 后续建议

- 登录 GitHub CLI 或确认 Git 凭证后推送当前基线。
- 建立 GitHub Issues 和 PR 模板。
- 尽快决定：继续静态版迭代，还是恢复完整后端生产线。
- 如果接入真实 AI，继续保留“AI 草稿 -> 人工确认 -> 导出/发布”的流程。

